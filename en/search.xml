<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>LangChain4j TextSplitter — Implementation Notes</title>
    <url>/en/2025/01/30/2025-01-30-langchain4j-text-splitter/</url>
    <content><![CDATA[<h1 id="langchain4j-splitter-package-document-splitting-source-walkthrough"><a class="markdownIt-Anchor" href="#langchain4j-splitter-package-document-splitting-source-walkthrough"></a> LangChain4j Splitter Package — Document Splitting Source Walkthrough</h1>
<h2 id="overview"><a class="markdownIt-Anchor" href="#overview"></a> Overview</h2>
<p>LangChain4j provides document splitting in the package <code>dev.langchain4j.data.document.splitter</code>: the <code>DocumentSplitter</code> interface turns a <code>Document</code> into multiple <code>TextSegment</code>s, with support for character, line, paragraph, sentence, word, and regex-based splitting.</p>
<ul>
<li>Package docs: <a href="https://docs.langchain4j.dev/apidocs/dev/langchain4j/data/document/splitter/package-summary.html">dev.langchain4j.data.document.splitter</a></li>
</ul>
<h2 id="core-interface"><a class="markdownIt-Anchor" href="#core-interface"></a> Core interface</h2>
<h3 id="documentsplitter"><a class="markdownIt-Anchor" href="#documentsplitter"></a> DocumentSplitter</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// Conceptual interface (LangChain4j)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">DocumentSplitter</span> {</span><br><span class="line">    List&lt;TextSegment&gt; <span class="title function_">split</span><span class="params">(Document document)</span>;</span><br><span class="line">    List&lt;TextSegment&gt; <span class="title function_">splitAll</span><span class="params">(Document... documents)</span>;</span><br><span class="line">    List&lt;TextSegment&gt; <span class="title function_">splitAll</span><span class="params">(List&lt;Document&gt; documents)</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>Input: <code>Document</code></li>
<li>Output: <code>List&lt;TextSegment&gt;</code>, each segment bounded by <code>maxSegmentSize</code></li>
</ul>
<h2 id="built-in-splitters-langchain4j"><a class="markdownIt-Anchor" href="#built-in-splitters-langchain4j"></a> Built-in splitters (LangChain4j)</h2>
<table>
<thead>
<tr>
<th>Class</th>
<th>Separator / behavior</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>DocumentByParagraphSplitter</code></td>
<td><code>"\n\n"</code></td>
<td>By paragraph</td>
</tr>
<tr>
<td><code>DocumentByLineSplitter</code></td>
<td><code>"\n"</code></td>
<td>By line</td>
</tr>
<tr>
<td><code>DocumentBySentenceSplitter</code></td>
<td>Sentence boundary</td>
<td>By sentence</td>
</tr>
<tr>
<td><code>DocumentByWordSplitter</code></td>
<td><code>" "</code></td>
<td>By word</td>
</tr>
<tr>
<td><code>DocumentByCharacterSplitter</code></td>
<td>Character</td>
<td>By character</td>
</tr>
<tr>
<td><code>DocumentByRegexSplitter</code></td>
<td>Regex</td>
<td>By regex</td>
</tr>
<tr>
<td><code>HierarchicalDocumentSplitter</code></td>
<td>-</td>
<td>Base for hierarchical splitting</td>
</tr>
</tbody>
</table>
<h2 id="source-hierarchicaldocumentsplitter-and-segmentbuilder"><a class="markdownIt-Anchor" href="#source-hierarchicaldocumentsplitter-and-segmentbuilder"></a> Source: HierarchicalDocumentSplitter and SegmentBuilder</h2>
<h3 id="1-overall-flow-hierarchicaldocumentsplittersplit"><a class="markdownIt-Anchor" href="#1-overall-flow-hierarchicaldocumentsplittersplit"></a> 1. Overall flow (HierarchicalDocumentSplitter.split)</h3>
<p>The core of hierarchical splitting is <code>HierarchicalDocumentSplitter.split(Document)</code>:</p>
<ol>
<li>Use the subclass’s <code>split(text)</code> to get <code>parts</code> at the current granularity (e.g. paragraph, line, word).</li>
<li>Use <strong>SegmentBuilder</strong> to combine parts into segments of at most <code>maxSegmentSize</code>; when a part doesn’t fit, flush the current segment, then take a tail overlap of size <code>maxOverlapSize</code> as the start of the next segment.</li>
<li>If a <strong>single part</strong> exceeds <code>maxSegmentSize</code>, hand it to <strong>subSplitter</strong> (e.g. long paragraph → split by sentence); sub-split results are reattached to the original Document’s metadata by index.</li>
</ol>
<p>Size can be character count or token count via <code>TokenCountEstimator</code> and <code>estimateSize(text)</code> in SegmentBuilder.</p>
<h3 id="2-segmentbuilder-role-and-structure"><a class="markdownIt-Anchor" href="#2-segmentbuilder-role-and-structure"></a> 2. SegmentBuilder role and structure</h3>
<p><code>SegmentBuilder</code> (<code>dev.langchain4j.data.document.splitter.SegmentBuilder</code>) is a package-level utility (<code>@Internal</code>). It has <strong>no private inner classes</strong> and only: concatenates text parts with a given separator without exceeding <code>maxSegmentSize</code>, and supports “overlap from end” by prepending in reverse order.</p>
<p><strong>Private fields (6)</strong></p>
<table>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Meaning</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>maxSegmentSize</code></td>
<td><code>int</code></td>
<td>Max length per segment (chars or tokens)</td>
</tr>
<tr>
<td><code>sizeFunction</code></td>
<td><code>Function&lt;String, Integer&gt;</code></td>
<td>Text length function</td>
</tr>
<tr>
<td><code>joinSeparator</code></td>
<td><code>String</code></td>
<td>Separator when joining (e.g. <code>"\n\n"</code>)</td>
</tr>
<tr>
<td><code>joinSeparatorSize</code></td>
<td><code>int</code></td>
<td>Length of the separator</td>
</tr>
<tr>
<td><code>segment</code></td>
<td><code>String</code></td>
<td>Current segment content</td>
</tr>
<tr>
<td><code>segmentSize</code></td>
<td><code>int</code></td>
<td>Current segment length (from sizeFunction)</td>
</tr>
</tbody>
</table>
<p><strong>Public API</strong></p>
<table>
<thead>
<tr>
<th>Method</th>
<th>Purpose</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>getSize()</code></td>
<td>Current segment length</td>
</tr>
<tr>
<td><code>hasSpaceFor(String text)</code> / <code>hasSpaceFor(int size)</code></td>
<td>Whether adding one more part still keeps size ≤ maxSegmentSize</td>
</tr>
<tr>
<td><code>sizeOf(String text)</code></td>
<td>Compute text length via sizeFunction</td>
</tr>
<tr>
<td><code>append(String text)</code></td>
<td>Append with joinSeparator (main flow)</td>
</tr>
<tr>
<td><code>prepend(String text)</code></td>
<td>Prepend (for overlap from segment end)</td>
</tr>
<tr>
<td><code>isNotEmpty()</code></td>
<td>Whether current segment is non-empty</td>
</tr>
<tr>
<td><code>toString()</code></td>
<td>Returns <code>segment.trim()</code></td>
</tr>
<tr>
<td><code>reset()</code></td>
<td>Clear current segment for next one</td>
</tr>
</tbody>
</table>
<h3 id="3-overlap-and-subsplitter"><a class="markdownIt-Anchor" href="#3-overlap-and-subsplitter"></a> 3. Overlap and subSplitter</h3>
<ul>
<li><strong>Overlap</strong>: After flushing a segment, <code>overlapFrom(segmentText)</code> takes up to <code>maxOverlapSize</code> from the <strong>end by sentence</strong> (in reverse order) as the start of the next segment, so adjacent segments overlap without breaking semantics.</li>
<li><strong>subSplitter</strong>: When a single part is too long, a sub-splitter (e.g. <code>DocumentBySentenceSplitter</code>) is used; the default chain is provided by <code>defaultSubSplitter()</code> (e.g. paragraph → sentence → word → character).</li>
</ul>
<h2 id="recommended-usage-documentsplittersrecursive"><a class="markdownIt-Anchor" href="#recommended-usage-documentsplittersrecursive"></a> Recommended usage: DocumentSplitters.recursive()</h2>
<p>LangChain4j recommends <strong>recursive</strong> splitting for general text: try paragraph → line → sentence → word → character until <code>maxSegmentSize</code> is satisfied.</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// LangChain4j example</span></span><br><span class="line"><span class="type">DocumentSplitter</span> <span class="variable">splitter</span> <span class="operator">=</span> DocumentSplitters.recursive(</span><br><span class="line">    <span class="number">1000</span>,  <span class="comment">// maxSegmentSize</span></span><br><span class="line">    <span class="number">200</span>    <span class="comment">// maxOverlap</span></span><br><span class="line">);</span><br><span class="line">List&lt;TextSegment&gt; segments = splitter.split(document);</span><br></pre></td></tr></tbody></table></figure>
<p>Supports both character count and token count for size.</p>
<h2 id="references"><a class="markdownIt-Anchor" href="#references"></a> References</h2>
<ul>
<li><a href="https://docs.langchain4j.dev/apidocs/dev/langchain4j/data/document/splitter/DocumentSplitters.html">LangChain4j DocumentSplitters</a></li>
<li><a href="https://docs.langchain4j.dev/apidocs/dev/langchain4j/data/document/splitter/DocumentByParagraphSplitter.html">LangChain4j DocumentByParagraphSplitter</a></li>
<li><a href="https://docs.langchain4j.dev/apidocs/dev/langchain4j/data/document/splitter/package-summary.html">LangChain4j package summary</a></li>
</ul>
]]></content>
      <categories>
        <category>agent</category>
        <category>source-reading</category>
        <category>textSplitter</category>
      </categories>
      <tags>
        <tag>agent</tag>
        <tag>java</tag>
        <tag>langchain4j</tag>
        <tag>source-reading</tag>
      </tags>
  </entry>
  <entry>
    <title>Python RecursiveCharacterTextSplitter — Source Walkthrough</title>
    <url>/en/2025/01/30/2025-01-30-python-recursive-character-text-splitter/</url>
    <content><![CDATA[<h1 id="python-recursivecharactertextsplitter-source-walkthrough"><a class="markdownIt-Anchor" href="#python-recursivecharactertextsplitter-source-walkthrough"></a> Python RecursiveCharacterTextSplitter — Source Walkthrough</h1>
<h2 id="overview"><a class="markdownIt-Anchor" href="#overview"></a> Overview</h2>
<p><code>RecursiveCharacterTextSplitter</code> is the <strong>recommended general-purpose text splitter</strong> in LangChain Python. It recursively splits text by <strong>separator priority</strong>, trying to keep paragraphs, lines, and words intact until each chunk fits within the <code>chunk_size</code> limit.</p>
<ul>
<li>API reference: <a href="https://python.langchain.com/api_reference/text_splitters/character/langchain_text_splitters.character.RecursiveCharacterTextSplitter.html">RecursiveCharacterTextSplitter</a></li>
</ul>
<h2 id="default-separator-order"><a class="markdownIt-Anchor" href="#default-separator-order"></a> Default separator order</h2>
<p>Separators are tried in order until the resulting chunks do not exceed <code>chunk_size</code>:</p>
<table>
<thead>
<tr>
<th>Priority</th>
<th>Separator</th>
<th>Meaning</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td><code>"\n\n"</code></td>
<td>Paragraph</td>
</tr>
<tr>
<td>2</td>
<td><code>"\n"</code></td>
<td>Line</td>
</tr>
<tr>
<td>3</td>
<td><code>" "</code></td>
<td>Space (word)</td>
</tr>
<tr>
<td>4</td>
<td><code>""</code></td>
<td>Character (fallback)</td>
</tr>
</tbody>
</table>
<p>So it first splits by paragraph, then by line if still too long, then by space, and finally by character.</p>
<h2 id="main-parameters"><a class="markdownIt-Anchor" href="#main-parameters"></a> Main parameters</h2>
<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Default</th>
<th>Description</th>
<th>Source</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>chunk_size</code></td>
<td>int</td>
<td>1000</td>
<td>Target length per chunk (chars or tokens)</td>
<td><strong>TextSplitter</strong></td>
</tr>
<tr>
<td><code>chunk_overlap</code></td>
<td>int</td>
<td>200</td>
<td>Overlap between adjacent chunks</td>
<td><strong>TextSplitter</strong></td>
</tr>
<tr>
<td><code>length_function</code></td>
<td>Callable</td>
<td><code>len</code></td>
<td>How to measure length (chars or tokens)</td>
<td><strong>TextSplitter</strong></td>
</tr>
<tr>
<td><code>separators</code></td>
<td>List[str]</td>
<td><code>["\n\n", "\n", " ", ""]</code></td>
<td>Separator list, used in order</td>
<td>This class / CharacterTextSplitter</td>
</tr>
<tr>
<td><code>keep_separator</code></td>
<td>bool</td>
<td>True</td>
<td>Whether to keep the separator in split text</td>
<td>This class / CharacterTextSplitter</td>
</tr>
<tr>
<td><code>is_separator_regex</code></td>
<td>bool</td>
<td>False</td>
<td>Whether separators are treated as regex</td>
<td>This class / CharacterTextSplitter</td>
</tr>
</tbody>
</table>
<p><strong>From TextSplitter</strong>: <code>chunk_size</code>, <code>chunk_overlap</code>, <code>length_function</code>. This class adds separator-related options: <code>separators</code>, <code>keep_separator</code>, <code>is_separator_regex</code>.</p>
<h2 id="source-walkthrough"><a class="markdownIt-Anchor" href="#source-walkthrough"></a> Source walkthrough</h2>
<p>LangChain Python’s <code>RecursiveCharacterTextSplitter</code> extends <code>TextSplitter</code>. The core logic is <strong>recursive splitting by separators</strong> and <strong>merging with overlap</strong>.</p>
<h3 id="1-recursive-split-_split_text"><a class="markdownIt-Anchor" href="#1-recursive-split-_split_text"></a> 1. Recursive split: <code>_split_text</code></h3>
<p>Split text by the “current” separator; if a segment still exceeds <code>chunk_size</code>, recursively split it with the “next finer” separator.</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">_split_text</span>(<span class="params">self, text: <span class="built_in">str</span>, separators: <span class="type">List</span>[<span class="built_in">str</span>]</span>) -&gt; <span class="type">List</span>[<span class="built_in">str</span>]:</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> text:</span><br><span class="line">        <span class="keyword">return</span> []</span><br><span class="line">    <span class="comment"># No more separators: fallback to character split</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> separators:</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>._merge_splits(<span class="built_in">list</span>(text), <span class="string">""</span>)</span><br><span class="line"></span><br><span class="line">    sep = separators[<span class="number">0</span>]                    <span class="comment"># Current separator (e.g. "\n\n")</span></span><br><span class="line">    rest_separators = separators[<span class="number">1</span>:]       <span class="comment"># Finer separators (e.g. "\n", " ", "")</span></span><br><span class="line">    splits = <span class="variable language_">self</span>._split_on_separator(text, sep)  <span class="comment"># Split by sep</span></span><br><span class="line"></span><br><span class="line">    good_splits = []</span><br><span class="line">    <span class="keyword">for</span> s <span class="keyword">in</span> splits:</span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>._length_function(s) &lt;= <span class="variable language_">self</span>._chunk_size:</span><br><span class="line">            good_splits.append(s)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">if</span> good_splits:</span><br><span class="line">                <span class="keyword">yield</span> <span class="keyword">from</span> <span class="variable language_">self</span>._merge_splits(good_splits, sep)</span><br><span class="line">                good_splits = []</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> rest_separators:</span><br><span class="line">                <span class="keyword">yield</span> <span class="keyword">from</span> <span class="variable language_">self</span>._merge_splits([s], sep)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">yield</span> <span class="keyword">from</span> <span class="variable language_">self</span>._split_text(s, rest_separators)  <span class="comment"># Recurse</span></span><br><span class="line">    <span class="keyword">if</span> good_splits:</span><br><span class="line">        <span class="keyword">yield</span> <span class="keyword">from</span> <span class="variable language_">self</span>._merge_splits(good_splits, sep)</span><br></pre></td></tr></tbody></table></figure>
<p>Notes:</p>
<ul>
<li><code>_split_on_separator(text, sep)</code> produces the current-level <code>splits</code>.</li>
<li>Segments with length ≤ <code>chunk_size</code> go into <code>good_splits</code>, then get merged with <code>_merge_splits</code> and yielded.</li>
<li>Segments longer than <code>chunk_size</code>: if <code>rest_separators</code> is non-empty, recurse with <code>_split_text(s, rest_separators)</code>; otherwise merge at current level or fall back to character split.</li>
</ul>
<h3 id="2-merge-and-overlap-_merge_splits"><a class="markdownIt-Anchor" href="#2-merge-and-overlap-_merge_splits"></a> 2. Merge and overlap: <code>_merge_splits</code></h3>
<p>Merge a batch of small segments into chunks of about <code>chunk_size</code>, with <code>chunk_overlap</code> between adjacent chunks.</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">_merge_splits</span>(<span class="params">self, splits: <span class="type">List</span>[<span class="built_in">str</span>], separator: <span class="built_in">str</span></span>) -&gt; <span class="type">List</span>[<span class="built_in">str</span>]:</span><br><span class="line">    docs = []</span><br><span class="line">    current = []</span><br><span class="line">    total_len = <span class="number">0</span></span><br><span class="line">    sep_len = <span class="variable language_">self</span>._length_function(separator)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> s <span class="keyword">in</span> splits:</span><br><span class="line">        n = <span class="variable language_">self</span>._length_function(s) + (sep_len <span class="keyword">if</span> current <span class="keyword">else</span> <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">if</span> total_len + n &gt; <span class="variable language_">self</span>._chunk_size <span class="keyword">and</span> current:</span><br><span class="line">            doc = <span class="variable language_">self</span>._join_docs(current, separator)</span><br><span class="line">            docs.append(doc)</span><br><span class="line">            <span class="comment"># Overlap: drop from the front until remaining length ≈ chunk_overlap</span></span><br><span class="line">            <span class="keyword">while</span> current <span class="keyword">and</span> (total_len &gt; <span class="variable language_">self</span>._chunk_overlap <span class="keyword">or</span> total_len + n &gt; <span class="variable language_">self</span>._chunk_size):</span><br><span class="line">                total_len -= <span class="variable language_">self</span>._length_function(current[<span class="number">0</span>]) + (sep_len <span class="keyword">if</span> <span class="built_in">len</span>(current) &gt; <span class="number">1</span> <span class="keyword">else</span> <span class="number">0</span>)</span><br><span class="line">                current = current[<span class="number">1</span>:]</span><br><span class="line">            total_len = <span class="built_in">sum</span>(<span class="variable language_">self</span>._length_function(x) <span class="keyword">for</span> x <span class="keyword">in</span> current) + sep_len * (<span class="built_in">len</span>(current) - <span class="number">1</span>)</span><br><span class="line">        current.append(s)</span><br><span class="line">        total_len += n</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> current:</span><br><span class="line">        docs.append(<span class="variable language_">self</span>._join_docs(current, separator))</span><br><span class="line">    <span class="keyword">return</span> docs</span><br></pre></td></tr></tbody></table></figure>
<p>Notes:</p>
<ul>
<li>Iterate over <code>splits</code>, accumulate length; when over <code>chunk_size</code>, emit the current chunk, then trim from the front according to <code>chunk_overlap</code> to get overlap.</li>
<li><code>_join_docs</code> joins the list with <code>separator</code> into the final string.</li>
</ul>
<h3 id="3-call-chain"><a class="markdownIt-Anchor" href="#3-call-chain"></a> 3. Call chain</h3>
<table>
<thead>
<tr>
<th>Entry method</th>
<th>Internal call</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>split_text(text)</code></td>
<td><code>_split_text(text, self.separators)</code> → list of chunks after recursive split + merge</td>
</tr>
<tr>
<td><code>split_documents(documents)</code></td>
<td>For each document’s <code>page_content</code>, call <code>split_text</code>, then reattach metadata by index</td>
</tr>
<tr>
<td><code>create_documents(texts, metadatas)</code></td>
<td>For each <code>text</code>, call <code>split_text</code>, build Document with <code>metadatas</code></td>
</tr>
</tbody>
</table>
<h2 id="common-methods"><a class="markdownIt-Anchor" href="#common-methods"></a> Common methods</h2>
<table>
<thead>
<tr>
<th>Method</th>
<th>Description</th>
<th>Source</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>split_text(text: str) -&gt; List[str]</code></td>
<td>Split a single string; return list of strings</td>
<td><strong>TextSplitter</strong> (this class overrides internals, still uses <code>_split_text</code>)</td>
</tr>
<tr>
<td><code>create_documents(texts: List[str], metadatas=None)</code></td>
<td>Create list of Documents from list of strings</td>
<td><strong>TextSplitter</strong></td>
</tr>
<tr>
<td><code>split_documents(documents: List[Document]) -&gt; List[Document]</code></td>
<td>Split list of Documents, keep metadata</td>
<td><strong>TextSplitter</strong></td>
</tr>
<tr>
<td><code>transform_documents(documents: List[Document]) -&gt; List[Document]</code></td>
<td>Same as <code>split_documents</code>, for pipelines</td>
<td><strong>TextSplitter</strong></td>
</tr>
<tr>
<td><code>from_tiktoken_encoder(...)</code></td>
<td>Class method: split by tiktoken token count</td>
<td>This class / CharacterTextSplitter</td>
</tr>
<tr>
<td><code>from_huggingface_tokenizer(...)</code></td>
<td>Class method: split by HuggingFace tokenizer</td>
<td>This class / CharacterTextSplitter</td>
</tr>
<tr>
<td><code>from_language(language, ...)</code></td>
<td>Class method: language-specific separator presets</td>
<td>This class / CharacterTextSplitter</td>
</tr>
</tbody>
</table>
<p><strong>From TextSplitter</strong>: <code>split_text</code>, <code>create_documents</code>, <code>split_documents</code>, <code>transform_documents</code>. This class changes behavior by overriding <code>_split_text</code> and related logic.</p>
<h2 id="algorithm-summary"><a class="markdownIt-Anchor" href="#algorithm-summary"></a> Algorithm summary</h2>
<ol>
<li><strong>Recursion</strong>: Split with the current separator; if any segment is still longer than <code>chunk_size</code>, recursively split it with the next separator.</li>
<li><strong>Overlap</strong>: Keep <code>chunk_overlap</code> characters (or tokens) between adjacent chunks to avoid cutting semantics at boundaries.</li>
<li><strong>Length</strong>: Default is <code>len()</code> (character count); <code>length_function</code> can be set to token count (e.g. tiktoken).</li>
</ol>
]]></content>
      <categories>
        <category>agent</category>
        <category>source-reading</category>
        <category>textSplitter</category>
      </categories>
      <tags>
        <tag>agent</tag>
        <tag>source-reading</tag>
        <tag>python</tag>
        <tag>langchain</tag>
      </tags>
  </entry>
</search>
