<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Python RecursiveCharacterTextSplitter 源码解读</title>
    <url>/2025/01/29/2025-01-30-python-recursive-character-text-splitter/</url>
    <content><![CDATA[<h1 id="python-recursivecharactertextsplitter-源码解读"><a class="markdownIt-Anchor" href="#python-recursivecharactertextsplitter-源码解读"></a> Python RecursiveCharacterTextSplitter 源码解读</h1>
<h2 id="概述"><a class="markdownIt-Anchor" href="#概述"></a> 概述</h2>
<p><code>RecursiveCharacterTextSplitter</code> 是 LangChain Python 中<strong>推荐的通用文本分割器</strong>。它按<strong>分隔符优先级</strong>递归切分文本，尽量保持段落、句子、词语的完整性，直到每个块满足 <code>chunk_size</code> 限制。</p>
<ul>
<li>官方 API：<a href="https://python.langchain.com/api_reference/text_splitters/character/langchain_text_splitters.character.RecursiveCharacterTextSplitter.html">RecursiveCharacterTextSplitter</a></li>
</ul>
<h2 id="默认分隔符顺序"><a class="markdownIt-Anchor" href="#默认分隔符顺序"></a> 默认分隔符顺序</h2>
<p>按优先级依次尝试，直到切出的块不超过 <code>chunk_size</code>：</p>
<table>
<thead>
<tr>
<th>优先级</th>
<th>分隔符</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td><code>"\n\n"</code></td>
<td>段落</td>
</tr>
<tr>
<td>2</td>
<td><code>"\n"</code></td>
<td>行</td>
</tr>
<tr>
<td>3</td>
<td><code>" "</code></td>
<td>空格（词）</td>
</tr>
<tr>
<td>4</td>
<td><code>""</code></td>
<td>字符（兜底）</td>
</tr>
</tbody>
</table>
<p>即：先按段落切，超长再按行，再按空格，最后才按字符切。</p>
<h2 id="核心参数"><a class="markdownIt-Anchor" href="#核心参数"></a> 核心参数</h2>
<table>
<thead>
<tr>
<th>参数</th>
<th>类型</th>
<th>默认值</th>
<th>说明</th>
<th>来源</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>chunk_size</code></td>
<td>int</td>
<td>1000</td>
<td>每个块的目标长度（字符数或 token 数）</td>
<td><strong>继承 TextSplitter</strong></td>
</tr>
<tr>
<td><code>chunk_overlap</code></td>
<td>int</td>
<td>200</td>
<td>相邻块之间的重叠长度</td>
<td><strong>继承 TextSplitter</strong></td>
</tr>
<tr>
<td><code>length_function</code></td>
<td>Callable</td>
<td><code>len</code></td>
<td>计算长度的方式（字符数或 token 数）</td>
<td><strong>继承 TextSplitter</strong></td>
</tr>
<tr>
<td><code>separators</code></td>
<td>List[str]</td>
<td><code>["\n\n", "\n", " ", ""]</code></td>
<td>分隔符列表，按顺序使用</td>
<td>本类 / CharacterTextSplitter</td>
</tr>
<tr>
<td><code>keep_separator</code></td>
<td>bool</td>
<td>True</td>
<td>是否在切分后的文本中保留分隔符</td>
<td>本类 / CharacterTextSplitter</td>
</tr>
<tr>
<td><code>is_separator_regex</code></td>
<td>bool</td>
<td>False</td>
<td>分隔符是否按正则解析</td>
<td>本类 / CharacterTextSplitter</td>
</tr>
</tbody>
</table>
<p><strong>继承自 TextSplitter 的参数</strong>：<code>chunk_size</code>、<code>chunk_overlap</code>、<code>length_function</code>（基类定义块大小、重叠与长度计算方式；本类在此基础上增加 <code>separators</code>、<code>keep_separator</code>、<code>is_separator_regex</code> 等与分隔符相关的配置）。</p>
<h2 id="源码解读"><a class="markdownIt-Anchor" href="#源码解读"></a> 源码解读</h2>
<p>LangChain Python 的 <code>RecursiveCharacterTextSplitter</code> 继承自 <code>TextSplitter</code>，核心逻辑在<strong>递归按分隔符切分</strong>和<strong>带重叠的合并</strong>两处。</p>
<h3 id="1-递归切分_split_text"><a class="markdownIt-Anchor" href="#1-递归切分_split_text"></a> 1. 递归切分：<code>_split_text</code></h3>
<p>按「当前分隔符」把文本拆成若干段；若某段仍超过 <code>chunk_size</code>，则用「下一个更细的分隔符」对该段递归切分。</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">_split_text</span>(<span class="params">self, text: <span class="built_in">str</span>, separators: <span class="type">List</span>[<span class="built_in">str</span>]</span>) -&gt; <span class="type">List</span>[<span class="built_in">str</span>]:</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> text:</span><br><span class="line">        <span class="keyword">return</span> []</span><br><span class="line">    <span class="comment"># 无更多分隔符时，按字符兜底</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> separators:</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>._merge_splits(<span class="built_in">list</span>(text), <span class="string">""</span>)</span><br><span class="line"></span><br><span class="line">    sep = separators[<span class="number">0</span>]                    <span class="comment"># 当前分隔符（如 "\n\n"）</span></span><br><span class="line">    rest_separators = separators[<span class="number">1</span>:]       <span class="comment"># 更细的分隔符（如 "\n", " ", ""）</span></span><br><span class="line">    splits = <span class="variable language_">self</span>._split_on_separator(text, sep)  <span class="comment"># 按 sep 切分</span></span><br><span class="line"></span><br><span class="line">    good_splits = []</span><br><span class="line">    <span class="keyword">for</span> s <span class="keyword">in</span> splits:</span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>._length_function(s) &lt;= <span class="variable language_">self</span>._chunk_size:</span><br><span class="line">            good_splits.append(s)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">if</span> good_splits:</span><br><span class="line">                <span class="keyword">yield</span> <span class="keyword">from</span> <span class="variable language_">self</span>._merge_splits(good_splits, sep)</span><br><span class="line">                good_splits = []</span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">not</span> rest_separators:</span><br><span class="line">                <span class="keyword">yield</span> <span class="keyword">from</span> <span class="variable language_">self</span>._merge_splits([s], sep)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">yield</span> <span class="keyword">from</span> <span class="variable language_">self</span>._split_text(s, rest_separators)  <span class="comment"># 递归</span></span><br><span class="line">    <span class="keyword">if</span> good_splits:</span><br><span class="line">        <span class="keyword">yield</span> <span class="keyword">from</span> <span class="variable language_">self</span>._merge_splits(good_splits, sep)</span><br></pre></td></tr></tbody></table></figure>
<p>要点：</p>
<ul>
<li>用 <code>_split_on_separator(text, sep)</code> 得到当前层的 <code>splits</code>。</li>
<li>长度 ≤ <code>chunk_size</code> 的进入 <code>good_splits</code>，凑一批后 <code>_merge_splits</code> 合并并产出。</li>
<li>长度 &gt; <code>chunk_size</code> 的：若还有 <code>rest_separators</code> 则递归 <code>_split_text(s, rest_separators)</code>；否则按当前层合并或按字符兜底。</li>
</ul>
<h3 id="2-合并与重叠_merge_splits"><a class="markdownIt-Anchor" href="#2-合并与重叠_merge_splits"></a> 2. 合并与重叠：<code>_merge_splits</code></h3>
<p>将一批小片段合并成若干块，每块长度约 <code>chunk_size</code>，相邻块之间重叠 <code>chunk_overlap</code>。</p>
<figure class="highlight python"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">_merge_splits</span>(<span class="params">self, splits: <span class="type">List</span>[<span class="built_in">str</span>], separator: <span class="built_in">str</span></span>) -&gt; <span class="type">List</span>[<span class="built_in">str</span>]:</span><br><span class="line">    docs = []</span><br><span class="line">    current = []</span><br><span class="line">    total_len = <span class="number">0</span></span><br><span class="line">    sep_len = <span class="variable language_">self</span>._length_function(separator)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> s <span class="keyword">in</span> splits:</span><br><span class="line">        n = <span class="variable language_">self</span>._length_function(s) + (sep_len <span class="keyword">if</span> current <span class="keyword">else</span> <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">if</span> total_len + n &gt; <span class="variable language_">self</span>._chunk_size <span class="keyword">and</span> current:</span><br><span class="line">            doc = <span class="variable language_">self</span>._join_docs(current, separator)</span><br><span class="line">            docs.append(doc)</span><br><span class="line">            <span class="comment"># 重叠：从队头丢弃片段，使剩余长度约等于 chunk_overlap</span></span><br><span class="line">            <span class="keyword">while</span> current <span class="keyword">and</span> (total_len &gt; <span class="variable language_">self</span>._chunk_overlap <span class="keyword">or</span> total_len + n &gt; <span class="variable language_">self</span>._chunk_size):</span><br><span class="line">                total_len -= <span class="variable language_">self</span>._length_function(current[<span class="number">0</span>]) + (sep_len <span class="keyword">if</span> <span class="built_in">len</span>(current) &gt; <span class="number">1</span> <span class="keyword">else</span> <span class="number">0</span>)</span><br><span class="line">                current = current[<span class="number">1</span>:]</span><br><span class="line">            total_len = <span class="built_in">sum</span>(<span class="variable language_">self</span>._length_function(x) <span class="keyword">for</span> x <span class="keyword">in</span> current) + sep_len * (<span class="built_in">len</span>(current) - <span class="number">1</span>)</span><br><span class="line">        current.append(s)</span><br><span class="line">        total_len += n</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> current:</span><br><span class="line">        docs.append(<span class="variable language_">self</span>._join_docs(current, separator))</span><br><span class="line">    <span class="keyword">return</span> docs</span><br></pre></td></tr></tbody></table></figure>
<p>要点：</p>
<ul>
<li>顺序遍历 <code>splits</code>，累加长度；超过 <code>chunk_size</code> 时先产出当前块，再根据 <code>chunk_overlap</code> 从队头丢弃片段，实现块间重叠。</li>
<li><code>_join_docs</code> 用 <code>separator</code> 把列表拼成最终字符串。</li>
</ul>
<h3 id="3-调用链"><a class="markdownIt-Anchor" href="#3-调用链"></a> 3. 调用链</h3>
<table>
<thead>
<tr>
<th>入口方法</th>
<th>内部调用</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>split_text(text)</code></td>
<td><code>_split_text(text, self.separators)</code>，得到递归 + 合并后的块列表</td>
</tr>
<tr>
<td><code>split_documents(documents)</code></td>
<td>对每个 document 的 <code>page_content</code> 调 <code>split_text</code>，再按索引挂回 metadata</td>
</tr>
<tr>
<td><code>create_documents(texts, metadatas)</code></td>
<td>对每个 <code>text</code> 调 <code>split_text</code>，用 <code>metadatas</code> 构造 Document</td>
</tr>
</tbody>
</table>
<h2 id="常用方法"><a class="markdownIt-Anchor" href="#常用方法"></a> 常用方法</h2>
<table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>来源</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>split_text(text: str) -&gt; List[str]</code></td>
<td>对单个字符串切分，返回字符串列表</td>
<td><strong>继承 TextSplitter</strong>（本类重写内部逻辑，仍调 <code>_split_text</code>）</td>
</tr>
<tr>
<td><code>create_documents(texts: List[str], metadatas=None)</code></td>
<td>从字符串列表创建 Document 列表</td>
<td><strong>继承 TextSplitter</strong></td>
</tr>
<tr>
<td><code>split_documents(documents: List[Document]) -&gt; List[Document]</code></td>
<td>对 Document 列表切分，保留元数据</td>
<td><strong>继承 TextSplitter</strong></td>
</tr>
<tr>
<td><code>transform_documents(documents: List[Document]) -&gt; List[Document]</code></td>
<td>与 <code>split_documents</code> 等价，用于管道</td>
<td><strong>继承 TextSplitter</strong></td>
</tr>
<tr>
<td><code>from_tiktoken_encoder(...)</code></td>
<td>类方法，按 tiktoken 的 token 数切分</td>
<td>本类 / CharacterTextSplitter</td>
</tr>
<tr>
<td><code>from_huggingface_tokenizer(...)</code></td>
<td>类方法，按 HuggingFace tokenizer 切分</td>
<td>本类 / CharacterTextSplitter</td>
</tr>
<tr>
<td><code>from_language(language, ...)</code></td>
<td>类方法，按编程语言预设分隔符</td>
<td>本类 / CharacterTextSplitter</td>
</tr>
</tbody>
</table>
<p><strong>继承自 TextSplitter 的方法</strong>：<code>split_text</code>、<code>create_documents</code>、<code>split_documents</code>、<code>transform_documents</code>（基类定义接口与默认实现，本类通过重写 <code>_split_text</code> 等改变切分行为）。</p>
<h2 id="算法要点"><a class="markdownIt-Anchor" href="#算法要点"></a> 算法要点</h2>
<ol>
<li><strong>递归</strong>：用当前分隔符切分；若有片段长度仍大于 <code>chunk_size</code>，则用下一个分隔符对该片段递归切分。</li>
<li><strong>重叠</strong>：相邻块之间保留 <code>chunk_overlap</code> 个字符（或 token），避免语义在边界处断裂。</li>
<li><strong>长度计算</strong>：默认用 <code>len()</code>（字符数）；通过 <code>length_function</code> 可改为 token 数（如 tiktoken）。</li>
</ol>
]]></content>
      <categories>
        <category>agent</category>
        <category>源码解读</category>
        <category>textSplitter</category>
      </categories>
      <tags>
        <tag>agent</tag>
        <tag>python</tag>
        <tag>longchain</tag>
        <tag>源码解读</tag>
      </tags>
  </entry>
  <entry>
    <title>LangChain4j TextSplitter 实现说明</title>
    <url>/2025/01/29/2025-01-30-langchain4j-text-splitter/</url>
    <content><![CDATA[<h1 id="langchain4j-splitter包-文档分割-源码解读"><a class="markdownIt-Anchor" href="#langchain4j-splitter包-文档分割-源码解读"></a> LangChain4j Splitter 包 文档分割 源码解读</h1>
<h2 id="概述"><a class="markdownIt-Anchor" href="#概述"></a> 概述</h2>
<p>LangChain4j 在包 <code>dev.langchain4j.data.document.splitter</code> 下提供文档分割能力：通过 <code>DocumentSplitter</code> 接口将 <code>Document</code> 切分为多个 <code>TextSegment</code>，并支持按字符、行、段落、句子、词、正则等不同粒度。</p>
<ul>
<li>包文档：<a href="https://docs.langchain4j.dev/apidocs/dev/langchain4j/data/document/splitter/package-summary.html">dev.langchain4j.data.document.splitter</a></li>
</ul>
<h2 id="核心接口"><a class="markdownIt-Anchor" href="#核心接口"></a> 核心接口</h2>
<h3 id="documentsplitter"><a class="markdownIt-Anchor" href="#documentsplitter"></a> DocumentSplitter</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 概念上的接口（LangChain4j）</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">DocumentSplitter</span> {</span><br><span class="line">    List&lt;TextSegment&gt; <span class="title function_">split</span><span class="params">(Document document)</span>;</span><br><span class="line">    List&lt;TextSegment&gt; <span class="title function_">splitAll</span><span class="params">(Document... documents)</span>;</span><br><span class="line">    List&lt;TextSegment&gt; <span class="title function_">splitAll</span><span class="params">(List&lt;Document&gt; documents)</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>输入：<code>Document</code></li>
<li>输出：<code>List&lt;TextSegment&gt;</code>，每个 segment 受 <code>maxSegmentSize</code> 限制</li>
</ul>
<h2 id="内置分割器langchain4j"><a class="markdownIt-Anchor" href="#内置分割器langchain4j"></a> 内置分割器（LangChain4j）</h2>
<table>
<thead>
<tr>
<th>类名</th>
<th>分隔符 / 行为</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>DocumentByParagraphSplitter</code></td>
<td><code>"\n\n"</code></td>
<td>按段落</td>
</tr>
<tr>
<td><code>DocumentByLineSplitter</code></td>
<td><code>"\n"</code></td>
<td>按行</td>
</tr>
<tr>
<td><code>DocumentBySentenceSplitter</code></td>
<td>句子边界</td>
<td>按句子</td>
</tr>
<tr>
<td><code>DocumentByWordSplitter</code></td>
<td><code>" "</code></td>
<td>按词</td>
</tr>
<tr>
<td><code>DocumentByCharacterSplitter</code></td>
<td>字符</td>
<td>按字符</td>
</tr>
<tr>
<td><code>DocumentByRegexSplitter</code></td>
<td>正则</td>
<td>按正则</td>
</tr>
<tr>
<td><code>HierarchicalDocumentSplitter</code></td>
<td>-</td>
<td>层级分割基类</td>
</tr>
</tbody>
</table>
<h2 id="源码解读hierarchicaldocumentsplitter-与-segmentbuilder"><a class="markdownIt-Anchor" href="#源码解读hierarchicaldocumentsplitter-与-segmentbuilder"></a> 源码解读：HierarchicalDocumentSplitter 与 SegmentBuilder</h2>
<h3 id="1-整体流程hierarchicaldocumentsplittersplit"><a class="markdownIt-Anchor" href="#1-整体流程hierarchicaldocumentsplittersplit"></a> 1. 整体流程（HierarchicalDocumentSplitter.split）</h3>
<p>层级分割的核心在 <code>HierarchicalDocumentSplitter.split(Document)</code>：</p>
<ol>
<li>用子类实现的 <code>split(text)</code> 按当前粒度得到 <code>parts</code>（如段落、行、词等）。</li>
<li>用 <strong>SegmentBuilder</strong> 按 <code>maxSegmentSize</code> 把多个 part 拼成一个 segment；拼不下时先刷出当前 segment，再根据 <code>maxOverlapSize</code> 取尾部 overlap，作为下一段的开头。</li>
<li>若<strong>单个 part 本身</strong>就超过 <code>maxSegmentSize</code>，则交给 <strong>subSplitter</strong> 再切（如段落太长 → 按句子切），子分割结果再按索引挂回原 Document 的 metadata。</li>
</ol>
<p>支持按字符数或按 token 数：通过 <code>TokenCountEstimator</code> 与 <code>estimateSize(text)</code> 注入到 SegmentBuilder 的 “长度” 计算里。</p>
<h3 id="2-segmentbuilder-的职责与结构"><a class="markdownIt-Anchor" href="#2-segmentbuilder-的职责与结构"></a> 2. SegmentBuilder 的职责与结构</h3>
<p><code>SegmentBuilder</code>（<code>dev.langchain4j.data.document.splitter.SegmentBuilder</code>）是包级工具类（<code>@Internal</code>），<strong>没有定义任何私有内部类</strong>，只负责：在不超过 <code>maxSegmentSize</code> 的前提下，用指定分隔符拼接多段文本，并支持 “从尾部取 overlap” 时用 <code>prepend</code> 逆序拼。</p>
<p><strong>私有字段（6 个）</strong></p>
<table>
<thead>
<tr>
<th>字段</th>
<th>类型</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>maxSegmentSize</code></td>
<td><code>int</code></td>
<td>单段最大长度（字符或 token）</td>
</tr>
<tr>
<td><code>sizeFunction</code></td>
<td><code>Function&lt;String, Integer&gt;</code></td>
<td>计算文本长度的函数</td>
</tr>
<tr>
<td><code>joinSeparator</code></td>
<td><code>String</code></td>
<td>拼接时的分隔符（如 <code>"\n\n"</code>）</td>
</tr>
<tr>
<td><code>joinSeparatorSize</code></td>
<td><code>int</code></td>
<td>分隔符自身长度</td>
</tr>
<tr>
<td><code>segment</code></td>
<td><code>String</code></td>
<td>当前正在拼的段内容</td>
</tr>
<tr>
<td><code>segmentSize</code></td>
<td><code>int</code></td>
<td>当前段长度（由 sizeFunction 得到）</td>
</tr>
</tbody>
</table>
<p><strong>对外 API（均为 public 方法）</strong></p>
<table>
<thead>
<tr>
<th>方法</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>getSize()</code></td>
<td>当前段长度</td>
</tr>
<tr>
<td><code>hasSpaceFor(String text)</code> / <code>hasSpaceFor(int size)</code></td>
<td>再拼一段是否仍 ≤ maxSegmentSize</td>
</tr>
<tr>
<td><code>sizeOf(String text)</code></td>
<td>用 sizeFunction 计算 text 长度</td>
</tr>
<tr>
<td><code>append(String text)</code></td>
<td>末尾用 joinSeparator 拼接（主流程拼段）</td>
</tr>
<tr>
<td><code>prepend(String text)</code></td>
<td>开头拼接（overlap 时从段尾逆序填）</td>
</tr>
<tr>
<td><code>isNotEmpty()</code></td>
<td>当前段是否非空</td>
</tr>
<tr>
<td><code>toString()</code></td>
<td>返回 <code>segment.trim()</code></td>
</tr>
<tr>
<td><code>reset()</code></td>
<td>清空当前段，开始拼下一段</td>
</tr>
</tbody>
</table>
<h3 id="3-overlap-与-subsplitter"><a class="markdownIt-Anchor" href="#3-overlap-与-subsplitter"></a> 3. Overlap 与 subSplitter</h3>
<ul>
<li><strong>Overlap</strong>：刷出一个 segment 后，用 <code>overlapFrom(segmentText)</code> 从该段<strong>按句子</strong>逆序取最多 <code>maxOverlapSize</code> 的内容，作为下一段的开头，保证相邻段有重叠而不割裂语义。</li>
<li><strong>subSplitter</strong>：当单个 part 超长时，用子分割器（如 <code>DocumentBySentenceSplitter</code>）再切，子类通过 <code>defaultSubSplitter()</code> 指定默认实现（如段落 → 句子 → 词 → 字符的链条）。</li>
</ul>
<h2 id="推荐用法documentsplittersrecursive"><a class="markdownIt-Anchor" href="#推荐用法documentsplittersrecursive"></a> 推荐用法：DocumentSplitters.recursive()</h2>
<p>LangChain4j 推荐对通用文本使用 <strong>recursive</strong> 方式，即按层级尝试：段落 → 行 → 句子 → 词 → 字符，直到满足 <code>maxSegmentSize</code>。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// LangChain4j 用法示例</span></span><br><span class="line"><span class="type">DocumentSplitter</span> <span class="variable">splitter</span> <span class="operator">=</span> DocumentSplitters.recursive(</span><br><span class="line">    <span class="number">1000</span>,  <span class="comment">// maxSegmentSize</span></span><br><span class="line">    <span class="number">200</span>    <span class="comment">// maxOverlap</span></span><br><span class="line">);</span><br><span class="line">List&lt;TextSegment&gt; segments = splitter.split(document);</span><br></pre></td></tr></tbody></table></figure>
<p>支持按字符数或按 token 数控制大小。</p>
<h2 id="参考链接"><a class="markdownIt-Anchor" href="#参考链接"></a> 参考链接</h2>
<ul>
<li><a href="https://docs.langchain4j.dev/apidocs/dev/langchain4j/data/document/splitter/DocumentSplitters.html">LangChain4j DocumentSplitters</a></li>
<li><a href="https://docs.langchain4j.dev/apidocs/dev/langchain4j/data/document/splitter/DocumentByParagraphSplitter.html">LangChain4j DocumentByParagraphSplitter</a></li>
<li><a href="https://docs.langchain4j.dev/apidocs/dev/langchain4j/data/document/splitter/package-summary.html">LangChain4j 包文档</a></li>
</ul>
]]></content>
      <categories>
        <category>agent</category>
        <category>源码解读</category>
        <category>textSplitter</category>
      </categories>
      <tags>
        <tag>agent</tag>
        <tag>源码解读</tag>
        <tag>java</tag>
        <tag>langchain4j</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（一）单例模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-01-singleton/</url>
    <content><![CDATA[<h1 id="单例模式singleton"><a class="markdownIt-Anchor" href="#单例模式singleton"></a> 单例模式（Singleton）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>单例模式</strong>保证一个类在全局只有一个实例，并提供一个统一的访问点。</p>
<p><strong>解决的问题</strong>：某些对象（配置、连接池、线程池、日志等）只需要一份，多实例会造成资源浪费或状态不一致。</p>
<p><strong>定义</strong>：确保某个类只有一个实例，并自行实例化并向整个系统提供该实例。</p>
<hr>
<h2 id="二-结构与实现方式"><a class="markdownIt-Anchor" href="#二-结构与实现方式"></a> 二、结构与实现方式</h2>
<p><strong>角色</strong>：单例类（自己负责创建唯一实例，并提供静态获取方法）。</p>
<p><strong>常见实现</strong>：</p>
<table>
<thead>
<tr>
<th>方式</th>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody>
<tr>
<td>饿汉式（静态常量 / 静态块）</td>
<td>线程安全、实现简单</td>
<td>类加载即创建，可能用不到也占内存</td>
</tr>
<tr>
<td>懒汉式（synchronized 方法）</td>
<td>用时再创建</td>
<td>每次获取都加锁，性能差</td>
</tr>
<tr>
<td>双重检查锁（DCL）</td>
<td>懒加载 + 较少加锁</td>
<td>需 volatile，实现稍复杂</td>
</tr>
<tr>
<td>静态内部类</td>
<td>懒加载、线程安全、代码简洁</td>
<td>仅适用于无参构造</td>
</tr>
<tr>
<td>枚举</td>
<td>防反射 / 反序列化破坏、最简洁</td>
<td>无法懒加载</td>
</tr>
</tbody>
</table>
<p><strong>示例（静态内部类）</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Singleton</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">Singleton</span><span class="params">()</span> {}</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Holder</span> {</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Singleton</span> <span class="variable">INSTANCE</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Singleton</span>();</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title function_">getInstance</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">return</span> Holder.INSTANCE;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>Bean 默认作用域即为单例</strong>：<code>@Scope("singleton")</code>（默认），同一 IoC 容器内同一 Bean 定义只对应一个实例。</li>
<li><strong>ApplicationContext 本身</strong>：通常整个应用只有一个主 Context，可视为单例的 “容器”。</li>
<li><strong>常见单例 Bean</strong>：数据源、事务管理器、模板类（如 JdbcTemplate）、工具类等，由容器保证单例而非手写 getInstance ()。</li>
</ul>
<p><strong>注意</strong>：Spring 是 “容器内单例”，多容器可有多个实例；且 Bean 可由配置改为 prototype 等其它作用域。</p>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>配置、全局设置</strong>：如系统配置类、 feature 开关，全应用一份即可。</li>
<li><strong>连接池、线程池</strong>：如数据源、Executor，复用同一池，避免重复创建。</li>
<li><strong>无状态工具类</strong>：如日期 / 加密工具，无实例状态，单例减少对象数量。</li>
<li><strong>缓存、注册表</strong>：如本地缓存、服务注册表，需要全局唯一访问点。</li>
</ul>
<p><strong>不适用</strong>：有实例状态且不同调用方需要隔离的对象（应用 prototype 或多例）、需要延迟加载且对内存敏感又很少用的组件（可结合懒加载作用域）。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>单例模式通过 “唯一实例 + 统一访问点” 控制全局资源与无状态对象；Spring 中通过 Bean 的 <strong>singleton 作用域</strong> 实现，无需手写单例逻辑。业务上多用于配置、池、工具、缓存等 “全应用一份” 的场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>创建型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>单例模式</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（三）抽象工厂模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-03-abstract-factory/</url>
    <content><![CDATA[<h1 id="抽象工厂模式abstract-factory"><a class="markdownIt-Anchor" href="#抽象工厂模式abstract-factory"></a> 抽象工厂模式（Abstract Factory）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>抽象工厂模式</strong>提供一个创建一系列相关或相互依赖对象的接口，无需指定具体类。</p>
<p><strong>解决的问题</strong>：当需要一组 “配套” 的产品（如 UI 主题：按钮 + 文本框 + 边框 为一族），且可能有多套实现（主题 A、主题 B）时，保证同一族产品一起创建、不混用。</p>
<p><strong>与工厂方法的区别</strong>：工厂方法针对 “一个产品”；抽象工厂针对 “一族产品”，一个工厂接口可创建多个产品类型。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>AbstractFactory</strong>：抽象工厂，声明创建一族产品的多个方法（如 createButton、createTextBox）。</li>
<li><strong>ConcreteFactory</strong>：具体工厂，实现一族产品的创建（如 WinFactory 创建 WinButton + WinTextBox）。</li>
<li><strong>AbstractProduct A/B</strong>：各产品类型的抽象。</li>
<li><strong>ConcreteProduct</strong>：各产品类型的具体实现。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Button</span> { <span class="keyword">void</span> <span class="title function_">render</span><span class="params">()</span>; }</span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">TextBox</span> { <span class="keyword">void</span> <span class="title function_">input</span><span class="params">()</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">WinButton</span> <span class="keyword">implements</span> <span class="title class_">Button</span> { <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">render</span><span class="params">()</span> {} }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">WinTextBox</span> <span class="keyword">implements</span> <span class="title class_">TextBox</span> { <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">input</span><span class="params">()</span> {} }</span><br><span class="line"></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">GUIFactory</span> {</span><br><span class="line">    Button <span class="title function_">createButton</span><span class="params">()</span>;</span><br><span class="line">    TextBox <span class="title function_">createTextBox</span><span class="params">()</span>;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">WinFactory</span> <span class="keyword">implements</span> <span class="title class_">GUIFactory</span> {</span><br><span class="line">    <span class="keyword">public</span> Button <span class="title function_">createButton</span><span class="params">()</span> { <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">WinButton</span>(); }</span><br><span class="line">    <span class="keyword">public</span> TextBox <span class="title function_">createTextBox</span><span class="params">()</span> { <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">WinTextBox</span>(); }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>BeanFactory 作为 “工厂的工厂”</strong>：可生产多种类型的 Bean（Service、Repository、DataSource 等），不同配置 / Profile 对应不同 “族” 的实现（如 dev 用 H2、prod 用 MySQL），相当于多套具体工厂。</li>
<li><strong>LocalContainerEntityManagerFactoryBean / 数据源与事务管理器</strong>：JPA、数据源、事务管理器往往 “一族” 一起配置，由不同配置类或 Profile 提供不同 “族” 的实现。</li>
<li><strong>多数据源、多消息中间件</strong>：同一应用内多套 “连接工厂 + 相关 Bean”，每套可视为一个抽象工厂实现。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>多套 “产品族” 可替换</strong>：如多租户下每租户一套数据源 + 缓存 + 配置，或多环境（dev/test/prod）各一套基础设施。</li>
<li><strong>UI 主题、跨平台 UI</strong>：同一套控件接口，多套外观实现（Windows/Mac/Linux 风格）。</li>
<li><strong>兼容多版本外部系统</strong>：如对接支付 / 消息的 v1、v2 两套 API，每套是一族（Client + Parser + Mapper），通过抽象工厂切换。</li>
<li><strong>平台相关实现</strong>：如不同操作系统、不同运行时提供不同底层实现，对外统一接口，内部按平台选工厂。</li>
</ul>
<p><strong>不适用</strong>：产品族概念不强、只有单一产品类型或产品之间无 “配套” 关系时，用工厂方法或简单工厂即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>抽象工厂通过 “一族产品由一个工厂创建” 保证产品配套、便于整体切换；Spring 中 <strong>BeanFactory 体系</strong>与<strong>多数据源 / 多环境配置</strong>体现了这一思想。业务上适用于 “多套可替换产品族、主题/平台/多租户” 等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>创建型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>抽象工厂</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（四）建造者模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-04-builder/</url>
    <content><![CDATA[<h1 id="建造者模式builder"><a class="markdownIt-Anchor" href="#建造者模式builder"></a> 建造者模式（Builder）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>建造者模式</strong>将复杂对象的构建与表示分离，使同样的构建过程可以创建不同的表示。</p>
<p><strong>解决的问题</strong>：对象有很多可选参数或构造步骤复杂，若用多参构造或大量 setter，可读性差且易出错；建造者通过链式调用、分步设置，使构建过程清晰并可校验。</p>
<p><strong>定义</strong>：将一个复杂对象的构建与它的表示分离，使得同样的构建过程可以创建不同的表示。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Product</strong>：被构建的复杂对象。</li>
<li><strong>Builder</strong>：抽象建造者，定义构建步骤的接口。</li>
<li><strong>ConcreteBuilder</strong>：具体建造者，实现各步骤并持有 Product。</li>
<li><strong>Director</strong>（可选）：指挥者，按固定顺序调用 Builder 的步骤；有时由调用方直接使用 Builder，无 Director。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Resource</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String url;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">int</span> timeout;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Map&lt;String, String&gt; headers;</span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">Resource</span><span class="params">(Builder b)</span> {</span><br><span class="line">        <span class="built_in">this</span>.url = b.url;</span><br><span class="line">        <span class="built_in">this</span>.timeout = b.timeout;</span><br><span class="line">        <span class="built_in">this</span>.headers = b.headers;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Builder</span> {</span><br><span class="line">        <span class="keyword">private</span> String url;</span><br><span class="line">        <span class="keyword">private</span> <span class="type">int</span> <span class="variable">timeout</span> <span class="operator">=</span> <span class="number">5000</span>;</span><br><span class="line">        <span class="keyword">private</span> Map&lt;String, String&gt; headers = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">        <span class="keyword">public</span> Builder <span class="title function_">url</span><span class="params">(String url)</span> { <span class="built_in">this</span>.url = url; <span class="keyword">return</span> <span class="built_in">this</span>; }</span><br><span class="line">        <span class="keyword">public</span> Builder <span class="title function_">timeout</span><span class="params">(<span class="type">int</span> t)</span> { <span class="built_in">this</span>.timeout = t; <span class="keyword">return</span> <span class="built_in">this</span>; }</span><br><span class="line">        <span class="keyword">public</span> Builder <span class="title function_">header</span><span class="params">(String k, String v)</span> { <span class="built_in">this</span>.headers.put(k, v); <span class="keyword">return</span> <span class="built_in">this</span>; }</span><br><span class="line">        <span class="keyword">public</span> Resource <span class="title function_">build</span><span class="params">()</span> {</span><br><span class="line">            <span class="keyword">if</span> (url == <span class="literal">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(<span class="string">"url required"</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Resource</span>(<span class="built_in">this</span>);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"><span class="comment">// 使用: new Resource.Builder().url("http://a.com").timeout(3000).build();</span></span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>XML / 配置的解析与构建</strong>：如解析 XML 生成 BeanDefinition、构建 Environment 等，内部常按步骤组装复杂对象，具有建造者思想。</li>
<li><strong>RestTemplate.Builder、WebClient.Builder</strong>：Spring Web 提供的 Builder API，链式设置 URL、头、超时等再 <code>build()</code> 出客户端实例。</li>
<li>**Spring Boot 的 <em>Properties 与 <em>AutoConfiguration</em></em>：部分配置类通过 Builder 或类似 “分步设置 + build” 的方式组装（如某些 Security、Actuator 配置）。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>参数多且多可选</strong>：如创建请求/连接/查询条件，参数超过 4～5 个且多数有默认值时，Builder 比多参构造和满屏 setter 更清晰。</li>
<li><strong>构建过程需校验</strong>：在 <code>build()</code> 中统一校验必填项、约束，避免生成无效对象。</li>
<li><strong>不可变对象</strong>：配合 final 字段 + 私有构造，只通过 Builder 构造，保证创建后不可被随意修改。</li>
<li><strong>多种 “套餐” 配置</strong>：同一类对象有多种常用组合（如 “高可用套餐”“开发套餐”），可在 Builder 上封装 preset 方法。</li>
</ul>
<p><strong>不适用</strong>：对象很简单、参数很少且必填时，直接构造或工厂方法即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>建造者模式通过 “分步设置 + build 校验” 降低复杂对象构建的出错率并提升可读性；Spring 中在<strong>配置解析、RestTemplate / WebClient.Builder</strong> 等处有体现。业务上适用于<strong>多可选参数、需校验、不可变对象、多种预设配置</strong>的构建场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>创建型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>建造者</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（六）适配器模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-06-adapter/</url>
    <content><![CDATA[<h1 id="适配器模式adapter"><a class="markdownIt-Anchor" href="#适配器模式adapter"></a> 适配器模式（Adapter）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>适配器模式</strong>将一个类的接口转换成客户期望的另一种接口，使原本接口不兼容的类可以一起工作。</p>
<p><strong>解决的问题</strong>：已有类（或第三方库）的接口与当前系统期望的接口不一致，又不能改源码时，通过适配器做 “转换层”。</p>
<p><strong>两种形式</strong>：<strong>类适配器</strong>（继承被适配类并实现目标接口）、<strong>对象适配器</strong>（持有被适配者引用并实现目标接口，更常用）。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Target</strong>：目标接口，客户依赖的接口。</li>
<li><strong>Adaptee</strong>：被适配者，已有但接口不兼容的类。</li>
<li><strong>Adapter</strong>：实现 Target，内部持有或继承 Adaptee，把对 Target 的调用转发并转换为对 Adaptee 的调用。</li>
</ul>
<p><strong>示例（对象适配器）</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Target</span> { <span class="keyword">void</span> <span class="title function_">request</span><span class="params">()</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Adaptee</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">specificRequest</span><span class="params">()</span> { System.out.println(<span class="string">"adapted"</span>); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Adapter</span> <span class="keyword">implements</span> <span class="title class_">Target</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">Adaptee</span> <span class="variable">adaptee</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Adaptee</span>();</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">request</span><span class="params">()</span> { adaptee.specificRequest(); }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>Spring AOP 的 MethodInterceptor 适配</strong>：如 <code>MethodBeforeAdviceAdapter</code> 将 <code>MethodBeforeAdvice</code> 适配成 <code>MethodInterceptor</code>，供统一拦截链使用；<code>AfterReturningAdviceAdapter</code> 等同理。</li>
<li><strong>HandlerAdapter（Spring MVC）</strong>：将多种 Controller 类型（<code>@RequestMapping</code>、Servlet、HttpRequestHandler 等）统一适配成 <code>ModelAndView handle(request, response, handler)</code> 的调用方式，DispatcherServlet 只依赖 HandlerAdapter 接口。</li>
<li><strong>Spring MVC 的 ViewResolver / View</strong>：将逻辑视图名适配为具体 View 实现（JSP、JSON、模板等）。</li>
<li><strong>JPA 的 Repository 实现</strong>：Spring Data 将 “方法名 / 注解” 适配成持久化操作，也是对 “接口” 的适配。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>对接遗留或第三方接口</strong>：老系统或第三方库接口与现有接口不一致，加一层适配器统一成当前系统接口，不改老代码。</li>
<li><strong>统一多种实现</strong>：多种数据源、多种协议（HTTP/gRPC/消息）需要统一成同一抽象（如统一成 “调用接口”），每种实现包一层适配器。</li>
<li><strong>版本兼容</strong>：新旧 API 共存期，用适配器把旧 API 转成新 API 形态，便于逐步迁移。</li>
<li><strong>与 AOP / 拦截器对接</strong>：不同风格的 “增强”（Advice 类型）需要统一成同一拦截接口时，用适配器转换。</li>
</ul>
<p><strong>不适用</strong>：能直接改接口或统一抽象时，优先重构而非堆适配器。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>适配器模式通过 “实现目标接口 + 转发到被适配者” 解决接口不兼容；Spring 中 <strong>HandlerAdapter</strong>、<strong>Advice 适配成 MethodInterceptor</strong> 是典型应用。业务上适用于<strong>对接遗留 / 第三方、统一多种实现、版本兼容、AOP 链统一</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>结构型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>适配器</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（五）原型模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-05-prototype/</url>
    <content><![CDATA[<h1 id="原型模式prototype"><a class="markdownIt-Anchor" href="#原型模式prototype"></a> 原型模式（Prototype）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>原型模式</strong>用原型实例指定创建对象的种类，并通过复制该原型创建新对象。</p>
<p><strong>解决的问题</strong>：当直接 new 成本高（如构造需大量计算或 I / O），或希望基于已有对象 “复制一份再改” 时，通过克隆避免重复初始化。</p>
<p><strong>定义</strong>：用原型实例指定创建对象的种类，并且通过拷贝这些原型创建新的对象。</p>
<hr>
<h2 id="二-结构与实现"><a class="markdownIt-Anchor" href="#二-结构与实现"></a> 二、结构与实现</h2>
<ul>
<li><strong>Prototype</strong>：声明克隆方法的接口（Java 中常用 <code>Cloneable</code> + <code>clone()</code>）。</li>
<li><strong>ConcretePrototype</strong>：实现克隆逻辑；浅拷贝直接复制字段，深拷贝需递归复制引用类型。</li>
</ul>
<p><strong>示例（浅拷贝）</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Document</span> <span class="keyword">implements</span> <span class="title class_">Cloneable</span> {</span><br><span class="line">    <span class="keyword">private</span> String title;</span><br><span class="line">    <span class="keyword">private</span> List&lt;String&gt; paragraphs;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Document <span class="title function_">clone</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">try</span> {</span><br><span class="line">            <span class="keyword">return</span> (Document) <span class="built_in">super</span>.clone(); <span class="comment">// 浅拷贝，paragraphs 仍共享引用</span></span><br><span class="line">        } <span class="keyword">catch</span> (CloneNotSupportedException e) {</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>深拷贝可重写 <code>clone()</code> 中对 <code>paragraphs</code> 等集合做 new 并复制元素，或使用序列化 / 拷贝构造器等方式。</p>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>Bean 的 scope=“prototype”</strong>：每次 <code>getBean()</code> 或注入时，容器都会<strong>新建一个实例</strong>，相当于 “根据 Bean 定义这一‘原型’复制出新对象”；与单例的 “只一份” 相对。</li>
<li><strong>ObjectFactory / Provider 注入</strong>：获取 prototype Bean 时，通过 <code>ObjectFactory&lt;T&gt;</code> 或 <code>Provider&lt;T&gt;</code> 延迟获取，每次 <code>getObject()</code> 得到新实例，避免单例 Bean 持有 prototype 导致只拿到同一实例的问题。</li>
<li><strong>Bean 的创建过程</strong>：容器内部根据 BeanDefinition 创建实例，可视为 “按定义原型创建”，但一般不直接实现 <code>Cloneable</code>，而是反射 / 构造器创建。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>创建成本高、状态可复制</strong>：如大对象初始化慢，或从模板复制后只改少量字段（如合同、工单从模板生成）。</li>
<li><strong>需要隔离实例状态</strong>：如每次请求 / 会话需要独立对象，避免线程或请求间共享导致并发问题，Spring 中对应 <strong>prototype 作用域</strong>。</li>
<li><strong>撤销 / 重做、快照</strong>：保存当前状态的一份拷贝，后续可恢复或对比（需深拷贝以保证一致性）。</li>
<li><strong>缓存与模板</strong>：从缓存中的 “模板对象” 克隆出一份再填充数据，避免污染模板。</li>
</ul>
<p><strong>不适用</strong>：对象无状态或创建成本很低时，用 new 或工厂即可；深拷贝成本高或引用关系复杂时需权衡。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>原型模式通过 “克隆已有对象” 创建新对象，避免重复昂贵初始化；Spring 中 <strong>prototype 作用域</strong> 与 <strong>ObjectFactory/Provider</strong> 体现了 “按需创建新实例” 的思想。业务上适用于<strong>高创建成本、需实例隔离、模板复制、快照 / 撤销</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>创建型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>原型</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（七）桥接模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-07-bridge/</url>
    <content><![CDATA[<h1 id="桥接模式bridge"><a class="markdownIt-Anchor" href="#桥接模式bridge"></a> 桥接模式（Bridge）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>桥接模式</strong>将抽象部分与实现部分分离，使它们可以独立变化；通过组合而非继承建立抽象与实现的连接。</p>
<p><strong>解决的问题</strong>：若用继承扩展 “抽象 × 实现” 的多种组合，类会爆炸；桥接把 “抽象” 和 “实现” 拆成两个维度，用组合组合在一起，各自独立扩展。</p>
<p><strong>定义</strong>：将抽象与实现解耦，使两者可以独立变化。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Abstraction</strong>：抽象部分，持有 Implementor 的引用，业务方法中委托给 implementor。</li>
<li><strong>RefinedAbstraction</strong>：对 Abstraction 的扩展。</li>
<li><strong>Implementor</strong>：实现部分接口。</li>
<li><strong>ConcreteImplementor</strong>：具体实现。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Implementor</span> { <span class="keyword">void</span> <span class="title function_">operationImpl</span><span class="params">()</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ImplA</span> <span class="keyword">implements</span> <span class="title class_">Implementor</span> { <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">operationImpl</span><span class="params">()</span> {} }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ImplB</span> <span class="keyword">implements</span> <span class="title class_">Implementor</span> { <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">operationImpl</span><span class="params">()</span> {} }</span><br><span class="line"></span><br><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Abstraction</span> {</span><br><span class="line">    <span class="keyword">protected</span> Implementor impl;</span><br><span class="line">    Abstraction(Implementor impl) { <span class="built_in">this</span>.impl = impl; }</span><br><span class="line">    <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title function_">operation</span><span class="params">()</span>;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">RefinedAbstraction</span> <span class="keyword">extends</span> <span class="title class_">Abstraction</span> {</span><br><span class="line">    RefinedAbstraction(Implementor impl) { <span class="built_in">super</span>(impl); }</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">operation</span><span class="params">()</span> { impl.operationImpl(); }</span><br><span class="line">}</span><br><span class="line"><span class="comment">// 使用: new RefinedAbstraction(new ImplA()).operation();</span></span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>JDBC 的 Driver 与 Connection</strong>：<code>Driver</code> 是 “实现”，<code>Connection</code> 是 “抽象”；应用代码依赖 <code>Connection</code> 接口，不同 Driver 提供不同实现，二者通过 DriverManager / DataSource 桥接，而不是为每种数据库写一个 Connection 子类。</li>
<li><strong>数据源与事务管理器</strong>：抽象是 “事务界定（声明式事务）”，实现是 “具体数据源 + 事务资源管理”；Spring 通过 PlatformTransactionManager 与不同 DataSource 组合，可视为桥接思想。</li>
<li><strong>消息抽象与具体 MQ</strong>：JmsTemplate / KafkaTemplate 等是抽象，背后不同 MQ 客户端是实现，可独立替换。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>多维度变化</strong>：如 “消息类型 × 传输方式”“报表格式 × 数据源”，用继承会导致组合爆炸，用桥接拆成 “抽象 + 实现” 两维分别扩展。</li>
<li><strong>运行时切换实现</strong>：如同一套业务逻辑（抽象）在不同环境用不同数据源或不同协议（实现），通过注入不同 Implementor 完成。</li>
<li><strong>跨平台 / 多驱动</strong>：同一套业务 API，底层是不同驱动或平台实现（如日志 API 对多种日志库、连接 API 对多种数据库）。</li>
<li><strong>避免多层继承</strong>：当 “抽象子类 × 实现子类” 的乘积很大时，用组合桥接替代多重继承。</li>
</ul>
<p><strong>不适用</strong>：抽象或实现只有一种或很少变化时，不需要拆成两个维度。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>桥接模式通过 “抽象持有一个实现接口” 把抽象与实现解耦，各自独立扩展；Spring 中 <strong>JDBC Driver/Connection</strong>、<strong>事务管理器与数据源</strong> 体现了这一思想。业务上适用于<strong>多维度变化、运行时切换实现、多驱动 / 多平台</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>结构型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>桥接</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（二）工厂方法模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-02-factory-method/</url>
    <content><![CDATA[<h1 id="工厂方法模式factory-method"><a class="markdownIt-Anchor" href="#工厂方法模式factory-method"></a> 工厂方法模式（Factory Method）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>工厂方法模式</strong>定义用于创建对象的接口，由子类决定实例化哪一个类，将对象的创建延迟到子类。</p>
<p><strong>解决的问题</strong>：避免在业务代码中直接 <code>new</code> 具体类，降低耦合，便于扩展新的产品类型。</p>
<p><strong>定义</strong>：定义一个创建对象的接口，让子类决定实例化哪个类；工厂方法使一个类的实例化延迟到其子类。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Product</strong>：抽象产品（接口或抽象类）。</li>
<li><strong>ConcreteProduct</strong>：具体产品。</li>
<li><strong>Creator</strong>：抽象工厂，声明工厂方法（返回 Product）。</li>
<li><strong>ConcreteCreator</strong>：具体工厂，实现工厂方法，返回 ConcreteProduct。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Product</span> { <span class="keyword">void</span> <span class="title function_">use</span><span class="params">()</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ProductA</span> <span class="keyword">implements</span> <span class="title class_">Product</span> { <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">use</span><span class="params">()</span> { System.out.println(<span class="string">"A"</span>); } }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ProductB</span> <span class="keyword">implements</span> <span class="title class_">Product</span> { <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">use</span><span class="params">()</span> { System.out.println(<span class="string">"B"</span>); } }</span><br><span class="line"></span><br><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Creator</span> {</span><br><span class="line">    <span class="keyword">abstract</span> Product <span class="title function_">createProduct</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">doSomething</span><span class="params">()</span> {</span><br><span class="line">        <span class="type">Product</span> <span class="variable">p</span> <span class="operator">=</span> createProduct();</span><br><span class="line">        p.use();</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">CreatorA</span> <span class="keyword">extends</span> <span class="title class_">Creator</span> {</span><br><span class="line">    Product <span class="title function_">createProduct</span><span class="params">()</span> { <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ProductA</span>(); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">CreatorB</span> <span class="keyword">extends</span> <span class="title class_">Creator</span> {</span><br><span class="line">    Product <span class="title function_">createProduct</span><span class="params">()</span> { <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ProductB</span>(); }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>BeanFactory / ApplicationContext</strong>：本质是 “创建 Bean 的工厂”，<code>getBean()</code> 即工厂方法；具体创建哪种 Bean、如何构造由配置与子类 / 实现决定。</li>
<li><strong>FactoryBean</strong>：实现 <code>FactoryBean&lt;T&gt;</code> 的 Bean，容器通过 <code>getObject()</code> 获取实际要暴露的对象，适合 “创建过程复杂、非简单 new” 的对象（如 AOP 代理、动态数据源等）。</li>
<li><strong>@Bean 方法</strong>：在配置类中声明 <code>@Bean</code>，相当于 “工厂方法”，由 Spring 调用得到实例并纳入容器。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>同一抽象多种实现</strong>：如多种支付方式、多种存储引擎，新增实现时只需新增工厂 / 产品类，不改调用方。</li>
<li><strong>创建逻辑复杂</strong>：对象需要多步初始化、依赖外部配置或策略时，集中到工厂方法中。</li>
<li><strong>希望与具体类解耦</strong>：调用方只依赖抽象产品与工厂接口，便于测试与替换实现。</li>
<li><strong>与配置 / 策略结合</strong>：根据配置或运行时条件选择不同产品实现（Spring 中常通过不同 Bean 定义或 Profile 实现）。</li>
</ul>
<p><strong>不适用</strong>：产品类型极少且几乎不变、创建逻辑极简单（直接 new 即可）的场景。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>工厂方法通过 “抽象工厂 + 工厂方法” 把对象创建延迟到子类，实现开闭原则；Spring 中 <strong>BeanFactory / ApplicationContext</strong> 与 <strong>FactoryBean</strong>、<strong>@Bean</strong> 都是工厂方法思想的体现。业务上适用于 “多种实现选一种、创建逻辑集中、与具体类解耦” 的场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>创建型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>工厂方法</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（八）组合模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-08-composite/</url>
    <content><![CDATA[<h1 id="组合模式composite"><a class="markdownIt-Anchor" href="#组合模式composite"></a> 组合模式（Composite）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>组合模式</strong>将对象组合成树形结构以表示 “部分 - 整体” 的层次结构，使客户对单个对象和组合对象的使用具有一致性。</p>
<p><strong>解决的问题</strong>：树形结构中的节点有的是 “叶子” 有的是 “容器”，希望客户代码不用区分，统一用同一接口（如 <code>operation()</code>）处理。</p>
<p><strong>定义</strong>：将对象组合成树形结构表示 “部分 - 整体” 层次，使得客户对单个对象和组合对象的使用具有一致性。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Component</strong>：抽象组件，定义叶子与组合的公共接口（如 <code>operation()</code>），可选地定义管理子节点的接口（add/remove/getChild）。</li>
<li><strong>Leaf</strong>：叶子节点，无子节点。</li>
<li><strong>Composite</strong>：组合节点，持有子 Component 集合，实现 <code>operation()</code> 时通常遍历子节点并调用其 <code>operation()</code>。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Component</span> {</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">operation</span><span class="params">()</span>;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Leaf</span> <span class="keyword">implements</span> <span class="title class_">Component</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">operation</span><span class="params">()</span> { System.out.println(<span class="string">"Leaf"</span>); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Composite</span> <span class="keyword">implements</span> <span class="title class_">Component</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> List&lt;Component&gt; children = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(Component c)</span> { children.add(c); }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">operation</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">for</span> (Component c : children) c.operation();</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>CompositeCacheManager</strong>：将多个 <code>CacheManager</code> 组合成一个，按名称路由到不同 CacheManager，对外表现为一个 CacheManager，是典型的 “组合对象”。</li>
<li><strong>Bean 定义中的嵌套</strong>：如父子 Bean 定义、嵌套的 <code>&lt;list&gt;</code>/<code>&lt;map&gt;</code> 配置，结构上是树形，解析时用组合思想处理。</li>
<li><strong>MyBatis 动态 SQL</strong>：<code>&lt;if&gt;/&lt;choose&gt;/&lt;foreach&gt;</code> 等组合成一棵 SQL 节点树，统一通过 <code>apply()</code> 或类似方法求值，可视为组合模式。</li>
<li><strong>组织架构、菜单树</strong>：若在业务中建模 “部门 - 子部门”“菜单 - 子菜单”，用组合模式可统一对单节点和子树的操作。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>树形结构且需统一操作</strong>：如菜单、权限树、组织架构、目录 - 文件，对 “节点” 和 “子树” 执行同一类操作（渲染、权限检查、汇总）。</li>
<li><strong>多级配置 / 规则</strong>：如配置有 “全局 → 应用 → 模块” 的覆盖关系，用组合表示层级，统一按层级解析。</li>
<li><strong>表达式或 AST</strong>：如简单表达式解析成树，叶子是常量 / 变量，组合节点是运算符，统一 <code>evaluate()</code>。</li>
<li><strong>多 CacheManager / 多数据源的路由</strong>：对外一个入口，内部按 key 或规则分发到多个子组件，对外表现为一个整体。</li>
</ul>
<p><strong>不适用</strong>：结构不是树形、或叶子与容器行为差异很大无需统一接口时。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>组合模式通过 “叶子与组合实现同一接口 + 组合内部委托子节点” 统一处理树形结构；Spring 中 <strong>CompositeCacheManager</strong>、<strong>Bean 定义嵌套</strong>、<strong>动态 SQL 节点树</strong> 有体现。业务上适用于<strong>菜单/权限/组织树、多级配置、表达式/AST、多子组件路由</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>结构型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>组合</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（九）装饰器模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-09-decorator/</url>
    <content><![CDATA[<h1 id="装饰器模式decorator"><a class="markdownIt-Anchor" href="#装饰器模式decorator"></a> 装饰器模式（Decorator）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>装饰器模式</strong>动态地给对象增加职责，比继承更灵活；装饰器与被装饰对象实现同一接口，内部持有被装饰对象，在调用前后增加行为。</p>
<p><strong>解决的问题</strong>：需要在不修改原类的前提下，为对象增加额外行为（如日志、缓存、加密）；若用继承，每种组合一个子类会爆炸，装饰器可任意叠放。</p>
<p><strong>与代理的区别</strong>：装饰器强调 “增强、包装”，代理常强调 “控制访问、延迟”；实现上类似，都是 “实现同一接口 + 持有目标 + 前后增强”。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Component</strong>：抽象组件，被装饰对象与装饰器的共同接口。</li>
<li><strong>ConcreteComponent</strong>：具体被装饰对象。</li>
<li><strong>Decorator</strong>：装饰器，实现 Component 并持有 Component，通常将请求转发给被装饰对象并在前后加逻辑。</li>
<li><strong>ConcreteDecorator</strong>：具体装饰器，增加具体职责。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Component</span> { String <span class="title function_">operation</span><span class="params">()</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ConcreteComponent</span> <span class="keyword">implements</span> <span class="title class_">Component</span> {</span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">operation</span><span class="params">()</span> { <span class="keyword">return</span> <span class="string">"data"</span>; }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Decorator</span> <span class="keyword">implements</span> <span class="title class_">Component</span> {</span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">final</span> Component target;</span><br><span class="line">    Decorator(Component target) { <span class="built_in">this</span>.target = target; }</span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">operation</span><span class="params">()</span> { <span class="keyword">return</span> target.operation(); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">LogDecorator</span> <span class="keyword">extends</span> <span class="title class_">Decorator</span> {</span><br><span class="line">    LogDecorator(Component target) { <span class="built_in">super</span>(target); }</span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">operation</span><span class="params">()</span> {</span><br><span class="line">        System.out.println(<span class="string">"before"</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">r</span> <span class="operator">=</span> target.operation();</span><br><span class="line">        System.out.println(<span class="string">"after"</span>);</span><br><span class="line">        <span class="keyword">return</span> r;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>TransactionAwareCacheDecorator</strong>：对 <code>Cache</code> 做装饰，在事务提交 / 回滚时再写缓存或清理，使缓存与事务语义一致。</li>
<li><strong>BeanWrapper 与 属性编辑器</strong>：对 Bean 的包装与编辑可视为对 “对象行为” 的装饰。</li>
<li><strong>InputStream / OutputStream 装饰</strong>：Java I / O 中 <code>BufferedInputStream</code> 装饰 <code>InputStream</code> 等；Spring 的 <code>Resource</code> 包装（如 <code>EncodedResource</code>）也是装饰思想。</li>
<li><strong>AOP 切面</strong>：从效果上，对 Bean 方法加事务、日志、权限，相当于对 “方法调用” 做多层装饰，Spring 用代理实现而非手写装饰类。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>动态叠加能力</strong>：如对 “读数据” 叠加缓存、日志、限流、脱敏，可任意组合且不修改原类。</li>
<li><strong>与事务 / 生命周期挂钩</strong>：如 “缓存在事务提交后写入”，用装饰器包装原有 Cache 实现。</li>
<li><strong>兼容老接口</strong>：老接口返回的数据需要在新逻辑里做一层包装（如加版本、加元数据），用装饰器包装返回值。</li>
<li><strong>流式 / 管道处理</strong>：数据流经多道处理（压缩、加密、校验），每道用装饰器包装上一级流。</li>
</ul>
<p><strong>不适用</strong>：行为固定、不需要多种组合增强时，直接继承或一个工具类即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>装饰器模式通过 “实现同一接口 + 持有被装饰对象 + 前后增强” 动态增加职责；Spring 中 <strong>TransactionAwareCacheDecorator</strong>、<strong>Resource 包装</strong>、<strong>AOP 增强</strong> 体现了装饰思想。业务上适用于<strong>动态叠加能力、与事务 / 生命周期挂钩、兼容老接口、流式处理</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>结构型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>装饰器</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十）外观模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-10-facade/</url>
    <content><![CDATA[<h1 id="外观模式facade"><a class="markdownIt-Anchor" href="#外观模式facade"></a> 外观模式（Facade）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>外观模式</strong>为子系统中的一组接口提供一个统一的高层接口，使子系统更易使用。</p>
<p><strong>解决的问题</strong>：子系统内类多、接口复杂，客户若直接依赖多个类则耦合高、调用繁琐；外观提供一个 “门面”，把常用流程封装成简单接口。</p>
<p><strong>定义</strong>：为子系统中的一组接口提供一个一致的界面，Facade 定义了一个高层接口，使子系统更易使用。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Facade</strong>：外观类，持有子系统若干模块的引用，提供少量高层方法，内部编排对子系统的调用。</li>
<li><strong>Subsystem classes</strong>：子系统中的多个类，各自负责一部分功能；客户可通过 Facade 间接使用，也可直接使用（若需要细粒度控制）。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">ModuleA</span> { <span class="keyword">void</span> <span class="title function_">doA</span><span class="params">()</span> {} }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ModuleB</span> { <span class="keyword">void</span> <span class="title function_">doB</span><span class="params">()</span> {} }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ModuleC</span> { <span class="keyword">void</span> <span class="title function_">doC</span><span class="params">()</span> {} }</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Facade</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">ModuleA</span> <span class="variable">a</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ModuleA</span>();</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">ModuleB</span> <span class="variable">b</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ModuleB</span>();</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">ModuleC</span> <span class="variable">c</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ModuleC</span>();</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">doOneThing</span><span class="params">()</span> {</span><br><span class="line">        a.doA();</span><br><span class="line">        b.doB();</span><br><span class="line">        c.doC();</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>SpringMVC 的 DispatcherServlet</strong>：对请求处理流程的统一入口，内部编排 HandlerMapping、HandlerAdapter、ViewResolver 等，对 “一次 HTTP 请求” 提供 “查 Handler → 适配执行 → 解析视图” 的简化视图，是典型外观。</li>
<li><strong>JdbcTemplate</strong>：对 JDBC 的 Connection/Statement/ResultSet 的获取、异常转换、资源关闭做封装，对外提供 <code>query/update</code> 等简单 API，降低直接使用 JDBC 的复杂度。</li>
<li><strong>Spring Boot 的自动配置与 Starter</strong>：如 <code>spring-boot-starter-web</code> 对外暴露 “开箱即用” 的 Web 能力，背后整合 Tomcat、MVC、Jackson 等，可视为 “模块组” 的外观。</li>
<li><strong>Tomcat 的 RequestFacade / ResponseFacade</strong>：对 ServletRequest / ServletResponse 的封装，隐藏内部实现细节，避免用户拿到内部对象后误操作。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>简化子系统使用</strong>：如 “下单” 涉及库存、订单、支付、消息等多个服务，对外提供 <code>OrderFacade.placeOrder()</code>，内部编排调用，减少调用方对多模块的依赖。</li>
<li><strong>统一入口与流程</strong>：如 “报表生成” 需查库、计算、导出，用 ReportFacade 封装步骤，调用方只调一个方法。</li>
<li><strong>降低升级影响</strong>：子系统内部重构时，只要外观接口不变，调用方无需改动。</li>
<li><strong>为第三方 / 遗留系统提供简版 API</strong>：把复杂、难用的 API 包一层，只暴露业务需要的少量方法。</li>
</ul>
<p><strong>不适用</strong>：调用方需要细粒度控制子系统每一步、或子系统本身很简单时，不必强行加外观。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>外观模式通过 “统一高层接口 + 内部编排子系统” 降低使用复杂度；Spring 中 <strong>DispatcherServlet</strong>、<strong>JdbcTemplate</strong>、<strong>Boot Starter</strong>、<strong>RequestFacade/ResponseFacade</strong> 都是外观思想的体现。业务上适用于<strong>简化多模块调用、统一流程入口、隔离变化、包装复杂 API</strong> 等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>结构型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>外观</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十一）享元模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-11-flyweight/</url>
    <content><![CDATA[<h1 id="享元模式flyweight"><a class="markdownIt-Anchor" href="#享元模式flyweight"></a> 享元模式（Flyweight）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>享元模式</strong>运用共享技术有效支持大量细粒度对象的复用，减少内存与对象数量。</p>
<p><strong>解决的问题</strong>：存在大量相似对象（如相同字符、相同图标、相同配置），若每个都单独实例会占用大量内存；把 “可共享的内在状态” 与 “不可共享的外在状态” 分离，共享内在状态，外在状态由调用方传入。</p>
<p><strong>定义</strong>：运用共享技术有效地支持大量细粒度的对象。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Flyweight</strong>：享元接口，定义对外方法，方法参数中常包含 “外在状态”。</li>
<li><strong>ConcreteFlyweight</strong>：具体享元，存储内在状态（可共享），方法中结合传入的外在状态完成行为。</li>
<li><strong>FlyweightFactory</strong>：享元工厂，维护享元池，根据 key 返回共享实例；无则创建并缓存。</li>
<li><strong>Client</strong>：使用享元时传入外在状态。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Flyweight</span> { <span class="keyword">void</span> <span class="title function_">operation</span><span class="params">(String extrinsicState)</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ConcreteFlyweight</span> <span class="keyword">implements</span> <span class="title class_">Flyweight</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String intrinsicState;</span><br><span class="line">    ConcreteFlyweight(String intrinsicState) { <span class="built_in">this</span>.intrinsicState = intrinsicState; }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">operation</span><span class="params">(String extrinsicState)</span> {</span><br><span class="line">        System.out.println(intrinsicState + <span class="string">", "</span> + extrinsicState);</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">FlyweightFactory</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Map&lt;String, Flyweight&gt; pool = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">    Flyweight <span class="title function_">get</span><span class="params">(String key)</span> {</span><br><span class="line">        <span class="keyword">return</span> pool.computeIfAbsent(key, ConcreteFlyweight::<span class="keyword">new</span>);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>String 常量池、Integer 缓存</strong>：JVM / JDK 层面的 “享元”，相同内容的 String、小范围 Integer 共享实例；Spring 未单独实现，但业务中大量用 String 作为 key、缓存 key 时天然受益。</li>
<li><strong>Bean 单例</strong>：单例 Bean 在容器内共享，可视为 “按 Bean 定义共享的实例”，与享元 “按 key 共享” 思想一致。</li>
<li><strong>元数据、配置的缓存</strong>：如反射得到的 Method、Annotation 等元数据，Spring 会缓存复用，避免重复解析；可视为 “按类 / 方法等 key 的享元”。</li>
<li><strong>线程池、连接池</strong>：池中的线程、连接是 “可复用对象”，与享元 “复用对象” 的目标一致，只是通常不叫 Flyweight 名字。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>大量相似对象且可区分内在 / 外在</strong>：如棋牌游戏中 “牌面” 可共享（内在），每张牌的 “位置、归属” 由外部传入（外在）；文档编辑器中 “字符样式” 共享，“位置” 外在。</li>
<li><strong>缓存、池化</strong>：按 key 缓存对象（如按配置 key 缓存解析结果），无则创建并放入池，有则直接返回，本质是享元工厂。</li>
<li><strong>减少重复解析 / 编译</strong>：如模板引擎中相同模板只编译一次；规则引擎中相同规则只编译一次，运行时只传不同参数。</li>
<li><strong>枚举、常量类</strong>：有限个实例对应有限个 “内在状态”，全应用共享，是享元的简化形式。</li>
</ul>
<p><strong>不适用</strong>：对象之间几乎没有可共享状态、或对象数量很少时，享元收益不大。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>享元模式通过 “内在状态共享 + 外在状态参数化” 减少对象数量与内存；Spring 中 <strong>单例 Bean</strong>、<strong>元数据缓存</strong>、<strong>池化</strong> 体现了共享复用思想。业务上适用于<strong>大量相似对象、缓存/池化、减少重复解析/编译、枚举/常量</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>结构型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>享元</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十三）责任链模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-13-chain-of-responsibility/</url>
    <content><![CDATA[<h1 id="责任链模式chain-of-responsibility"><a class="markdownIt-Anchor" href="#责任链模式chain-of-responsibility"></a> 责任链模式（Chain of Responsibility）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>责任链模式</strong>使多个对象都有机会处理请求，将这些对象连成一条链，并沿链传递请求，直到有对象处理为止。</p>
<p><strong>解决的问题</strong>：请求的发送者与多个处理者解耦，处理者可动态组合、顺序可调，且可随时增删节点。</p>
<p><strong>定义</strong>：将请求的发送者和接收者解耦，使多个对象都有机会处理该请求；将这些对象连成链，并沿链传递请求。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Handler</strong>：抽象处理者，定义处理接口并持有后继者（next），在 handle 中可选地调用 next.handle()。</li>
<li><strong>ConcreteHandler</strong>：具体处理者，能处理则处理并返回，否则交给后继。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Handler</span> {</span><br><span class="line">    <span class="keyword">protected</span> Handler next;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setNext</span><span class="params">(Handler next)</span> { <span class="built_in">this</span>.next = next; }</span><br><span class="line">    <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title function_">handle</span><span class="params">(Request req)</span>;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">HandlerA</span> <span class="keyword">extends</span> <span class="title class_">Handler</span> {</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">handle</span><span class="params">(Request req)</span> {</span><br><span class="line">        <span class="keyword">if</span> (canHandle(req)) { <span class="comment">/* 处理 */</span> <span class="keyword">return</span>; }</span><br><span class="line">        <span class="keyword">if</span> (next != <span class="literal">null</span>) next.handle(req);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">private</span> <span class="type">boolean</span> <span class="title function_">canHandle</span><span class="params">(Request req)</span> { <span class="keyword">return</span> <span class="literal">true</span>; }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>Filter 链（Servlet / Spring Security）</strong>：请求依次经过多个 Filter，每个 Filter 可决定是否放行、是否继续链；Spring Security 的 FilterChainProxy 编排多个 SecurityFilter，是责任链。</li>
<li><strong>HandlerInterceptor</strong>：Spring MVC 中 preHandle/postHandle/afterCompletion 构成 “拦截链”，多个 Interceptor 按顺序执行，任一 preHandle 返回 false 可中断。</li>
<li><strong>AOP 的 Advice 链</strong>：多个 Advisor / Advice 组成拦截链，按顺序执行，可视为责任链。</li>
<li><strong>异常解析链</strong>：HandlerExceptionResolver 有多个实现，按顺序尝试解析异常，直到某个能处理为止。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>多级审批、工作流</strong>：请求按 “一级 → 二级 → 三级” 传递，每级能处理则处理并结束，否则交给下一级。</li>
<li><strong>请求预处理 / 后处理</strong>：如鉴权 → 限流 → 日志 → 业务，每步可中断或放行。</li>
<li><strong>异常 / 错误处理</strong>：多种异常处理器按顺序尝试，直到某个能处理（转换、记录、返回统一格式）。</li>
<li><strong>管道式处理</strong>：数据经多道 “处理器”（校验、转换、丰富、落库），每道可选处理或跳过。</li>
</ul>
<p><strong>不适用</strong>：请求必然只由一个固定对象处理、或处理逻辑无 “链式传递” 需求时。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>责任链模式通过 “处理者持有后继 + 沿链传递请求” 实现发送者与多个处理者解耦；Spring 中 <strong>Filter/FilterChain</strong>、<strong>HandlerInterceptor</strong>、<strong>AOP 链</strong>、<strong>HandlerExceptionResolver</strong> 是典型应用。业务上适用于<strong>多级审批、请求预处理链、异常解析链、管道处理</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>责任链</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十二）代理模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-12-proxy/</url>
    <content><![CDATA[<h1 id="代理模式proxy"><a class="markdownIt-Anchor" href="#代理模式proxy"></a> 代理模式（Proxy）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>代理模式</strong>为对象提供代理，由代理控制对原对象的访问；客户通过代理间接访问目标，代理可在访问前后增加逻辑（权限、日志、事务等）。</p>
<p><strong>解决的问题</strong>：不修改目标类的前提下，增加访问控制、延迟加载、增强逻辑等。</p>
<p><strong>常见实现</strong>：静态代理（手写代理类）、JDK 动态代理（基于接口）、CGLIB 动态代理（基于子类）。Spring AOP 默认 “有接口用 JDK 动态代理，无接口用 CGLIB”。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Subject</strong>：抽象主题，目标与代理的共同接口。</li>
<li><strong>RealSubject</strong>：真实主题，被代理对象。</li>
<li><strong>Proxy</strong>：代理，实现 Subject，持有 RealSubject（或通过工厂 / 延迟创建），在请求前后执行附加逻辑后转发给 RealSubject。</li>
</ul>
<p><strong>示例（静态代理）</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Subject</span> { <span class="keyword">void</span> <span class="title function_">request</span><span class="params">()</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">RealSubject</span> <span class="keyword">implements</span> <span class="title class_">Subject</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">request</span><span class="params">()</span> { System.out.println(<span class="string">"real"</span>); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Proxy</span> <span class="keyword">implements</span> <span class="title class_">Subject</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">RealSubject</span> <span class="variable">target</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RealSubject</span>();</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">request</span><span class="params">()</span> {</span><br><span class="line">        System.out.println(<span class="string">"before"</span>);</span><br><span class="line">        target.request();</span><br><span class="line">        System.out.println(<span class="string">"after"</span>);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>AOP 与 @Transactional</strong>：对 Bean 方法做事务、日志、权限等增强时，Spring 为 Bean 创建代理（JDK 或 CGLIB），调用方拿到的是代理对象，方法调用经代理再落到目标方法，是代理模式的直接应用。</li>
<li><strong>ProxyFactoryBean</strong>：显式配置 AOP 代理时，通过 ProxyFactoryBean 指定目标与切面，由它生成代理 Bean。</li>
<li><strong>远程调用 / RPC</strong>：客户端拿到的 “服务接口” 实现实为代理，代理将调用序列化后发到服务端，结果反序列化返回，对调用方透明。</li>
<li><strong>懒加载 / Lazy</strong>：部分场景下注入的是 “懒代理”，首次使用时才初始化真实对象。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>横切关注点</strong>：事务、日志、权限、限流、异常转换等，用代理统一加在方法调用前后，不改业务类。</li>
<li><strong>远程 / 跨进程调用</strong>：本地接口的 “实现” 是代理，负责序列化、网络调用、反序列化。</li>
<li><strong>访问控制</strong>：只允许通过代理访问敏感资源，代理中做鉴权、审计。</li>
<li><strong>延迟加载</strong>：大对象或昂贵资源，首次使用时通过代理触发加载。</li>
<li><strong>缓存</strong>：代理在调用前查缓存，命中则返回，未命中再调目标并回填缓存。</li>
</ul>
<p><strong>不适用</strong>：无增强需求、无访问控制或远程需求时，直接依赖真实对象即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>代理模式通过 “代理与目标同一接口 + 代理控制访问并增强” 在不改目标的前提下扩展行为；Spring 中 <strong>AOP/@Transactional</strong>、<strong>ProxyFactoryBean</strong>、<strong>远程代理</strong> 是典型应用。业务上适用于<strong>横切关注点、远程调用、访问控制、延迟加载、缓存</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>结构型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>代理</tag>
        <tag>AOP</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十五）解释器模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-15-interpreter/</url>
    <content><![CDATA[<h1 id="解释器模式interpreter"><a class="markdownIt-Anchor" href="#解释器模式interpreter"></a> 解释器模式（Interpreter）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>解释器模式</strong>给定一门语言（或表达式）的表示与解释方式，用解释器解释句子（或表达式）并执行。</p>
<p><strong>解决的问题</strong>：当存在一种 “小语言”（如表达式、简单脚本、规则描述），需要解析并执行时，用解释器把语法拆成若干类，每类负责一种语法单元的解析与求值。</p>
<p><strong>定义</strong>：给定一个语言，定义它的文法的一种表示，并定义一个解释器，用该解释器解释语言中的句子。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>AbstractExpression</strong>：抽象表达式，声明 interpret (Context) 接口。</li>
<li><strong>TerminalExpression</strong>：终结符表达式，对应文法中的终结符（如常量、变量）。</li>
<li><strong>NonterminalExpression</strong>：非终结符表达式，对应文法中的规则，内部包含多个子表达式并组合求值。</li>
<li><strong>Context</strong>：上下文，存放变量、输入等，供解释过程使用。</li>
</ul>
<p><strong>示例</strong>（简单加减法）：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Expr</span> {</span><br><span class="line">    <span class="type">int</span> <span class="title function_">interpret</span><span class="params">(Map&lt;String, Integer&gt; ctx)</span>;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Number</span> <span class="keyword">implements</span> <span class="title class_">Expr</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">int</span> value;</span><br><span class="line">    Number(<span class="type">int</span> value) { <span class="built_in">this</span>.value = value; }</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">interpret</span><span class="params">(Map&lt;String, Integer&gt; ctx)</span> { <span class="keyword">return</span> value; }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Add</span> <span class="keyword">implements</span> <span class="title class_">Expr</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Expr left, right;</span><br><span class="line">    Add(Expr l, Expr r) { left = l; right = r; }</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">interpret</span><span class="params">(Map&lt;String, Integer&gt; ctx)</span> {</span><br><span class="line">        <span class="keyword">return</span> left.interpret(ctx) + right.interpret(ctx);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>SpEL（Spring Expression Language）</strong>：Spring 的表达式语言，用于 @Value、@ConditionalOnExpression、缓存 key、安全表达式等；内部将表达式解析成 AST 并解释执行，是典型的解释器模式。</li>
<li><strong>ExpressionParser 与 Expression</strong>：<code>ExpressionParser.parseExpression(expr)</code> 得到 <code>Expression</code>，再 <code>getValue(context)</code> 求值，对应 “解析 + 解释”。</li>
<li><strong>条件注解中的表达式</strong>：如 <code>@ConditionalOnExpression("...")</code>，字符串被解析并求值，决定是否加载 Bean。</li>
<li><strong>简单规则引擎</strong>：若业务中用 “规则字符串” 描述条件（如 “age &gt; 18 &amp;&amp; city == ‘Beijing’”），可用解释器或 SpEL 解析并求值。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>配置 / 脚本中的表达式</strong>：如配置里写 <code>"${env}-${version}"</code>、简单运算或条件，需要解析并求值。</li>
<li><strong>规则 / 策略的文本描述</strong>：如风控规则 “金额 &gt; 10000 且 次数 &gt; 5”，用小型 DSL 描述并由解释器执行。</li>
<li><strong>查询 / 过滤条件</strong>：用户输入简单条件表达式，解析后生成过滤逻辑或 SQL 条件（需防注入）。</li>
<li><strong>公式计算</strong>：如报表中的单元格公式、定价公式，用表达式描述并由解释器求值。</li>
</ul>
<p><strong>不适用</strong>：语法复杂、性能要求高时，应使用 parser generator 或预编译；简单固定格式用正则或手写解析即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>解释器模式通过 “为语法中的每种符号定义解释类 + 组合求值” 实现小语言的解析与执行；Spring 中 <strong>SpEL</strong>、<strong>ExpressionParser/Expression</strong>、<strong>条件表达式</strong> 是典型应用。业务上适用于<strong>配置表达式、规则描述、简单公式 / 条件</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>解释器</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十四）命令模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-14-command/</url>
    <content><![CDATA[<h1 id="命令模式command"><a class="markdownIt-Anchor" href="#命令模式command"></a> 命令模式（Command）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>命令模式</strong>将请求封装成对象，从而可用不同的请求对客户进行参数化、支持请求排队、记录日志、撤销等。</p>
<p><strong>解决的问题</strong>：把 “操作” 变成对象，便于传递、排队、记录、撤销 / 重做；调用方只依赖 “命令” 接口，不依赖具体执行者。</p>
<p><strong>定义</strong>：将请求封装为对象，从而可以参数化客户、排队、记录请求日志、支持撤销等。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Command</strong>：抽象命令，声明 execute ()（及可选 undo ()）。</li>
<li><strong>ConcreteCommand</strong>：具体命令，持有 Receiver 的引用，execute () 中调用 Receiver 的相应方法。</li>
<li><strong>Receiver</strong>：接收者，真正执行操作的对象。</li>
<li><strong>Invoker</strong>：调用者，持有 Command，在适当时机调用 command.execute()。</li>
<li><strong>Client</strong>：组装 Command 与 Receiver，将 Command 交给 Invoker。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Command</span> { <span class="keyword">void</span> <span class="title function_">execute</span><span class="params">()</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Receiver</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">action</span><span class="params">()</span> { System.out.println(<span class="string">"do"</span>); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ConcreteCommand</span> <span class="keyword">implements</span> <span class="title class_">Command</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">Receiver</span> <span class="variable">receiver</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Receiver</span>();</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">execute</span><span class="params">()</span> { receiver.action(); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Invoker</span> {</span><br><span class="line">    <span class="keyword">private</span> Command command;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setCommand</span><span class="params">(Command c)</span> { <span class="built_in">this</span>.command = c; }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> { command.execute(); }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>Runnable / Callable 作为 “命令”</strong>：线程池提交的 Runnable / Callable 可视为命令对象，Invoker 是线程池，Receiver 是业务逻辑；Spring 的 @Async、TaskExecutor 也基于此类 “可执行命令”。</li>
<li><strong>JdbcTemplate 的 StatementCreator / PreparedStatementCallback</strong>：将 “创建语句 / 执行语句” 封装成对象，由模板在合适时机（获取连接后）执行，具有命令模式思想。</li>
<li><strong>Spring MVC 的 HandlerExecutionChain</strong>：Handler 与 Interceptor 可视为 “命令 + 链”，DispatcherServlet 作为 Invoker 在合适时机执行。</li>
<li><strong>消息驱动</strong>：MQ 消息体可视为 “命令”，消费者根据消息类型执行不同逻辑，支持异步、排队、重试。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>撤销 / 重做</strong>：每次操作封装成命令并入栈，撤销时执行 undo 或从栈中弹出并反向执行。</li>
<li><strong>任务队列、异步执行</strong>：任务封装成命令对象，放入队列，由工作线程依次执行；支持延迟、重试、优先级。</li>
<li><strong>多端触发同一操作</strong>：UI、API、定时任务都可发出同一 “命令对象”，由统一 Invoker 执行，便于扩展与测试。</li>
<li><strong>审计、日志</strong>：命令对象可记录 “谁在何时执行了什么”，便于审计与回放。</li>
<li><strong>宏 / 批处理</strong>：将多个命令组合成复合命令，一次执行一批操作。</li>
</ul>
<p><strong>不适用</strong>：操作极简单、无需排队/撤销/审计时，直接调用即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>命令模式通过 “将请求封装为对象” 支持参数化、排队、撤销、审计；Spring 中 <strong>Runnable / Callable 与线程池</strong>、<strong>JdbcTemplate 回调</strong>、<strong>消息驱动</strong> 体现了命令思想。业务上适用于<strong>撤销重做、任务队列、多端触发、审计、宏 / 批处理</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>命令</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十六）迭代器模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-16-iterator/</url>
    <content><![CDATA[<h1 id="迭代器模式iterator"><a class="markdownIt-Anchor" href="#迭代器模式iterator"></a> 迭代器模式（Iterator）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>迭代器模式</strong>提供一种顺序访问聚合对象元素的方法，且不暴露其内部表示。</p>
<p><strong>解决的问题</strong>：聚合结构（列表、树、图）的遍历方式多样（顺序、逆序、过滤等），若在聚合内部写死会违反单一职责；把遍历逻辑抽到迭代器中，聚合只负责存储，迭代器负责 “如何遍历”。</p>
<p><strong>定义</strong>：提供一种方法顺序访问聚合对象中的元素，而又不暴露其内部表示。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Iterator</strong>：迭代器接口，定义 hasNext ()、next () 等。</li>
<li><strong>ConcreteIterator</strong>：具体迭代器，持有聚合的引用或内部状态，实现遍历逻辑。</li>
<li><strong>Aggregate</strong>：聚合接口，定义 createIterator ()。</li>
<li><strong>ConcreteAggregate</strong>：具体聚合，实现 createIterator () 返回对应迭代器。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Iterator</span>&lt;T&gt; {</span><br><span class="line">    <span class="type">boolean</span> <span class="title function_">hasNext</span><span class="params">()</span>;</span><br><span class="line">    T <span class="title function_">next</span><span class="params">()</span>;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Aggregate</span>&lt;T&gt; {</span><br><span class="line">    Iterator&lt;T&gt; <span class="title function_">iterator</span><span class="params">()</span>;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ListAggregate</span>&lt;T&gt; <span class="keyword">implements</span> <span class="title class_">Aggregate</span>&lt;T&gt; {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> List&lt;T&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    <span class="keyword">public</span> Iterator&lt;T&gt; <span class="title function_">iterator</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ListIterator</span>&lt;&gt;(list);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>Java 的 <code>java.util.Iterator</code> 与 <code>Iterable</code> 已是语言级支持。</p>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>集合的迭代</strong>：Spring 大量使用 <code>Collection</code>、<code>Iterable</code>、<code>Iterator</code> 遍历 Bean 列表、Resource 列表等；与 JDK 一致，不单独实现迭代器接口，但 “通过迭代器访问聚合” 的思想一致。</li>
<li><strong>Resource 的 getInputStream / 遍历</strong>：如 <code>Resource[]</code>、多位置资源时，通过迭代或 Stream 顺序访问，不暴露底层存储结构。</li>
<li><strong>Bean 定义、属性等的遍历</strong>：BeanDefinition 的 property、constructor-arg 等，通过迭代方式访问，内部结构可变化。</li>
<li><strong>Spring Data 的 Page / Slice</strong>：分页结果实现 <code>Iterable</code>，可用 for-each 或 Iterator 遍历，隐藏分页拉取细节。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>统一遍历多种集合</strong>：列表、树、图用同一 “迭代器” 接口遍历，调用方不关心底层是数组还是链表。</li>
<li><strong>惰性遍历 / 流式</strong>：迭代器按需取下一个元素，适合大集合或 “无限” 序列，不必一次性加载到内存。</li>
<li><strong>多种遍历方式</strong>：同一聚合提供 “正序迭代器”“逆序迭代器”“过滤迭代器” 等，满足不同遍历需求。</li>
<li><strong>封装内部结构</strong>：聚合内部从数组改为链表，只要迭代器接口不变，调用方无需修改。</li>
</ul>
<p><strong>不适用</strong>：简单数组/列表一次性遍历、且无多种遍历方式需求时，直接 for/forEach 即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>迭代器模式通过 “迭代器接口 + 聚合提供迭代器” 解耦遍历与存储；Java 与 Spring 中 <strong>Iterator/Iterable</strong>、<strong>集合与 Resource 的遍历</strong>、<strong>Spring Data 分页</strong> 都体现了该思想。业务上适用于<strong>统一遍历多种结构、惰性 / 流式遍历、多种遍历方式、封装内部结构</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>迭代器</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十八）备忘录模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-18-memento/</url>
    <content><![CDATA[<h1 id="备忘录模式memento"><a class="markdownIt-Anchor" href="#备忘录模式memento"></a> 备忘录模式（Memento）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>备忘录模式</strong>在不破坏封装的前提下捕获对象的内部状态，并在对象之外保存该状态，以便之后恢复。</p>
<p><strong>解决的问题</strong>：需要保存 / 恢复对象某一时刻的状态（如撤销、快照、回放），又不希望把内部状态直接暴露给外部；通过 “备忘录” 对象保存状态，由原发者自己写入与读出。</p>
<p><strong>定义</strong>：在不破坏封装性的前提下捕获对象的内部状态，并在对象之外保存这个状态，以便之后恢复。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Originator</strong>：原发者，产生需要保存的状态，可 createMemento () 生成备忘录，也可 restore (memento) 从备忘录恢复。</li>
<li><strong>Memento</strong>：备忘录，存放 Originator 的内部状态；通常只由 Originator 读写，对外可只暴露给 Caretaker 存储而不暴露内容。</li>
<li><strong>Caretaker</strong>：负责人，持有 Memento，不操作其内容，只负责 “保存” 和 “交给 Originator 恢复”。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Memento</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String state;</span><br><span class="line">    Memento(String state) { <span class="built_in">this</span>.state = state; }</span><br><span class="line">    String <span class="title function_">getState</span><span class="params">()</span> { <span class="keyword">return</span> state; }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Originator</span> {</span><br><span class="line">    <span class="keyword">private</span> String state;</span><br><span class="line">    <span class="keyword">public</span> Memento <span class="title function_">save</span><span class="params">()</span> { <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Memento</span>(state); }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">restore</span><span class="params">(Memento m)</span> { <span class="built_in">this</span>.state = m.getState(); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Caretaker</span> {</span><br><span class="line">    <span class="keyword">private</span> Memento memento;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">save</span><span class="params">(Memento m)</span> { <span class="built_in">this</span>.memento = m; }</span><br><span class="line">    <span class="keyword">public</span> Memento <span class="title function_">get</span><span class="params">()</span> { <span class="keyword">return</span> memento; }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>Spring Web Flow 状态管理</strong>：流程状态（如 conversation、flow 的 snapshot）被保存以便恢复、回退，可视为 “流程的备忘录”；状态存储在 session 或存储中，由框架管理。</li>
<li><strong>会话 / 请求级状态</strong>：如 “草稿”“多步表单的已填数据”，保存为某种 Memento 结构（如 Map、DTO），在提交前可恢复或重填。</li>
<li><strong>事务的 savepoint</strong>：数据库 savepoint 相当于 “事务状态的备忘录”，可回滚到某一点。</li>
<li><strong>快照 / 版本</strong>：实体保存历史版本（如文档版本、配置版本），需要时恢复到某一版本，本质是 “状态的备忘录 + 恢复”。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>撤销 / 重做</strong>：编辑器中每步操作前保存 Memento，撤销时 restore；命令模式常与备忘录结合（命令里保存执行前状态以便 undo）。</li>
<li><strong>草稿 / 自动保存</strong>：表单或文档定期把当前状态保存为 Memento，崩溃或关闭后可恢复。</li>
<li><strong>多步 wizard 的回退</strong>：每步完成后保存当前步骤与数据，用户可回到上一步并恢复当时状态。</li>
<li><strong>快照与回滚</strong>：配置、脚本、虚拟机等做快照，出问题后回滚到某快照。</li>
<li><strong>游戏存档</strong>：把角色 / 场景状态序列化为 Memento 存盘，读档时 restore。</li>
</ul>
<p><strong>不适用</strong>：状态简单、无需恢复或只需简单 “重算” 即可得到历史状态时，不必引入 Memento。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>备忘录模式通过 “原发者生成 / 恢复 Memento + Caretaker 只负责存储” 实现状态保存与恢复且不破坏封装；Spring 中 <strong>Web Flow 状态</strong>、<strong>会话 / 草稿</strong>、<strong>savepoint</strong> 有体现。业务上适用于<strong>撤销重做、草稿/自动保存、wizard 回退、快照/回滚、存档</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>备忘录</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十七）中介者模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-17-mediator/</url>
    <content><![CDATA[<h1 id="中介者模式mediator"><a class="markdownIt-Anchor" href="#中介者模式mediator"></a> 中介者模式（Mediator）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>中介者模式</strong>用一个中介对象封装一系列对象交互，使各对象不需要显式相互引用，从而降低耦合。</p>
<p><strong>解决的问题</strong>：多个对象之间两两通信会导致关系网状化、难以维护；引入中介者后，对象只与中介者通信，由中介者协调多方，结构变为星形。</p>
<p><strong>定义</strong>：用一个中介对象封装一系列对象交互；中介者使各对象不需要显式相互引用，从而使其耦合松散。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Mediator</strong>：中介者接口，定义与各 Colleague 通信的方法（如 send (message, from)）。</li>
<li><strong>ConcreteMediator</strong>：具体中介者，持有所有 Colleague 的引用，实现协调逻辑，负责把消息路由到目标 Colleague。</li>
<li><strong>Colleague</strong>：同事类，各 Colleague 只依赖 Mediator，不直接引用其他 Colleague；需要通知他人时调用 mediator.send()。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Mediator</span> {</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">send</span><span class="params">(String msg, Colleague from)</span>;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ConcreteMediator</span> <span class="keyword">implements</span> <span class="title class_">Mediator</span> {</span><br><span class="line">    <span class="keyword">private</span> Colleague a, b;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setColleagues</span><span class="params">(Colleague a, Colleague b)</span> { <span class="built_in">this</span>.a = a; <span class="built_in">this</span>.b = b; }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">send</span><span class="params">(String msg, Colleague from)</span> {</span><br><span class="line">        <span class="keyword">if</span> (from == a) b.receive(msg); <span class="keyword">else</span> a.receive(msg);</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Colleague</span> {</span><br><span class="line">    <span class="keyword">protected</span> Mediator mediator;</span><br><span class="line">    Colleague(Mediator m) { <span class="built_in">this</span>.mediator = m; }</span><br><span class="line">    <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title function_">receive</span><span class="params">(String msg)</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>MVC 中的 Controller</strong>：Controller 作为 “中介”，接收请求后协调 Model（服务 / 数据）与 View（渲染）；不直接让 View 与 Model 互相引用，降低耦合。</li>
<li><strong>事件与 ApplicationEventMulticaster</strong>：多个监听器与事件发布者不直接引用，通过 ApplicationContext（或 Multicaster）作为中介广播事件，监听器只订阅 “某类事件”。</li>
<li><strong>消息中间件</strong>：MQ 作为中介，生产者与消费者不直接引用，通过 Topic / Queue 通信，可视为 “消息中介者”。</li>
<li><strong>网关 / API 网关</strong>：网关作为中介，协调认证、路由、限流与后端服务，各服务不直接互调。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>多对象需协调</strong>：如 “订单创建” 涉及库存、支付、消息、积分等多模块，由 “订单服务” 或 “领域协调器” 作为中介，统一调用各模块，模块之间不直接依赖。</li>
<li><strong>聊天室 / 协作</strong>：多个用户 / 组件只与 “房间” 或 “会话” 通信，由中介者把消息分发给其他人。</li>
<li><strong>表单 / UI 组件联动</strong>：多个输入框、下拉框相互联动（如省市区、品类与规格），由表单中介者统一处理变更并更新其他组件。</li>
<li><strong>工作流 / 审批流</strong>：节点之间不直接引用，由流程引擎作为中介推进流程、分配任务。</li>
</ul>
<p><strong>不适用</strong>：对象之间关系简单、只有一对一或少量交互时，不必引入中介者。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>中介者模式通过 “中介者集中协调、同事只依赖中介者” 将网状交互变为星形；Spring 中 <strong>MVC Controller</strong>、<strong>事件广播</strong>、<strong>消息 / 网关</strong> 体现了中介思想。业务上适用于<strong>多对象协调、聊天 / 协作、表单联动、工作流</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>中介者</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（十九）观察者模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-19-observer/</url>
    <content><![CDATA[<h1 id="观察者模式observer"><a class="markdownIt-Anchor" href="#观察者模式observer"></a> 观察者模式（Observer）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>观察者模式</strong>定义对象间的一对多依赖，当一个对象状态改变时，其所有依赖者都会收到通知并自动更新。</p>
<p><strong>解决的问题</strong>：一个主题（被观察者）状态变化时，需要通知多个订阅者且不与之强耦合；主题只负责发布 “我变了”，观察者自行订阅与处理。</p>
<p><strong>定义</strong>：定义对象间的一种一对多依赖关系，使得每当一个对象状态发生改变时，其相关依赖对象皆得到通知并自动更新。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Subject</strong>：主题，维护观察者列表，提供 attach / detach，状态变化时 notify 所有观察者。</li>
<li><strong>Observer</strong>：观察者接口，定义 update ()（或 onEvent），供主题调用。</li>
<li><strong>ConcreteSubject</strong>：具体主题，状态改变时调用 notify。</li>
<li><strong>ConcreteObserver</strong>：具体观察者，实现 update () 以响应通知。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Observer</span> { <span class="keyword">void</span> <span class="title function_">update</span><span class="params">(String state)</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Subject</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> List&lt;Observer&gt; observers = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    <span class="keyword">private</span> String state;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">attach</span><span class="params">(Observer o)</span> { observers.add(o); }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setState</span><span class="params">(String state)</span> {</span><br><span class="line">        <span class="built_in">this</span>.state = state;</span><br><span class="line">        observers.forEach(o -&gt; o.update(state));</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>Spring 事件机制</strong>：<code>ApplicationEventPublisher.publishEvent(event)</code> 发布事件，实现 <code>ApplicationListener&lt;E&gt;</code> 或使用 <code>@EventListener</code> 的 Bean 作为观察者被自动调用；主题是 ApplicationContext（或事件发布者），观察者是各 Listener。</li>
<li><strong>ContextRefreshedEvent、RequestHandledEvent 等</strong>：容器刷新完成、请求处理完毕等生命周期节点发布事件，监听器可做初始化、统计、清理等，与核心逻辑解耦。</li>
<li><strong>自定义业务事件</strong>：继承 <code>ApplicationEvent</code>，在业务中 publish，多个监听器分别处理（发消息、记日志、更新缓存等），是观察者模式的直接应用。</li>
<li><strong>@TransactionalEventListener</strong>：在事务提交后等阶段再触发监听器，仍属 “事件 → 观察者” 模型。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>事件驱动、解耦通知</strong>：如 “订单支付成功” 后需要更新库存、发短信、送积分、记审计，支付模块只发布 “支付成功” 事件，各处理方订阅即可，支付模块不依赖具体处理逻辑。</li>
<li><strong>配置 / 数据变更通知</strong>：配置中心推送变更，多个服务订阅并刷新本地配置；缓存失效时通知多个节点。</li>
<li><strong>UI 与数据同步</strong>：MVC 中 Model 变化后通知 View 更新；前端也常用观察者 / 订阅发布（如 Rx、EventBus）。</li>
<li><strong>日志、监控、审计</strong>：核心业务发布 “关键操作” 事件，日志/监控/审计监听器统一处理，业务代码不写这些横切逻辑。</li>
<li><strong>生命周期钩子</strong>：容器启动/关闭、请求开始/结束发布事件，便于做统一初始化或清理。</li>
</ul>
<p><strong>不适用</strong>：通知方与接收方严格一对一、且逻辑稳定不变时，直接调用即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>观察者模式通过 “主题维护观察者列表 + 状态变化时通知” 实现一对多解耦；Spring 中 <strong>ApplicationEvent/ApplicationListener</strong>、<strong>@EventListener</strong>、<strong>@TransactionalEventListener</strong> 是典型实现。业务上适用于<strong>事件驱动、配置/缓存通知、UI 同步、日志/监控/审计、生命周期钩子</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>观察者</tag>
        <tag>事件</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（二十）状态模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-20-state/</url>
    <content><![CDATA[<h1 id="状态模式state"><a class="markdownIt-Anchor" href="#状态模式state"></a> 状态模式（State）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>状态模式</strong>允许对象在内部状态改变时改变其行为，对象看起来像是修改了类。</p>
<p><strong>解决的问题</strong>：当对象行为依赖多种状态且不同状态下行为差异大时，若用大量 if-else/switch 会难以维护；把 “每种状态” 封装成独立类，状态转换时切换当前状态对象，行为随状态对象变化。</p>
<p><strong>定义</strong>：允许一个对象在其内部状态改变时改变它的行为，对象看起来修改了它的类。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Context</strong>：上下文，持有当前 State，客户请求委托给当前 state 处理；可提供 setState () 供状态类切换状态。</li>
<li><strong>State</strong>：抽象状态，定义行为接口（如 handle ()）。</li>
<li><strong>ConcreteState</strong>：具体状态，实现行为，并在适当时机调用 context.setState(nextState) 切换状态。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">State</span> { <span class="keyword">void</span> <span class="title function_">handle</span><span class="params">(Context ctx)</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Context</span> {</span><br><span class="line">    <span class="keyword">private</span> State state;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setState</span><span class="params">(State s)</span> { <span class="built_in">this</span>.state = s; }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">request</span><span class="params">()</span> { state.handle(<span class="built_in">this</span>); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ConcreteStateA</span> <span class="keyword">implements</span> <span class="title class_">State</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handle</span><span class="params">(Context ctx)</span> {</span><br><span class="line">        <span class="comment">// 处理并可能切换</span></span><br><span class="line">        ctx.setState(<span class="keyword">new</span> <span class="title class_">ConcreteStateB</span>());</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>Spring State Machine</strong>：spring-statemachine 库显式支持状态机建模，状态与迁移由配置或代码定义，事件驱动迁移，是状态模式的完整实现；适用于订单、工作流、审批等有明确状态与迁移的场景。</li>
<li><strong>Bean 生命周期</strong>：Bean 的创建、初始化、运行、销毁等可视为不同 “状态”，容器在不同状态下执行不同逻辑（如 PostProcessor、InitializingBean），可抽象为状态思想。</li>
<li><strong>连接 / 会话状态</strong>：如 TCP 连接、WebSocket 会话的 “未连接/已连接/关闭中/已关闭”，用状态对象封装各阶段行为与迁移条件。</li>
<li><strong>事务状态</strong>：事务管理器内部有 “无事务/活跃/提交中/回滚中” 等状态，行为（提交、回滚、挂起）随状态变化。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>订单/工单/审批流</strong>：待支付、已支付、发货中、已完成、已取消等，每状态下可执行的操作与下一状态不同，用状态模式（或状态机）清晰建模。</li>
<li><strong>游戏 / 动画</strong>：角色 “待机/跑/攻击/受伤” 等，每状态对应不同动画与输入响应，切换状态即切换行为。</li>
<li><strong>连接 / 会话管理</strong>：连接建立、认证、传输、关闭等阶段，每阶段允许的操作与错误处理不同。</li>
<li><strong>UI 控件</strong>：按钮 “正常/禁用/加载中”，不同状态下点击行为、样式不同。</li>
<li><strong>协议解析</strong>：解析器处于 “读头/读体/读尾” 等状态，收到数据时根据当前状态决定如何处理与迁移。</li>
</ul>
<p><strong>不适用</strong>：状态很少、行为差异不大时，简单分支即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>状态模式通过 “状态即对象 + 行为委托给当前状态 + 状态间迁移” 消除复杂分支；Spring 中 <strong>Spring State Machine</strong>、<strong>Bean 生命周期</strong>、<strong>连接 / 事务状态</strong> 有体现。业务上适用于<strong>订单/工作流、游戏/动画、连接/会话、UI 状态、协议解析</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>状态</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（二十一）策略模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-21-strategy/</url>
    <content><![CDATA[<h1 id="策略模式strategy"><a class="markdownIt-Anchor" href="#策略模式strategy"></a> 策略模式（Strategy）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>策略模式</strong>定义一系列算法，将每个算法封装起来并使它们可互换；策略使算法独立于使用它的客户而变化。</p>
<p><strong>解决的问题</strong>：同一类问题有多种解法（如多种排序、多种支付、多种校验），若用 if-else 堆砌会难以扩展；把每种解法抽成独立策略类，客户依赖策略接口，运行时注入不同实现即可切换。</p>
<p><strong>定义</strong>：定义一系列算法，把它们一个个封装起来，并且使它们可以相互替换；策略模式使算法独立于使用它的客户。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Strategy</strong>：策略接口，定义算法方法（如 execute (data)）。</li>
<li><strong>ConcreteStrategy</strong>：具体策略，实现不同算法。</li>
<li><strong>Context</strong>：上下文，持有 Strategy，将客户请求委托给当前策略；可在构造或 setter 中注入策略。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Strategy</span> { <span class="type">int</span> <span class="title function_">compute</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span>; }</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Add</span> <span class="keyword">implements</span> <span class="title class_">Strategy</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compute</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> { <span class="keyword">return</span> a + b; }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Multiply</span> <span class="keyword">implements</span> <span class="title class_">Strategy</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compute</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> { <span class="keyword">return</span> a * b; }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Context</span> {</span><br><span class="line">    <span class="keyword">private</span> Strategy strategy;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setStrategy</span><span class="params">(Strategy s)</span> { <span class="built_in">this</span>.strategy = s; }</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">execute</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> { <span class="keyword">return</span> strategy.compute(a, b); }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>ResourceLoader / 资源加载策略</strong>：根据 URL 前缀（file:、classpath:、http:）选择不同 Resource 实现，可视为 “按前缀选择加载策略”；Spring 内置多种 Resource 实现，由 ResourceLoader 路由。</li>
<li><strong>AOP 代理策略</strong>：JdkDynamicAopProxy 与 Cglib2AopProxy 是两种 “生成代理” 的策略，根据目标是否实现接口选择其一。</li>
<li><strong>PlatformTransactionManager 与具体实现</strong>：事务管理是抽象，DataSourceTransactionManager、HibernateTransactionManager 等是不同策略，由配置或自动装配决定使用哪种。</li>
<li><strong>MessageConverter</strong>：HTTP 消息的序列化 / 反序列化有多种策略（JSON、XML 等），根据 Content-Type 或配置选择不同 Converter。</li>
<li><strong>CacheManager</strong>：缓存抽象，Redis、Caffeine、Simple 等是不同策略，通过配置切换。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>多种算法 / 规则可替换</strong>：如支付（微信/支付宝/银联）、运费计算（按重量/按体积/固定）、折扣（满减/会员价/券），每种一种策略，便于新增与测试。</li>
<li><strong>多数据源 / 多协议</strong>：根据配置或路由键选择不同数据源、不同 RPC 协议，客户只依赖 “获取连接 / 发请求” 的接口。</li>
<li><strong>多格式解析 / 序列化</strong>：同一业务数据支持 JSON/XML/二进制等多种格式，每种格式一个策略。</li>
<li><strong>多验证规则</strong>：校验逻辑有多种（格式、黑名单、风控），可组合或按场景选不同策略。</li>
<li><strong>多通知渠道</strong>：发送通知支持短信/邮件/站内信/推送，每种渠道一个策略，由配置或业务选择。</li>
</ul>
<p><strong>不适用</strong>：算法唯一或很少变化时，直接实现即可。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>策略模式通过 “策略接口 + 多种实现 + 上下文委托” 使算法可替换、易扩展；Spring 中 <strong>ResourceLoader</strong>、<strong>AOP 代理选择</strong>、<strong>事务管理器</strong>、<strong>MessageConverter</strong>、<strong>CacheManager</strong> 都是策略思想的体现。业务上适用于<strong>多种支付/计算/校验、多数据源/多协议、多格式、多通知渠道</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>策略</tag>
      </tags>
  </entry>
  <entry>
    <title>AbstractQueuedSynchronizer（AQS）详解与 JDK 演进</title>
    <url>/2025/03/01/2025-03-02-java-aqs-and-jdk-evolution/</url>
    <content><![CDATA[<h1 id="abstractqueuedsynchronizeraqs详解与-jdk-演进"><a class="markdownIt-Anchor" href="#abstractqueuedsynchronizeraqs详解与-jdk-演进"></a> AbstractQueuedSynchronizer（AQS）详解与 JDK 演进</h1>
<p>本文说明 <strong>AbstractQueuedSynchronizer（AQS）</strong> 的定位、核心结构、独占 / 共享模式与条件队列，并梳理 <strong>JDK 各版本</strong> 中与 AQS 相关的重要实现变化与缺陷修复（尤其是 <strong>Java 11 起用 VarHandle 管理状态</strong> 及后续维护性更新）。</p>
<hr>
<h2 id="一-aqs-是什么"><a class="markdownIt-Anchor" href="#一-aqs-是什么"></a> 一、AQS 是什么</h2>
<p><strong>AQS</strong> 位于 <code>java.util.concurrent.locks</code> 包，由 <strong>Doug Lea</strong> 设计，是 JUC 中 <strong>阻塞锁与同步器</strong> 的公共骨架：</p>
<ul>
<li><strong>ReentrantLock</strong>、<strong>ReentrantReadWriteLock</strong>（内部 Sync）</li>
<li><strong>Semaphore</strong></li>
<li><strong>CountDownLatch</strong></li>
<li><strong>ThreadPoolExecutor</strong>（Worker 关闭等场景）</li>
</ul>
<p>等均通过 <strong>继承 AQS</strong> 并实现 <code>tryAcquire</code> / <code>tryRelease</code>（独占）或 <code>tryAcquireShared</code> / <code>tryReleaseShared</code>（共享）等钩子，复用 <strong>FIFO 等待队列</strong> 与 <strong>阻塞 / 唤醒</strong> 逻辑。</p>
<p><strong>对外</strong>：应用代码通常只用 <code>Lock</code>、<code>Semaphore</code> 等 API；<strong>AQS 是 <code>public</code> 类</strong>，也可被子类扩展实现自定义同步器，但门槛较高。</p>
<hr>
<h2 id="二-核心结构"><a class="markdownIt-Anchor" href="#二-核心结构"></a> 二、核心结构</h2>
<h3 id="21-同步状态-state"><a class="markdownIt-Anchor" href="#21-同步状态-state"></a> 2.1 同步状态 state</h3>
<ul>
<li><strong><code>volatile int state</code></strong>：由子类约定语义。
<ul>
<li><strong>ReentrantLock</strong>：0 表示无主，&gt;0 表示持有次数（可重入）。</li>
<li><strong>Semaphore</strong>：可用许可数。</li>
<li><strong>CountDownLatch</strong>：还剩多少次 countDown。</li>
</ul>
</li>
</ul>
<p>通过 <strong><code>getState()</code> / <code>setState()</code> / <code>compareAndSetState()</code></strong> 读取与原子更新状态（具体底层实现随 JDK 版本演进，见第四节）。</p>
<h3 id="22-等待队列clh-变体"><a class="markdownIt-Anchor" href="#22-等待队列clh-变体"></a> 2.2 等待队列（CLH 变体）</h3>
<ul>
<li><strong>FIFO 双向链表</strong>，节点 <code>Node</code> 含：<code>waitStatus</code>、<code>prev</code>、<code>next</code>、<code>thread</code> 等。</li>
<li><strong><code>head</code> / <code>tail</code></strong> 指向队列头尾（头结点可视为占位，真正等待从其后继开始很常见）。</li>
<li>获取失败时线程被封装成 Node <strong>入队</strong>，经自旋后调用 <strong><code>LockSupport.park</code></strong> 阻塞；释放时 <strong><code>unpark</code></strong> 后继。</li>
</ul>
<h3 id="23-独占与共享"><a class="markdownIt-Anchor" href="#23-独占与共享"></a> 2.3 独占与共享</h3>
<table>
<thead>
<tr>
<th>模式</th>
<th>典型用途</th>
<th>模板入口</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>独占（Exclusive）</strong></td>
<td>互斥锁</td>
<td><code>acquire</code> / <code>release</code></td>
</tr>
<tr>
<td><strong>共享（Shared）</strong></td>
<td>信号量、读锁（阶段）</td>
<td><code>acquireShared</code> / <code>releaseShared</code></td>
</tr>
</tbody>
</table>
<p>子类实现对应的 <strong><code>tryAcquire</code> / <code>tryRelease</code></strong>** 或 <strong><code>tryAcquireShared</code> / <code>tryReleaseShared</code></strong>，返回是否成功；AQS 负责排队、阻塞与唤醒传播（共享模式下可能连续唤醒多个后继）。</p>
<h3 id="24-条件队列conditionobject"><a class="markdownIt-Anchor" href="#24-条件队列conditionobject"></a> 2.4 条件队列：ConditionObject</h3>
<ul>
<li><strong><code>ConditionObject</code></strong> 是 AQS 的<strong>内部类</strong>，实现 <code>Condition</code>。</li>
<li>每个 <code>Condition</code> 维护<strong>独立于锁等待队列</strong>的条件单向链表；<strong><code>await</code></strong> 释放锁并进入条件队列，<strong><code>signal</code></strong> 将节点移回锁等待队列。</li>
<li><strong><code>ReentrantLock.newCondition()</code></strong> 即基于同一套 AQS 状态与队列框架。</li>
</ul>
<h3 id="25-兄弟类abstractqueuedlongsynchronizer"><a class="markdownIt-Anchor" href="#25-兄弟类abstractqueuedlongsynchronizer"></a> 2.5 兄弟类：AbstractQueuedLongSynchronizer</h3>
<p>若 <code>int</code> 状态不够（如大范围计数），JDK 提供 <strong><code>AbstractQueuedLongSynchronizer</code></strong>：<strong><code>long</code> 型 state</strong> 与相同队列语义，用法与 AQS 平行。</p>
<hr>
<h2 id="三-典型-acquire-release-流程概念"><a class="markdownIt-Anchor" href="#三-典型-acquire-release-流程概念"></a> 三、典型 acquire / release 流程（概念）</h2>
<p><strong>独占 <code>acquire</code>：</strong></p>
<ol>
<li>调用子类 <strong><code>tryAcquire</code></strong>；成功则结束。</li>
<li>失败则 <strong>构造 Node、入队</strong>，在队列中自旋或 <strong><code>park</code></strong>。</li>
<li>被 <strong><code>release</code> 唤 unpark</strong> 后继续竞争 <code>tryAcquire</code>。</li>
</ol>
<p><strong><code>release</code>：</strong></p>
<ol>
<li>调用子类 <strong><code>tryRelease</code></strong>；成功则唤醒后继（<code>unparkSuccessor</code>）。</li>
</ol>
<p><strong>共享模式</strong>还会在释放后做 <strong>传播（propagate）</strong>，使多个等待线程在许可充足时依次通过。</p>
<p>（具体源码分支、中断、取消 <code>CANCELLED</code> 节点清理等细节较长，本文不展开到每一行。）</p>
<hr>
<h2 id="四-jdk-演进aqs之后的变化"><a class="markdownIt-Anchor" href="#四-jdk-演进aqs之后的变化"></a> 四、JDK 演进：AQS「之后的变化」</h2>
<p>对外 API（<code>acquire</code> / <code>release</code> / <code>Condition</code> 等）<strong>长期保持稳定</strong>；变化主要集中在 <strong>状态的原子实现</strong>、<strong>队列/中断/取消</strong> 的健壮性与微优化。</p>
<h3 id="41-java-8-及更早"><a class="markdownIt-Anchor" href="#41-java-8-及更早"></a> 4.1 Java 8 及更早</h3>
<ul>
<li>使用 <strong>Unsafe</strong> 或底层原子原语对 <strong><code>state</code> 字段</strong>做 CAS（<code>compareAndSwapInt</code> 等）。</li>
<li>AQS 框架形态已是：<strong>state + CLH 风格队列 + <code>LockSupport</code></strong>。</li>
</ul>
<h3 id="42-java-11varhandle-重写状态访问重要"><a class="markdownIt-Anchor" href="#42-java-11varhandle-重写状态访问重要"></a> 4.2 Java 11：<strong>VarHandle 重写状态访问（重要）</strong></h3>
<ul>
<li><strong><code>compareAndSetState</code> / <code>getState</code> / <code>setState</code></strong> 等改为通过 <strong><code>java.lang.invoke.VarHandle</code></strong> 访问 <strong><code>state</code></strong>，取代对 <strong><code>sun.misc.Unsafe</code></strong> 的直接依赖（与 <strong>JDK-8149644</strong> 等变更相关）。</li>
<li><strong>语义不变</strong>：仍满足 JMM 对同步器的要求；<strong>子类与 <code>ReentrantLock</code> 等公共 API 无需修改</strong>。</li>
<li><strong>动机</strong>：更安全、更规范的原子访问 API，便于维护与 JVM 优化；与 <strong>JEP 193（VarHandle）</strong> 路线一致。</li>
</ul>
<h3 id="43-java-12~15-附近修复与刷新"><a class="markdownIt-Anchor" href="#43-java-12~15-附近修复与刷新"></a> 4.3 Java 12～15 附近：修复与 “刷新”</h3>
<p>OpenJDK 变更记录中可见多例仅针对 AQS 或其子类交互的缺陷修复，例如：</p>
<ul>
<li><strong>中断与 <code>tryAcquire</code> 抛异常</strong> 时的处理（8191937）。</li>
<li><strong>cancel 节点</strong> 相关的竞态与队列一致性（8191483）。</li>
<li>未持锁就 <strong>await</strong> 导致队列损坏等问题（8187408）。</li>
<li><strong>8229442</strong>：对 AQS 与相关 lock 类的一批刷新 / 整理（实现层，非破坏性 API）。</li>
</ul>
<p>这些版本<strong>不增加新的 AQS 抽象模式</strong>，而是提高 <strong>正确性与边界行为</strong>。</p>
<h3 id="44-java-17~21-及以后"><a class="markdownIt-Anchor" href="#44-java-17~21-及以后"></a> 4.4 Java 17～21 及以后</h3>
<ul>
<li>AQS 作为稳定核心，随 <strong>LTS</strong> 继续接受小修复与性能相关微调（以各版本 <strong>Release Notes / JDK Issue</strong> 为准）。</li>
<li><strong>虚拟线程（Java 21）</strong>：阻塞在 <code>LockSupport.park</code> 上的锁等待路径与虚拟线程调度模型配合；<strong><code>synchronized</code></strong> 在早期虚拟线程上易产生 <strong>pin</strong>，而 <strong>基于 AQS 的 <code>ReentrantLock</code></strong> 常作为替代讨论对象 —— 这是<strong>使用侧</strong>的架构选择，不一定改变 AQS 类签名，但说明了 AQS 在运行时仍承担大量阻塞同步。</li>
</ul>
<h3 id="45-小结表"><a class="markdownIt-Anchor" href="#45-小结表"></a> 4.5 小结表</h3>
<table>
<thead>
<tr>
<th>阶段</th>
<th>AQS 相关变化要点</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>≤ JDK 10</strong></td>
<td><code>state</code> 多通过 Unsafe 类原子操作</td>
</tr>
<tr>
<td><strong>JDK 11</strong></td>
<td><strong><code>state</code> 通过 VarHandle</strong> 更新；对齐标准 JMM API</td>
</tr>
<tr>
<td><strong>JDK 12+</strong></td>
<td>以 <strong>bugfix、队列 / 中断一致性、微调</strong> 为主</td>
</tr>
<tr>
<td><strong>JDK 21+</strong></td>
<td>与 <strong>虚拟线程</strong> 生态协同；AQS 仍是 JUC 锁 / 同步器底座</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="五-学习与实践建议"><a class="markdownIt-Anchor" href="#五-学习与实践建议"></a> 五、学习与实践建议</h2>
<ol>
<li>读源码时抓住 <strong><code>acquire</code> / <code>release</code></strong> 与 <strong><code>Node.waitStatus</code></strong> 两条线，再看 <strong>条件队列</strong> <code>ConditionObject</code>。</li>
<li>自定义同步器优先评估是否可用 <strong><code>Semaphore</code> / <code>ReentrantLock</code></strong> 组合替代，<strong>直接继承 AQS</strong> 需严谨处理 <strong><code>tryRelease</code> 的返回值与 state 含义</strong>。</li>
<li>关注 <strong>JDK 发行说明</strong> 与 <strong>JDK Bug System</strong> 中带 <code>AbstractQueuedSynchronizer</code> 的条目，可获知具体版本行为修复。</li>
</ol>
<hr>
<h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2>
<p><strong>AbstractQueuedSynchronizer</strong> 用 <strong><code>volatile int state</code> + FIFO 等待队列 + LockSupport</strong> 统一实现阻塞式同步；<strong>独占 / 共享</strong>与 <strong>Condition</strong> 覆盖大部分 JUC 锁与门闩类。<strong>JDK 11</strong> 起用 <strong>VarHandle</strong> 管理 <strong><code>state</code></strong>，是 <strong>Java 8 之后最明显的实现层变化</strong>；其后版本以<strong>正确性修复与细节优化</strong>为主，公共模板 API 保持稳定。理解 AQS 有助于深入掌握 <strong>Java 并发与锁实现</strong>。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>并发</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>AQS</tag>
        <tag>AbstractQueuedSynchronizer</tag>
        <tag>并发</tag>
        <tag>JUC</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（二十二）模板方法模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-22-template-method/</url>
    <content><![CDATA[<h1 id="模板方法模式template-method"><a class="markdownIt-Anchor" href="#模板方法模式template-method"></a> 模板方法模式（Template Method）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>模板方法模式</strong>在抽象类中定义算法的骨架，将某些步骤延迟到子类；子类在不改变算法结构的前提下可重写特定步骤。</p>
<p><strong>解决的问题</strong>：多个类有相同流程（如 “打开连接 → 执行 → 关闭连接”），只有少数步骤不同；把相同流程放在父类模板方法中，不同步骤抽象为钩子方法由子类实现，避免重复代码。</p>
<p><strong>定义</strong>：在方法中定义算法的骨架，而将一些步骤延迟到子类；模板方法使子类可以在不改变算法结构的情况下重新定义算法的某些步骤。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>AbstractClass</strong>：抽象类，定义模板方法（如 templateMethod ()），内部按顺序调用抽象方法或钩子方法；部分步骤可有默认实现。</li>
<li><strong>ConcreteClass</strong>：具体子类，实现抽象方法或覆盖钩子方法，完成特定步骤的具体逻辑。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">AbstractClass</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">templateMethod</span><span class="params">()</span> {</span><br><span class="line">        step1();</span><br><span class="line">        step2();  <span class="comment">// 子类实现</span></span><br><span class="line">        step3();</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">step1</span><span class="params">()</span> { System.out.println(<span class="string">"step1"</span>); }</span><br><span class="line">    <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title function_">step2</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">step3</span><span class="params">()</span> { System.out.println(<span class="string">"step3"</span>); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ConcreteClass</span> <span class="keyword">extends</span> <span class="title class_">AbstractClass</span> {</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">step2</span><span class="params">()</span> { System.out.println(<span class="string">"custom step2"</span>); }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>JdbcTemplate</strong>：<code>query/update</code> 等方法是模板方法，内部固定 “获取连接、创建语句、执行、处理结果集、关闭资源”，把 “如何设置参数、如何映射结果” 通过回调（RowMapper、PreparedStatementCreator 等）交给调用方，是典型的 “模板 + 回调” 结合。</li>
<li><strong>RestTemplate</strong>：请求流程固定（构建请求、发送、处理响应），具体 URL、头、解析方式由调用方传入，模板方法思想。</li>
<li>*<em>AbstractController / 各类 <em>Template</em></em>：许多抽象基类定义 doGet / doPost 或 execute 模板，子类或回调填充具体步骤。</li>
<li><strong>Bean 的初始化 / 销毁</strong>：InitializingBean.afterPropertiesSet()、DisposableBean.destroy() 可视为 “生命周期模板” 中的钩子，子类实现以插入自定义逻辑。</li>
<li><strong>JpaDaoSupport / HibernateDaoSupport</strong>（传统用法）：父类提供 getSessionFactory、getHibernateTemplate 等模板式封装，子类专注业务方法。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>固定流程、可变步骤</strong>：如 “校验 → 查库 → 计算 → 落库 → 发消息”，其中 “计算”“发消息” 等由业务子类或策略实现，其余流程统一在模板中。</li>
<li><strong>复用公共骨架</strong>：如 “打开资源 → 处理 → 关闭资源”“开启事务 → 执行业务 → 提交 / 回滚”，骨架在基类，差异在子类。</li>
<li><strong>框架 / 基类设计</strong>：框架定义 “请求处理流程”“任务执行流程”，用户通过重写钩子或实现回调插入逻辑，而不改流程顺序。</li>
<li><strong>测试</strong>：模板方法便于用子类或匿名类只重写待测步骤，其余用默认实现，方便单测。</li>
</ul>
<p><strong>不适用</strong>：流程中每步都差异很大、几乎没有公共骨架时，用组合 + 策略更合适。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>模板方法模式通过 “抽象类定义模板方法 + 子类实现钩子” 复用算法骨架；Spring 中 <strong>JdbcTemplate</strong>、<strong>RestTemplate</strong>、<em><em>各类 <em>Template 与抽象基类</em></em>、<strong>生命周期钩子</strong> 是典型应用。业务上适用于</em><em>固定流程可变步骤、资源 / 事务骨架、框架扩展点</em> * 等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>模板方法</tag>
      </tags>
  </entry>
  <entry>
    <title>CGLIB 动态代理原理与使用</title>
    <url>/2025/03/01/2025-03-02-java-cglib-dynamic-proxy/</url>
    <content><![CDATA[<h1 id="cglib-动态代理原理与使用"><a class="markdownIt-Anchor" href="#cglib-动态代理原理与使用"></a> CGLIB 动态代理原理与使用</h1>
<p>本文介绍 <strong>CGLIB</strong>（Code Generation Library）动态代理：与 JDK 动态代理的差异、核心 API（Enhancer、MethodInterceptor）、使用示例、实现原理与限制，并简要说明在 Spring AOP 中的使用方式。</p>
<hr>
<h2 id="一-cglib-与-jdk-动态代理的差异"><a class="markdownIt-Anchor" href="#一-cglib-与-jdk-动态代理的差异"></a> 一、CGLIB 与 JDK 动态代理的差异</h2>
<table>
<thead>
<tr>
<th>对比项</th>
<th>JDK 动态代理</th>
<th>CGLIB 动态代理</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>代理方式</strong></td>
<td>实现与目标相同的<strong>接口</strong>，委托给 InvocationHandler</td>
<td>生成目标类的<strong>子类</strong>，重写非 final 方法</td>
</tr>
<tr>
<td><strong>前提</strong></td>
<td>目标必须实现接口</td>
<td>目标为<strong>普通类</strong>即可，不能是 final 类</td>
</tr>
<tr>
<td><strong>实现手段</strong></td>
<td>反射 + Proxy 生成实现接口的类</td>
<td><strong>ASM</strong> 字节码生成子类</td>
</tr>
<tr>
<td><strong>方法调用</strong></td>
<td>反射 Method.invoke</td>
<td>子类重写方法内调用 <strong>MethodProxy.invokeSuper</strong>（或反射）</td>
</tr>
<tr>
<td><strong>典型场景</strong></td>
<td>有接口的 Bean、RPC 客户端</td>
<td>无接口的 Bean、对类方法做 AOP</td>
</tr>
</tbody>
</table>
<p><strong>一句话</strong>：JDK Proxy 是 “实现同一接口的代理”；CGLIB 是 “继承目标类，用子类做代理”。</p>
<hr>
<h2 id="二-核心-api"><a class="markdownIt-Anchor" href="#二-核心-api"></a> 二、核心 API</h2>
<h3 id="21-enhancer"><a class="markdownIt-Anchor" href="#21-enhancer"></a> 2.1 Enhancer</h3>
<ul>
<li><strong>Enhancer</strong> 是 CGLIB 里用来生成 “代理子类” 的入口。</li>
<li>常用步骤：
<ul>
<li><strong>setSuperclass(Class)</strong>：指定要继承的目标类（被代理类）。</li>
<li><strong>setCallback(Callback)</strong>：设置方法拦截逻辑，一般用 <strong>MethodInterceptor</strong>。</li>
<li><strong>create()</strong>：无参时生成代理实例（会调用目标类无参构造）；<strong>create(Class[], Object[])</strong>：指定构造器参数类型与实参。</li>
</ul>
</li>
</ul>
<h3 id="22-methodinterceptor"><a class="markdownIt-Anchor" href="#22-methodinterceptor"></a> 2.2 MethodInterceptor</h3>
<ul>
<li><strong>Callback</strong> 的子接口，最常用的拦截器。</li>
<li>方法：<strong>Object intercept(Object obj, Method method, Object[] args, MethodProxy proxy)</strong>
<ul>
<li><strong>obj</strong>：CGLIB 生成的代理对象（子类实例）。</li>
<li><strong>method</strong>：被拦截的方法（目标类上的方法）。</li>
<li><strong>args</strong>：方法参数。</li>
<li><strong>proxy</strong>：<strong>MethodProxy</strong>，用于调用 “父类（目标类）的原始方法”，避免反射，性能更好。</li>
</ul>
</li>
<li>调用 <strong>proxy.invokeSuper(obj, args)</strong> 即执行 “目标类上的该方法”，返回值即原方法返回值。</li>
</ul>
<h3 id="23-其他-callback-类型"><a class="markdownIt-Anchor" href="#23-其他-callback-类型"></a> 2.3 其他 Callback 类型</h3>
<ul>
<li><strong>NoOp.INSTANCE</strong>：不增强，直接调用父类实现，常用于 <strong>CallbackFilter</strong> 中 “某些方法不拦截”。</li>
<li><strong>FixedValue</strong>：不执行原方法，每次返回 <strong>loadObject()</strong> 的固定值，需与返回类型兼容。</li>
<li><strong>CallbackFilter</strong>：按 <strong>Method</strong> 返回不同 Callback 数组下标，实现 “按方法选择不同拦截逻辑”。</li>
</ul>
<hr>
<h2 id="三-基本使用示例"><a class="markdownIt-Anchor" href="#三-基本使用示例"></a> 三、基本使用示例</h2>
<p><strong>依赖</strong>（Maven 示例，CGLIB 独立库）：</p>
<figure class="highlight xml"><table><tbody><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>cglib<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>cglib<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.3.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></tbody></table></figure>
<p><strong>目标类（无接口）</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserService</span> {</span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">login</span><span class="params">(String username)</span> {</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"User "</span> + username + <span class="string">" logged in."</span>;</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">logout</span><span class="params">()</span> {</span><br><span class="line">        System.out.println(<span class="string">"logout"</span>);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><strong>创建 CGLIB 代理</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> net.sf.cglib.proxy.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CglibProxyDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="type">Enhancer</span> <span class="variable">enhancer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Enhancer</span>();</span><br><span class="line">        enhancer.setSuperclass(UserService.class);</span><br><span class="line">        enhancer.setCallback((MethodInterceptor) (obj, method, args, proxy) -&gt; {</span><br><span class="line">            System.out.println(<span class="string">"Before: "</span> + method.getName());</span><br><span class="line">            <span class="type">Object</span> <span class="variable">result</span> <span class="operator">=</span> proxy.invokeSuper(obj, args);  <span class="comment">// 调用目标类方法</span></span><br><span class="line">            System.out.println(<span class="string">"After: "</span> + method.getName());</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        });</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 无参 create() 会走目标类无参构造</span></span><br><span class="line">        <span class="type">UserService</span> <span class="variable">proxy</span> <span class="operator">=</span> (UserService) enhancer.create();</span><br><span class="line">        System.out.println(proxy.login(<span class="string">"Alice"</span>));</span><br><span class="line">        proxy.logout();</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>输出示例：</p>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Before: login</span><br><span class="line">After: login</span><br><span class="line">User Alice logged in.</span><br><span class="line">Before: logout</span><br><span class="line">logout</span><br><span class="line">After: logout</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>若目标类<strong>没有无参构造</strong>，需用 <strong>enhancer.create(Class[] parameterTypes, Object[] args)</strong> 指定有参构造的类型与实参。</li>
<li>在 <strong>intercept</strong> 里不要用 <strong>method.invoke(obj, args)</strong> 调用 “当前方法”，会再次进入拦截器造成递归；应使用 <strong>proxy.invokeSuper(obj, args)</strong> 调用父类（目标类）实现。</li>
</ul>
<hr>
<h2 id="四-实现原理简述"><a class="markdownIt-Anchor" href="#四-实现原理简述"></a> 四、实现原理简述</h2>
<ol>
<li><strong>字节码生成</strong>：CGLIB 使用 <strong>ASM</strong> 在内存中生成目标类的子类字节码，子类重写目标类中所有 <strong>非 final</strong> 的 public/protected 方法。</li>
<li><strong>方法体</strong>：重写后的方法体内不直接写业务逻辑，而是调用 <strong>Callback</strong>（如 MethodInterceptor）的 <strong>intercept</strong>，并把 <strong>MethodProxy</strong> 传进去。</li>
<li><strong>MethodProxy</strong>：内部使用 <strong>FastClass</strong> 机制（为类生成下标索引，按索引调用方法），避免每次反射 <strong>Method.invoke</strong>，因此多次调用时往往比纯反射更快。</li>
<li><strong>加载</strong>：生成的字节码通过 <strong>ClassLoader.defineClass</strong> 或类似方式加载，再通过反射取构造器并 <strong>newInstance</strong> 得到代理实例。</li>
</ol>
<p>因此 CGLIB 代理对象在运行时是 “目标类的子类”，<code>proxy instanceof UserService</code> 为 true。</p>
<hr>
<h2 id="五-限制与注意点"><a class="markdownIt-Anchor" href="#五-限制与注意点"></a> 五、限制与注意点</h2>
<h3 id="51-final-类与-final-方法"><a class="markdownIt-Anchor" href="#51-final-类与-final-方法"></a> 5.1 final 类与 final 方法</h3>
<ul>
<li><strong>final 类</strong>：不能被继承，CGLIB 无法生成子类，无法代理。</li>
<li><strong>final 方法</strong>：不能被子类重写，CGLIB 无法拦截，调用时直接走目标类实现。</li>
</ul>
<h3 id="52-构造器"><a class="markdownIt-Anchor" href="#52-构造器"></a> 5.2 构造器</h3>
<ul>
<li><strong>create()</strong> 无参时，会调用目标类的 <strong>无参构造器</strong>。若目标类没有无参构造器，需使用 <strong>create(Class[] parameterTypes, Object[] args)</strong> 指定可访问的构造器及参数。</li>
<li>目标类若只有 <strong>private 构造器</strong>，子类无法在其构造器里调用 <code>super(...)</code>，会报错（如 IllegalAccessError），这类类无法用 CGLIB 代理。</li>
</ul>
<h3 id="53-与-jdk-proxy-的选用"><a class="markdownIt-Anchor" href="#53-与-jdk-proxy-的选用"></a> 5.3 与 JDK Proxy 的选用</h3>
<ul>
<li>有<strong>接口</strong>且只需代理接口方法 → 优先 <strong>JDK 动态代理</strong>（无额外依赖、与模块化兼容更好）。</li>
<li><strong>无接口</strong>或需要代理 “类上的方法” → 使用 <strong>CGLIB</strong>（或 Spring 封装后的 CGLIB）。</li>
</ul>
<hr>
<h2 id="六-在-spring-aop-中的使用"><a class="markdownIt-Anchor" href="#六-在-spring-aop-中的使用"></a> 六、在 Spring AOP 中的使用</h2>
<ul>
<li>Spring AOP 在对 <strong>Bean</strong> 做代理时：
<ul>
<li>若 Bean <strong>实现了接口</strong>且未强制指定 “按类代理”，默认用 <strong>JDK 动态代理</strong>（基于接口）。</li>
<li>若 Bean <strong>没有实现接口</strong>，或配置了 <strong>proxy-target-class=true</strong>，则使用 <strong>CGLIB</strong> 生成目标类的子类代理。</li>
</ul>
</li>
<li>Spring 自带的 CGLIB 相关类在 <strong>spring-core</strong> 中（如 <code>org.springframework.cglib.proxy.Enhancer</code>），多数场景无需单独引入 cglib 依赖；高版本 Spring 可能使用 repackage 后的 CGLIB 或其它字节码方案，但用法概念一致：基于子类、MethodInterceptor、invokeSuper。</li>
</ul>
<hr>
<h2 id="七-小结"><a class="markdownIt-Anchor" href="#七-小结"></a> 七、小结</h2>
<ul>
<li><strong>CGLIB</strong> 通过 <strong>Enhancer</strong> 指定 <strong>setSuperclass</strong> 和 <strong>setCallback(MethodInterceptor)</strong>，在运行时生成目标类的<strong>子类</strong>，重写非 final 方法并在方法内转交 <strong>intercept</strong>，通过 <strong>MethodProxy.invokeSuper</strong> 调用原始逻辑。</li>
<li><strong>特点</strong>：可代理 “无接口的类”，基于 ASM / FastClass，适合对类方法做 AOP；<strong>限制</strong>：不能代理 final 类 / 方法，目标类需有可被调用的构造器（通常为无参）。</li>
<li>与 <strong>JDK 动态代理</strong> 互补：有接口用 JDK Proxy，无接口或需代理类方法用 CGLIB；Spring AOP 会根据 Bean 是否有接口及配置自动选择二者之一。</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
        <category>源码解读</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>CGLIB</tag>
        <tag>动态代理</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式（二十三）访问者模式 — 详解、Spring 运用与业务场景</title>
    <url>/2025/03/01/2025-03-02-design-pattern-23-visitor/</url>
    <content><![CDATA[<h1 id="访问者模式visitor"><a class="markdownIt-Anchor" href="#访问者模式visitor"></a> 访问者模式（Visitor）</h1>
<h2 id="一-模式概述"><a class="markdownIt-Anchor" href="#一-模式概述"></a> 一、模式概述</h2>
<p><strong>访问者模式</strong>将作用于某对象结构中的各元素的操作封装成访问者，可在不改变各元素类的前提下定义作用于这些元素的新操作。</p>
<p><strong>解决的问题</strong>：对象结构（如 AST、组合结构）稳定，但需要经常增加 “对结构中元素的新操作”；若把操作写在每个元素里会导致元素类不断修改；访问者把 “操作” 从元素中抽离，每种操作一个 Visitor，元素只负责 accept (visitor) 并委托 visitor.visit(this)。</p>
<p><strong>定义</strong>：表示一个作用于某对象结构中的各元素的操作；可以在不改变各元素类的前提下定义作用于这些元素的新操作。</p>
<hr>
<h2 id="二-结构与角色"><a class="markdownIt-Anchor" href="#二-结构与角色"></a> 二、结构与角色</h2>
<ul>
<li><strong>Visitor</strong>：访问者接口，为结构中每个具体元素声明 visit (ConcreteElement) 方法。</li>
<li><strong>ConcreteVisitor</strong>：具体访问者，实现每个 visit 方法，即 “对某类元素做某种操作” 的具体逻辑。</li>
<li><strong>Element</strong>：元素接口，声明 accept (Visitor)。</li>
<li><strong>ConcreteElement</strong>：具体元素，accept 中调用 visitor.visit(this)，把自身交给访问者处理。</li>
<li><strong>ObjectStructure</strong>：对象结构，持有一组 Element，可提供 “遍历并 accept 某 visitor” 的入口。</li>
</ul>
<p><strong>示例</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Element</span> { <span class="keyword">void</span> <span class="title function_">accept</span><span class="params">(Visitor v)</span>; }</span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Visitor</span> {</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">visit</span><span class="params">(ElementA a)</span>;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">visit</span><span class="params">(ElementB b)</span>;</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ElementA</span> <span class="keyword">implements</span> <span class="title class_">Element</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">accept</span><span class="params">(Visitor v)</span> { v.visit(<span class="built_in">this</span>); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ElementB</span> <span class="keyword">implements</span> <span class="title class_">Element</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">accept</span><span class="params">(Visitor v)</span> { v.visit(<span class="built_in">this</span>); }</span><br><span class="line">}</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ConcreteVisitor</span> <span class="keyword">implements</span> <span class="title class_">Visitor</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">visit</span><span class="params">(ElementA a)</span> { <span class="comment">/* 对 A 的操作 */</span> }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">visit</span><span class="params">(ElementB b)</span> { <span class="comment">/* 对 B 的操作 */</span> }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-在-spring-中的运用"><a class="markdownIt-Anchor" href="#三-在-spring-中的运用"></a> 三、在 Spring 中的运用</h2>
<ul>
<li><strong>BeanDefinitionVisitor</strong>：遍历 BeanDefinition 中的属性、构造参数等 “元素”，对其中引用的字符串、值做访问（如解析占位符、计算），可视为 “对 BeanDefinition 结构的访问者”。</li>
<li><strong>元数据访问</strong>：对类、方法、字段上的注解做 “扫描”“校验”“生成配置” 等不同操作时，每种操作可对应一个访问者，遍历元数据并 accept。</li>
<li><strong>AST / 表达式树</strong>：若用树表示表达式或 DSL，对树的 “求值”“打印”“优化” 等不同操作可用不同 Visitor 实现，Spring 的 SpEL 等内部有类似结构。</li>
<li><strong>序列化 / 反序列化</strong>：对对象图的 “遍历并序列化” 可抽象为访问者，不同格式（JSON / XML）对应不同 Visitor。</li>
</ul>
<hr>
<h2 id="四-适用业务场景"><a class="markdownIt-Anchor" href="#四-适用业务场景"></a> 四、适用业务场景</h2>
<ul>
<li><strong>稳定结构 + 多变操作</strong>：如语法树、DOM、组合树，结构稳定，但需要 “类型检查”“格式化”“求值”“翻译” 等多种操作，每种操作一个 Visitor，便于新增操作而不改节点类。</li>
<li><strong>报表 / 统计</strong>：对同一批数据做 “汇总”“导出 Excel”“导出 PDF” 等，数据视为元素，每种导出方式一个 Visitor。</li>
<li><strong>编译器 / 解释器</strong>：AST 的语义分析、代码生成、优化等，各阶段可视为不同访问者遍历同一棵 AST。</li>
<li><strong>差异 / 合并</strong>：比较两棵结构（如配置树、文档树），可对其中一棵用 “比较访问者”（另一棵作为参数）实现 diff / merge。</li>
<li><strong>序列化 / 遍历对象图</strong>：按固定顺序遍历对象图并做不同处理（序列化、拷贝、校验），可用访问者封装 “处理逻辑”。</li>
</ul>
<p><strong>不适用</strong>：结构经常变化（增删节点类型）时，每增加节点类型就要改所有 Visitor，反而麻烦；此时考虑其他方式（如策略 + 类型分发）。</p>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<p>访问者模式通过 “元素 accept (visitor) + visitor.visit(具体元素)” 把 “对结构的操作” 从元素中分离；Spring 中 <strong>BeanDefinitionVisitor</strong>、<strong>元数据扫描 / 处理</strong>、<strong>AST / 表达式处理</strong> 有体现。业务上适用于<strong>稳定结构上的多种操作、报表/导出、编译器/解释器、diff/merge、对象图遍历</strong>等场景。</p>
]]></content>
      <categories>
        <category>设计模式</category>
        <category>行为型</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
        <tag>Spring</tag>
        <tag>访问者</tag>
      </tags>
  </entry>
  <entry>
    <title>ReentrantLock 详解与 Java 8 之后版本相关改动</title>
    <url>/2025/03/01/2025-03-02-java-reentrantlock-and-jdk-changes/</url>
    <content><![CDATA[<h1 id="reentrantlock-详解与-java-8-之后版本相关改动"><a class="markdownIt-Anchor" href="#reentrantlock-详解与-java-8-之后版本相关改动"></a> ReentrantLock 详解与 Java 8 之后版本相关改动</h1>
<p>本文说明 <strong>ReentrantLock</strong> 的定位、与 <code>synchronized</code> 的差异、核心 API、底层与 <strong>AbstractQueuedSynchronizer（AQS）</strong> 的关系，以及 <strong>Java 8 之后各版本</strong> 与可重入锁相关的实现与生态变化（VarHandle、AQS 内部实现、虚拟线程与 “锁” 等）。</p>
<hr>
<h2 id="一-reentrantlock-是什么"><a class="markdownIt-Anchor" href="#一-reentrantlock-是什么"></a> 一、ReentrantLock 是什么</h2>
<p><code>ReentrantLock</code> 位于 <code>java.util.concurrent.locks</code> 包，实现 <code>Lock</code> 接口，提供<strong>可重入的互斥锁</strong>：</p>
<ul>
<li><strong>可重入</strong>：同一线程可多次 <code>lock()</code>，需相应次数 <code>unlock()</code> 才真正释放。</li>
<li><strong>显式加解锁</strong>：不在 JVM 监视器（<code>synchronized</code>）语义内，必须手写 <code>lock()</code> / <code>unlock()</code>，通常放在 <code>try/finally</code> 里保证释放。</li>
<li><strong>公平 / 非公平</strong>：构造时可选 <code>new ReentrantLock(true)</code> 为<strong>公平锁</strong>（大致按排队顺序获取）；默认 <strong>false</strong> 为<strong>非公平</strong>，吞吐量通常更高。</li>
</ul>
<p>内部依赖 <strong>AQS</strong>（<code>AbstractQueuedSynchronizer</code>）维护状态与<strong> FIFO 等待队列</strong>，<code>LockSupport.park/unpark</code> 阻塞 / 唤醒线程。</p>
<hr>
<h2 id="二-与-synchronized-的对比"><a class="markdownIt-Anchor" href="#二-与-synchronized-的对比"></a> 二、与 synchronized 的对比</h2>
<table>
<thead>
<tr>
<th>对比项</th>
<th>synchronized</th>
<th>ReentrantLock</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>语法</strong></td>
<td>关键字，自动在块结束时释放</td>
<td>显式 <code>lock</code>/<code>unlock</code></td>
</tr>
<tr>
<td><strong>可中断</strong></td>
<td>阻塞不可被中断</td>
<td><code>lockInterruptibly()</code> 可响应中断</td>
</tr>
<tr>
<td><strong>超时 / 尝试</strong></td>
<td>无</td>
<td><code>tryLock()</code>、<code>tryLock(time, unit)</code></td>
</tr>
<tr>
<td><strong>条件队列</strong></td>
<td><code>wait</code>/<code>notify</code> 绑定一个条件</td>
<td><code>newCondition()</code> 可多个 <code>Condition</code></td>
</tr>
<tr>
<td><strong>公平性</strong></td>
<td>非公平</td>
<td>可选公平或非公平</td>
</tr>
<tr>
<td><strong>作用域</strong></td>
<td>块结构，易避免忘记释放</td>
<td>更灵活，但需自己保证在 finally 中 <code>unlock</code></td>
</tr>
</tbody>
</table>
<p><strong>选型建议</strong>：大多数互斥场景 <code>synchronized</code> 足够且更简单；需要<strong>可中断、定时获取、多条件、公平策略</strong>或与其他 Lock API 统一建模时用 <code>ReentrantLock</code>。</p>
<hr>
<h2 id="三-核心-api-用法"><a class="markdownIt-Anchor" href="#三-核心-api-用法"></a> 三、核心 API 用法</h2>
<h3 id="31-标准写法"><a class="markdownIt-Anchor" href="#31-标准写法"></a> 3.1 标准写法</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="type">ReentrantLock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReentrantLock</span>();</span><br><span class="line">lock.lock();</span><br><span class="line"><span class="keyword">try</span> {</span><br><span class="line">    <span class="comment">// 临界区</span></span><br><span class="line">} <span class="keyword">finally</span> {</span><br><span class="line">    lock.unlock();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="32-可中断获取"><a class="markdownIt-Anchor" href="#32-可中断获取"></a> 3.2 可中断获取</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">lock.lockInterruptibly();</span><br><span class="line"><span class="keyword">try</span> {</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">} <span class="keyword">finally</span> {</span><br><span class="line">    lock.unlock();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>等待过程中线程可被 <code>interrupt()</code>，抛出 <code>InterruptedException</code>；适合可取消的长任务。</p>
<h3 id="33-尝试与定时获取"><a class="markdownIt-Anchor" href="#33-尝试与定时获取"></a> 3.3 尝试与定时获取</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (lock.tryLock()) {</span><br><span class="line">    <span class="keyword">try</span> {</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    } <span class="keyword">finally</span> {</span><br><span class="line">        lock.unlock();</span><br><span class="line">    }</span><br><span class="line">} <span class="keyword">else</span> {</span><br><span class="line">    <span class="comment">// 未拿到锁的降级逻辑</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (lock.tryLock(<span class="number">3</span>, TimeUnit.SECONDS)) {</span><br><span class="line">    <span class="keyword">try</span> {</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    } <span class="keyword">finally</span> {</span><br><span class="line">        lock.unlock();</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="34-condition多条件等待"><a class="markdownIt-Anchor" href="#34-condition多条件等待"></a> 3.4 Condition（多条件等待）</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="type">ReentrantLock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReentrantLock</span>();</span><br><span class="line"><span class="type">Condition</span> <span class="variable">notEmpty</span> <span class="operator">=</span> lock.newCondition();</span><br><span class="line"><span class="type">Condition</span> <span class="variable">notFull</span> <span class="operator">=</span> lock.newCondition();</span><br><span class="line"></span><br><span class="line">lock.lock();</span><br><span class="line"><span class="keyword">try</span> {</span><br><span class="line">    <span class="keyword">while</span> (queue.isEmpty()) notEmpty.await();</span><br><span class="line">    <span class="comment">// 取元素</span></span><br><span class="line">    notFull.signal();</span><br><span class="line">} <span class="keyword">finally</span> {</span><br><span class="line">    lock.unlock();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="四-实现原理简述aqs"><a class="markdownIt-Anchor" href="#四-实现原理简述aqs"></a> 四、实现原理简述（AQS）</h2>
<ul>
<li><code>ReentrantLock</code> 内部有一个 <code>Sync</code> 继承 <strong>AQS</strong>，用 <strong>state</strong> 表示重入次数：首次获取为 1，同线程再次 <code>lock</code> 则 <code>state++</code>，<code>unlock</code> 时 <code>state--</code>，到 0 才真正释放。</li>
<li><strong>非公平</strong>：新来的线程可能插队抢锁（在实现允许的情况下），减少上下文切换，通常更快。</li>
<li><strong>公平</strong>：先检查队列中是否有等待者，尝试减少 “饥饿”，但吞吐量可能略低。</li>
</ul>
<p>AQS 使用 <strong>CAS</strong> 更新状态、<strong>CLH 变体队列</strong> 管理等待节点，阻塞时使用 <code>LockSupport.park</code>，唤醒用 <code>unpark</code>。这些在 Java 8 及以后大方向一致，<strong>JDK 内部的具体原子实现</strong>在 8 之后有演进（见下节）。</p>
<hr>
<h2 id="五-java-8-之后与-reentrantlock-相关的改动"><a class="markdownIt-Anchor" href="#五-java-8-之后与-reentrantlock-相关的改动"></a> 五、Java 8 之后与 ReentrantLock 相关的改动</h2>
<p>下面区分：<strong>对外 API</strong> 与 <strong>JVM / JUC 内部实现</strong>，以及与 <strong>虚拟线程</strong> 的关系。</p>
<h3 id="51-java-9varhandlejep-193铺好底层基础设施"><a class="markdownIt-Anchor" href="#51-java-9varhandlejep-193铺好底层基础设施"></a> 5.1 Java 9：VarHandle（JEP 193）铺好底层基础设施</h3>
<ul>
<li>Java 9 引入 <strong>VarHandle</strong>，提供类型安全、规范化的字段/数组/静态变量的细粒度访问（含 volatile、CAS 等），旨在替代部分 <strong>sun.misc.Unsafe</strong> 用法。</li>
<li><strong><code>ReentrantLock</code> 的公开 API 在 Java 9 未做破坏性变更</strong>；变化主要在 JUC 底层逐步采用更规范的内存访问原语。</li>
</ul>
<h3 id="52-java-11aqs-等同步器内部迁移到-varhandlejdk-8149644-等"><a class="markdownIt-Anchor" href="#52-java-11aqs-等同步器内部迁移到-varhandlejdk-8149644-等"></a> 5.2 Java 11：AQS 等同步器内部迁移到 VarHandle（JDK-8149644 等）</h3>
<ul>
<li><strong>AbstractQueuedSynchronizer</strong> 等对 <strong>state</strong> 的 CAS、volatile 读写等，在 JDK 11 前后改为通过 <strong>VarHandle</strong> 完成，减少对 Unsafe 的直接依赖，语义更清晰，便于维护与优化。</li>
<li>对应用开发者而言：<strong>调用方式不变</strong>，<strong>行为与内存模型语义仍与 JLS 监视器 / 锁规则一致</strong>；可能带来实现层面的性能微调，但一般无需改代码。</li>
</ul>
<h3 id="53-java-8-已存在-常与-reentrantlock-对比的-apistampedlock"><a class="markdownIt-Anchor" href="#53-java-8-已存在-常与-reentrantlock-对比的-apistampedlock"></a> 5.3 Java 8 已存在、常与 ReentrantLock 对比的 API：StampedLock</h3>
<ul>
<li><strong>StampedLock</strong>（Java 8 引入）不是 <code>ReentrantLock</code> 的替代品提供互斥的简单升级版，而是<strong>读写 + 乐观读</strong>的另一套模型；<strong>不可重入</strong>，且无 <code>Condition</code>。</li>
<li><strong>读多写少</strong>且能接受更复杂用法时，可考虑 <code>StampedLock</code>；<strong>需要可重入或 Condition</strong> 时仍用 <code>ReentrantLock</code> / <code>ReentrantReadWriteLock</code>。</li>
</ul>
<h3 id="54-java-21虚拟线程jep-444与锁"><a class="markdownIt-Anchor" href="#54-java-21虚拟线程jep-444与锁"></a> 5.4 Java 21：虚拟线程（JEP 444）与锁</h3>
<ul>
<li><strong>虚拟线程</strong>在阻塞时（包括通过 JUC 锁等待）使用 <strong>pinning 较少</strong> 的路径时，由运行时调度到 <code>LockSupport.park</code>，可释放底层载体线程给其它虚拟线程使用。</li>
<li><strong><code>synchronized</code> 在 JDK 21 中仍可能导致虚拟线程 “钉住”（pin）平台线程</strong>，高并发阻塞在监视器上会压缩虚拟线程优势；在虚拟线程环境下，<strong>需要长时间持锁或复杂阻塞时，更倾向使用 <code>ReentrantLock</code> 等 <code>java.util.concurrent</code> 锁</strong>，这也是官方文档与实践中常提到的差异之一。</li>
<li><strong>JEP 491（JDK 24 起）</strong>：改进 <strong>synchronized</strong> 与虚拟线程的配合，减少 pin；<strong>不替代</strong>按需使用 <code>ReentrantLock</code> 的场景，但缩小了 “只用 JVM 监视器” 的劣势。</li>
</ul>
<h3 id="55-小结表对应用代码的含义"><a class="markdownIt-Anchor" href="#55-小结表对应用代码的含义"></a> 5.5 小结表（对应用代码的含义）</h3>
<table>
<thead>
<tr>
<th>版本区间</th>
<th>与 ReentrantLock 最相关的内容</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Java 8</strong></td>
<td>API 形态稳定；与 <code>StampedLock</code> 并存</td>
</tr>
<tr>
<td><strong>Java 9</strong></td>
<td>VarHandle 引入，为后续 JUC 内部改写奠基</td>
</tr>
<tr>
<td><strong>Java 11</strong></td>
<td>AQS 等内部广泛使用 VarHandle，<strong>API 不变</strong></td>
</tr>
<tr>
<td><strong>Java 21</strong></td>
<td>虚拟线程：<strong>JUC 锁</strong>与 <code>synchronized</code> 对虚拟线程的友好度差异需纳入设计</td>
</tr>
<tr>
<td><strong>JDK 24+</strong></td>
<td><code>synchronized</code> 与虚拟线程行为改进（JEP 491），策略上可多评估是否仍需显式 Lock</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="六-实践注意点"><a class="markdownIt-Anchor" href="#六-实践注意点"></a> 六、实践注意点</h2>
<ol>
<li><strong>必须在 finally 中 <code>unlock</code></strong>，且<strong>仅释放在本线程成功获取的锁</strong>；<code>tryLock</code> 失败时不要 <code>unlock</code>。</li>
<li><strong>可重入</strong>：递归或同线程嵌套调用若都 <code>lock</code>，必须成对 <code>unlock</code>，否则泄漏或死锁。</li>
<li><strong>Condition 使用</strong>：须在持有对应 <code>lock</code> 时 <code>await</code>/<code>signal</code>，语义类似 <code>wait</code>/<code>notify</code> 但粒度可多路。</li>
<li><strong>性能</strong>：默认非公平锁通常优于公平锁；公平锁用于对 “饥饿” 极敏感的场景。</li>
<li><strong>虚拟线程</strong>：大量阻塞在锁上的逻辑，优先考虑 <strong>JUC Lock</strong> + 短临界区，避免在 <code>synchronized</code> 里长时间阻塞（JDK 21 尤其如此）。</li>
</ol>
<hr>
<h2 id="七-小结"><a class="markdownIt-Anchor" href="#七-小结"></a> 七、小结</h2>
<p><strong>ReentrantLock</strong> 提供可重入互斥、可中断与定时获取、多 <code>Condition</code>，底层由 <strong>AQS</strong> 实现。Java 8 之后<strong>公开 API 保持稳定</strong>；<strong>Java 9–11</strong> 一代主要改进是 <strong>VarHandle 与 AQS 内部实现</strong>；<strong>Java 21+</strong> 在<strong>虚拟线程</strong>场景下，<code>ReentrantLock</code> 等 JUC 锁与 <code>synchronized</code> 的取舍成为架构关注点；<strong>JDK 24+</strong> 通过 <strong>JEP 491</strong> 缓解 <code>synchronized</code> 与虚拟线程的 pin 问题。掌握显式锁的用法与这些演进，有助于写出更安全、可维护的并发代码。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>并发</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>AQS</tag>
        <tag>并发</tag>
        <tag>ReentrantLock</tag>
        <tag>虚拟线程</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 序列化对比 —— 官方 Serializable 与常见三方库的优缺点与适用场景</title>
    <url>/2025/03/01/2025-03-02-java-serialization-compare-scenarios/</url>
    <content><![CDATA[<h1 id="java-序列化对比官方-serializable-与常见三方库的优缺点与适用场景"><a class="markdownIt-Anchor" href="#java-序列化对比官方-serializable-与常见三方库的优缺点与适用场景"></a> Java 序列化对比：官方 Serializable 与常见三方库的优缺点与适用场景</h1>
<p>本文以 <strong>Java 官方序列化</strong>（<code>java.io.ObjectOutputStream</code> / <code>ObjectInputStream</code>，即 Serializable 那套）为基准，对比常见三方方案（JSON / 高性能二进制 / 跨语言协议）的<strong>优缺点</strong>、<strong>源码层面差异</strong>，以及<strong>各自适用场景</strong>。</p>
<hr>
<h2 id="一-官方-serializable-的底层决定了它的优缺点"><a class="markdownIt-Anchor" href="#一-官方-serializable-的底层决定了它的优缺点"></a> 一、官方 Serializable 的 “底层决定了它的优缺点”</h2>
<h3 id="11-核心机制源码要点"><a class="markdownIt-Anchor" href="#11-核心机制源码要点"></a> 1.1 核心机制（源码要点）</h3>
<ul>
<li><strong>写类元数据</strong>：<code>ObjectOutputStream.writeClassDesc()</code> / <code>writeNonProxyDesc()</code> 把 <code>ObjectStreamClass</code> 的 class descriptor（类名、serialVersionUID、字段列表等）写进流。</li>
<li><strong>写对象图 + 循环引用</strong>：<code>writeObject0()</code> + <strong>HandleTable</strong>（句柄表）+ <code>ObjectStreamClass</code>，实现 “同一对象只写一次、后续写引用句柄”。</li>
<li><strong>字段写入路径</strong>：默认走 <code>ObjectStreamClass</code> 的字段描述写 primitive / object；特殊情况走 <code>writeObject</code>/<code>readObject</code>、Externalizable、<code>writeReplace</code>/<code>readResolve</code> 等钩子。</li>
<li><strong>反序列化创建对象</strong>：<code>ObjectInputStream.readObject0()</code> + <code>ObjectStreamClass.newInstance()</code>（内部用 ReflectionFactory 等 “绕过构造器” 的路径），再把字段灌进去。</li>
</ul>
<h3 id="12-由此带来的优缺点"><a class="markdownIt-Anchor" href="#12-由此带来的优缺点"></a> 1.2 由此带来的优缺点</h3>
<table>
<thead>
<tr>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody>
<tr>
<td>对象图完整，循环引用 / 共享引用天然支持</td>
<td>流里带大量类描述，体积大</td>
</tr>
<tr>
<td>对 Java 对象模型最忠实</td>
<td>反射 + 安全检查多，性能一般</td>
</tr>
<tr>
<td>一套 API 覆盖各种对象</td>
<td>强绑定 Java 类结构，跨语言差、演进麻烦</td>
</tr>
<tr>
<td></td>
<td>历史上反序列化安全风险高（gadget 链）</td>
</tr>
</tbody>
</table>
<p><strong>结论</strong>：三方库的优缺点，本质上来自这几件事的差异 ——<strong> 是否写类描述、是否用 schema / 字段 tag、是否大量反射、是否保留对象图语义、对象创建方式</strong>。</p>
<hr>
<h2 id="二-源码差异归纳五条因果链"><a class="markdownIt-Anchor" href="#二-源码差异归纳五条因果链"></a> 二、源码差异归纳：五条因果链</h2>
<h3 id="21-写不写-class-descriptorobjectstreamclass"><a class="markdownIt-Anchor" href="#21-写不写-class-descriptorobjectstreamclass"></a> 2.1 写不写 class descriptor（ObjectStreamClass）</h3>
<table>
<thead>
<tr>
<th>做法</th>
<th>代表</th>
<th>结果</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>写</strong></td>
<td>官方 Serializable</td>
<td>通用、能完整恢复对象图，但臃肿、强绑定 Java</td>
</tr>
<tr>
<td><strong>不写</strong></td>
<td>多数三方</td>
<td>更小更快，但要靠 schema / 注册 / 约定</td>
</tr>
</tbody>
</table>
<h3 id="22-字段标识方式字段名-vs-数字-tag-vs-顺序"><a class="markdownIt-Anchor" href="#22-字段标识方式字段名-vs-数字-tag-vs-顺序"></a> 2.2 字段标识方式：字段名 vs 数字 tag vs 顺序</h3>
<table>
<thead>
<tr>
<th>方式</th>
<th>代表</th>
<th>结果</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>字段名字符串</strong></td>
<td>JSON</td>
<td>可读、演进宽松，但体积大、解析慢</td>
</tr>
<tr>
<td><strong>数字 tag</strong></td>
<td>Protobuf / Avro</td>
<td>紧凑、快、演进强，但要维护 schema</td>
</tr>
<tr>
<td><strong>顺序</strong></td>
<td>部分 Kryo / 自定义</td>
<td>最快最小，但对字段增删改最敏感</td>
</tr>
</tbody>
</table>
<h3 id="23-对象图语义引用循环是否协议原生支持"><a class="markdownIt-Anchor" href="#23-对象图语义引用循环是否协议原生支持"></a> 2.3 对象图语义（引用 / 循环）是否协议原生支持</h3>
<table>
<thead>
<tr>
<th>支持方式</th>
<th>代表</th>
<th>结果</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>协议原生</strong></td>
<td>官方 Serializable（HandleTable）</td>
<td>同一对象只写一次，读回同一引用，环自然还原</td>
</tr>
<tr>
<td><strong>默认不支持</strong></td>
<td>JSON / Protobuf</td>
<td>需自己用 ID 引用等方式表达</td>
</tr>
<tr>
<td><strong>可选</strong></td>
<td>Kryo</td>
<td>可配置引用跟踪，不一定默认等价 Serializable</td>
</tr>
</tbody>
</table>
<h3 id="24-对象创建方式绕过构造器-vs-正常构造"><a class="markdownIt-Anchor" href="#24-对象创建方式绕过构造器-vs-正常构造"></a> 2.4 对象创建方式：绕过构造器 vs 正常构造</h3>
<table>
<thead>
<tr>
<th>方式</th>
<th>代表</th>
<th>结果</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>绕过构造器</strong></td>
<td>官方（ReflectionFactory 等）</td>
<td>能恢复复杂对象图，但反序列化风险高、行为复杂</td>
</tr>
<tr>
<td><strong>构造器 / 无参 + setter</strong></td>
<td>JSON 类库</td>
<td>行为更可预期，安全面更好</td>
</tr>
<tr>
<td><strong>生成代码控制</strong></td>
<td>Protobuf</td>
<td>构建过程可控，性能好</td>
</tr>
</tbody>
</table>
<h3 id="25-反射-vs-字节码生成-vs-代码生成"><a class="markdownIt-Anchor" href="#25-反射-vs-字节码生成-vs-代码生成"></a> 2.5 反射 vs 字节码生成 vs 代码生成</h3>
<table>
<thead>
<tr>
<th>手段</th>
<th>代表</th>
<th>结果</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>反射 + 复杂元数据</strong></td>
<td>官方 Serializable</td>
<td>灵活但慢、安全检查多</td>
</tr>
<tr>
<td><strong>反射 + 缓存 + 可选 bytecode</strong></td>
<td>Jackson</td>
<td>平衡可读与性能</td>
</tr>
<tr>
<td><strong>生成代码 + 紧凑二进制</strong></td>
<td>Protobuf</td>
<td>通常最快、最稳定、跨语言</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="三-各方案对比优缺点与源码差异"><a class="markdownIt-Anchor" href="#三-各方案对比优缺点与源码差异"></a> 三、各方案对比：优缺点与源码差异</h2>
<h3 id="31-json-类jackson-fastjson2-gson"><a class="markdownIt-Anchor" href="#31-json-类jackson-fastjson2-gson"></a> 3.1 JSON 类：Jackson / Fastjson2 / Gson</h3>
<p><strong>与官方的核心差异</strong>：不走 <code>ObjectStreamClass</code> / <code>writeClassDesc</code> 的二进制协议，而是：</p>
<ul>
<li>字符/UTF-8 的 token 流（字段名、数值、数组/对象边界）</li>
<li>映射层把 “字段名/注解/策略” 绑定到 POJO</li>
</ul>
<p><strong>Jackson 典型源码点</strong>：</p>
<ul>
<li>写：<code>ObjectMapper#writeValue</code> → <code>JsonFactory</code> → <code>JsonGenerator</code>，<code>BeanSerializer</code> / <code>BeanPropertyWriter</code> 遍历属性输出 JSON</li>
<li>读：<code>ObjectMapper#readValue</code> → <code>JsonParser</code> 读 token，<code>BeanDeserializer</code> / <code>SettableBeanProperty</code> 把值 set 进对象</li>
</ul>
<p><strong>Fastjson2</strong>：入口类似 <code>JSON#toJSONString</code> / <code>parseObject</code>，更多快速路径、按类型走专用 reader / writer，性能更激进。</p>
<p><strong>Gson</strong>：<code>TypeAdapter</code> + 反射读写字段，结构简单，配置与优化路径相对少。</p>
<table>
<thead>
<tr>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody>
<tr>
<td>跨语言（JSON 通用）</td>
<td>不天然保留对象图（循环 / 共享引用默认丢，需额外机制如 Jackson object identity）</td>
</tr>
<tr>
<td>可读、可调试</td>
<td>类型信息缺失，多态 / 接口需显式 type info</td>
</tr>
<tr>
<td>schema 变更相对宽松（新增字段对旧端友好）</td>
<td>性能 / 体积一般不如高效二进制（字段名重复、文本解析）</td>
</tr>
<tr>
<td>安全面通常好于 Serializable（不会自动走 gadget 链那套）</td>
<td>Fastjson 历史安全包袱需谨慎配置</td>
</tr>
</tbody>
</table>
<hr>
<h3 id="32-高性能-java-二进制kryo-protostuff"><a class="markdownIt-Anchor" href="#32-高性能-java-二进制kryo-protostuff"></a> 3.2 高性能 Java 二进制：Kryo / Protostuff</h3>
<p><strong>与官方的核心差异</strong>：一般不把 “完整 class descriptor” 写在每个对象旁，而是 <strong>type id + 字段序列化</strong> 的紧凑模型。</p>
<p><strong>Kryo 典型源码点</strong>：</p>
<ul>
<li>入口：<code>Kryo#writeObject</code> / <code>readObject</code></li>
<li><code>Serializer</code> 体系（如 <code>FieldSerializer</code>），<code>Output</code>/<code>Input</code> 做紧凑二进制（varint、无字段名）</li>
<li>引用跟踪是<strong>可配置特性</strong>，不是协议必带</li>
</ul>
<table>
<thead>
<tr>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody>
<tr>
<td>体积小、速度快（无大段 writeClassDesc）</td>
<td>跨语言差（Java 内部协议）</td>
</tr>
<tr>
<td>注册类 → 小整数 id，进一步缩小体积</td>
<td>类演进敏感，字段顺序 / 增删改需控制 serializer 或兼容策略</td>
</tr>
<tr>
<td>可选引用跟踪</td>
<td>对象图语义依赖配置，不一定默认等价 Serializable</td>
</tr>
</tbody>
</table>
<p><strong>Protostuff</strong>：思路类似 “Protobuf 编码 + POJO 友好”，无类描述字符串、字段用 tag、二进制紧凑，在性能/紧凑与演进/跨语言之间取平衡。</p>
<hr>
<h3 id="33-跨语言协议protobuf-avro-messagepack"><a class="markdownIt-Anchor" href="#33-跨语言协议protobuf-avro-messagepack"></a> 3.3 跨语言协议：Protobuf / Avro / MessagePack</h3>
<p><strong>共同点</strong>：不写 Java 类描述（<code>ObjectStreamClass</code>），用 <strong>schema + 字段 tag / 编号</strong> 编码，生成代码或按 schema 驱动解析，减少反射。</p>
<p><strong>Protobuf</strong>：</p>
<ul>
<li>Java 侧：<code>CodedOutputStream</code> / <code>CodedInputStream</code> 做 varint、tag 编码，字段用<strong>数字 tag</strong> 而非字段名</li>
<li>大量场景用<strong>代码生成</strong>的 <code>MessageLite</code> 子类读写</li>
</ul>
<table>
<thead>
<tr>
<th>优点</th>
<th>缺点</th>
</tr>
</thead>
<tbody>
<tr>
<td>超紧凑、快、跨语言</td>
<td>需维护 .proto（或等价 schema）</td>
</tr>
<tr>
<td>schema 演进能力强（tag 不变，新增字段对旧端安全）</td>
<td>对复杂 “任意对象图” 不友好，引用 / 循环需自己设计 ID</td>
</tr>
</tbody>
</table>
<p><strong>Avro</strong>：schema 驱动（常为 JSON schema），reader/writer schema 可不同，靠 schema resolution 做兼容，适合数据管道/Kafka；对象图语义同样不是目标。</p>
<p><strong>MessagePack</strong>：二进制 JSON，比 JSON 更紧凑、解析更快，跨语言；若不用 schema，类型与演进需自行约定，对象图也不天然保留。</p>
<hr>
<h2 id="四-适用场景按框架"><a class="markdownIt-Anchor" href="#四-适用场景按框架"></a> 四、适用场景（按框架）</h2>
<h3 id="41-官方-serializable"><a class="markdownIt-Anchor" href="#41-官方-serializable"></a> 4.1 官方 Serializable</h3>
<table>
<thead>
<tr>
<th>适用</th>
<th>不适用</th>
</tr>
</thead>
<tbody>
<tr>
<td>纯 Java 内部短期持久化（如 HttpSession、本地临时缓存）</td>
<td>微服务、跨语言、高性能系统</td>
</tr>
<tr>
<td>Java RMI（历史项目）</td>
<td>公网输入反序列化（安全风险）</td>
</tr>
<tr>
<td>原型 / Demo</td>
<td></td>
</tr>
</tbody>
</table>
<p><strong>原因</strong>：写完整类描述、强绑 serialVersionUID、HandleTable 维护对象图、反序列化可绕过构造器 → 适合 “完整恢复 Java 对象图”，不适合高性能与跨语言。</p>
<hr>
<h3 id="42-jackson企业-rest-标准"><a class="markdownIt-Anchor" href="#42-jackson企业-rest-标准"></a> 4.2 Jackson（企业 REST 标准）</h3>
<table>
<thead>
<tr>
<th>适用</th>
<th>不适用</th>
</tr>
</thead>
<tbody>
<tr>
<td>REST API（Spring Boot 默认）</td>
<td>超高 QPS 内部 RPC</td>
</tr>
<tr>
<td>配置文件（YAML、JSON config）</td>
<td>需要极致压缩</td>
</tr>
<tr>
<td>ES / OpenSearch 文档映射</td>
<td></td>
</tr>
<tr>
<td>中等规模缓存（如 Redis JSON）</td>
<td></td>
</tr>
</tbody>
</table>
<p><strong>原因</strong>：基于字段名、JsonGenerator / JsonParser、反射 + 缓存 BeanSerializer → 可读、跨语言、兼容性好，但字段名重复、体积较大。</p>
<hr>
<h3 id="43-fastjson2"><a class="markdownIt-Anchor" href="#43-fastjson2"></a> 4.3 Fastjson2</h3>
<table>
<thead>
<tr>
<th>适用</th>
<th>不适用</th>
</tr>
</thead>
<tbody>
<tr>
<td>高 QPS JSON 服务</td>
<td>安全要求极高的公网反序列化（需谨慎配置）</td>
</tr>
<tr>
<td>内部系统接口、Redis JSON 序列化</td>
<td></td>
</tr>
</tbody>
</table>
<p><strong>原因</strong>：高性能 Reader / Writer、优化路径多，比 Jackson 快，但生态标准仍是 Jackson，安全历史需注意。</p>
<hr>
<h3 id="44-kryo高性能-java-内部二进制"><a class="markdownIt-Anchor" href="#44-kryo高性能-java-内部二进制"></a> 4.4 Kryo（高性能 Java 内部二进制）</h3>
<table>
<thead>
<tr>
<th>适用</th>
<th>不适用</th>
</tr>
</thead>
<tbody>
<tr>
<td>Redis 高性能缓存（对象 → byte []）</td>
<td>跨语言</td>
</tr>
<tr>
<td>Spark / Flink 等分布式计算对象传输</td>
<td>长期存储（类结构变动风险大）</td>
</tr>
<tr>
<td>内部 Java RPC、本地内存持久化</td>
<td></td>
</tr>
</tbody>
</table>
<p><strong>原因</strong>：不写 class descriptor、可注册类→小整数 ID、Output / Input 紧凑、可选引用跟踪 → 小、快，但强依赖类定义。</p>
<hr>
<h3 id="45-protobuf微服务-rpc-首选"><a class="markdownIt-Anchor" href="#45-protobuf微服务-rpc-首选"></a> 4.5 Protobuf（微服务 / RPC 首选）</h3>
<table>
<thead>
<tr>
<th>适用</th>
<th>不适用</th>
</tr>
</thead>
<tbody>
<tr>
<td>微服务 RPC（gRPC 标准）</td>
<td>直接存复杂对象图</td>
</tr>
<tr>
<td>跨语言系统（Java / Go / Python / C++）</td>
<td>无 schema 管理能力的团队</td>
</tr>
<tr>
<td>高性能网关、移动端 API</td>
<td></td>
</tr>
</tbody>
</table>
<p><strong>原因</strong>：tag 编码、CodedOutputStream、生成代码、不存字段名 → 小、快、强 schema 版本兼容。</p>
<hr>
<h3 id="46-avro数据管道-kafka"><a class="markdownIt-Anchor" href="#46-avro数据管道-kafka"></a> 4.6 Avro（数据管道 / Kafka）</h3>
<table>
<thead>
<tr>
<th>适用</th>
<th>不适用</th>
</tr>
</thead>
<tbody>
<tr>
<td>Kafka 消息、大数据平台</td>
<td>复杂对象模型</td>
</tr>
<tr>
<td>需要 Schema 演进的场景</td>
<td>实时高并发 RPC（通常不如 Protobuf）</td>
</tr>
</tbody>
</table>
<p><strong>原因</strong>：reader / writer schema 机制专门解决版本演进 → 数据平台常用。</p>
<hr>
<h3 id="47-messagepack"><a class="markdownIt-Anchor" href="#47-messagepack"></a> 4.7 MessagePack</h3>
<table>
<thead>
<tr>
<th>适用</th>
<th>不适用</th>
</tr>
</thead>
<tbody>
<tr>
<td>替代 JSON、WebSocket 二进制传输</td>
<td>强 schema 系统</td>
</tr>
<tr>
<td>游戏服务器等</td>
<td></td>
</tr>
</tbody>
</table>
<p><strong>原因</strong>：JSON 二进制化、无字段名重复、无强 schema → 比 JSON 小、快、跨语言。</p>
<hr>
<h2 id="五-工程选型速查"><a class="markdownIt-Anchor" href="#五-工程选型速查"></a> 五、工程选型速查</h2>
<table>
<thead>
<tr>
<th>场景</th>
<th>推荐</th>
</tr>
</thead>
<tbody>
<tr>
<td>Spring Boot REST</td>
<td>Jackson</td>
</tr>
<tr>
<td>Redis 缓存</td>
<td>Kryo / JSON</td>
</tr>
<tr>
<td>内部 Java RPC</td>
<td>Kryo</td>
</tr>
<tr>
<td>微服务跨语言</td>
<td>Protobuf</td>
</tr>
<tr>
<td>Kafka</td>
<td>Avro</td>
</tr>
<tr>
<td>WebSocket 二进制</td>
<td>MessagePack</td>
</tr>
<tr>
<td>本地 Java 持久化</td>
<td>Kryo</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="六-一句话总结"><a class="markdownIt-Anchor" href="#六-一句话总结"></a> 六、一句话总结</h2>
<table>
<thead>
<tr>
<th>方案</th>
<th>定位</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Serializable</strong></td>
<td>恢复 Java 对象图</td>
</tr>
<tr>
<td><strong>JSON（Jackson/Fastjson/Gson）</strong></td>
<td>交换数据、可读、跨语言</td>
</tr>
<tr>
<td><strong>Kryo</strong></td>
<td>快速 Java 内部二进制</td>
</tr>
<tr>
<td><strong>Protobuf</strong></td>
<td>跨语言高性能协议</td>
</tr>
<tr>
<td><strong>Avro</strong></td>
<td>数据平台 schema 管理</td>
</tr>
<tr>
<td><strong>MessagePack</strong></td>
<td>二进制 JSON</td>
</tr>
</tbody>
</table>
<p><strong>源码层面的本质区别</strong>：是否写 class descriptor、字段用名字还是 tag 还是顺序、是否原生支持对象图、对象创建方式（反射/构造/生成代码），共同决定了体积、性能、演进能力和适用场景。选型时按 “是否跨语言、是否要对象图、是否要极致性能、是否有 schema 管理” 对号入座即可。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>架构</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>序列化</tag>
        <tag>Jackson</tag>
        <tag>Kryo</tag>
        <tag>Protobuf</tag>
        <tag>选型</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 标准库 Serializable 整体实现流程（源码与示例）</title>
    <url>/2025/03/01/2025-03-02-java-serializable-implementation/</url>
    <content><![CDATA[<h1 id="java-标准库-serializable-整体实现流程源码与示例"><a class="markdownIt-Anchor" href="#java-标准库-serializable-整体实现流程源码与示例"></a> Java 标准库 Serializable 整体实现流程（源码与示例）</h1>
<p>本文用 JDK 标准库（<code>java.io</code>）里的关键类 / 方法名 + 伪源码结构（接近 OpenJDK 源码组织方式），配合可运行示例，把 <strong>Serializable 从对象写出到读回</strong> 的整体实现流程讲清楚。</p>
<hr>
<h2 id="一-serializable-是标记接口真正干活的是-oosois"><a class="markdownIt-Anchor" href="#一-serializable-是标记接口真正干活的是-oosois"></a> 一、Serializable 是 “标记接口”，真正干活的是 OOS / OIS</h2>
<p><code>Serializable</code> 本身几乎没有代码：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> java.io;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Serializable</span> { }</span><br></pre></td></tr></tbody></table></figure>
<p>它的意义：告诉序列化框架 ——<strong> 这个类允许被 Java 默认序列化机制处理</strong>。</p>
<p>真正的入口在：</p>
<ul>
<li><strong>写出</strong>：<code>java.io.ObjectOutputStream</code></li>
<li><strong>读回</strong>：<code>java.io.ObjectInputStream</code></li>
</ul>
<hr>
<h2 id="二-写出流程objectoutputstream从-writeobject-到字节流"><a class="markdownIt-Anchor" href="#二-写出流程objectoutputstream从-writeobject-到字节流"></a> 二、写出流程（ObjectOutputStream）：从 writeObject 到字节流</h2>
<h3 id="21-入口writeobjectobject-obj"><a class="markdownIt-Anchor" href="#21-入口writeobjectobject-obj"></a> 2.1 入口：writeObject (Object obj)</h3>
<p>典型调用：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> (<span class="type">ObjectOutputStream</span> <span class="variable">oos</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectOutputStream</span>(out)) {</span><br><span class="line">    oos.writeObject(obj);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>核心流程（接近源码结构的伪源码）：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">writeObject</span><span class="params">(Object obj)</span> <span class="keyword">throws</span> IOException {</span><br><span class="line">    writeObject0(obj, <span class="literal">false</span>);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">writeObject0</span><span class="params">(Object obj, <span class="type">boolean</span> unshared)</span> {</span><br><span class="line">    <span class="keyword">if</span> (obj == <span class="literal">null</span>) { writeNull(); <span class="keyword">return</span>; }</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1) 处理循环引用/重复引用：句柄表（HandleTable）</span></span><br><span class="line">    <span class="keyword">if</span> (handles.contains(obj)) { writeHandle(handles.lookup(obj)); <span class="keyword">return</span>; }</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2) 写类描述（class descriptor）</span></span><br><span class="line">    <span class="type">ObjectStreamClass</span> <span class="variable">desc</span> <span class="operator">=</span> ObjectStreamClass.lookup(obj.getClass());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3) 根据对象类型分派</span></span><br><span class="line">    <span class="keyword">if</span> (obj <span class="keyword">instanceof</span> String) writeString((String)obj);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (obj.getClass().isArray()) writeArray(obj);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (obj <span class="keyword">instanceof</span> Enum) writeEnum((Enum)obj);</span><br><span class="line">    <span class="keyword">else</span> writeOrdinaryObject(obj, desc);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="22-关键objectstreamclass类的序列化描述"><a class="markdownIt-Anchor" href="#22-关键objectstreamclass类的序列化描述"></a> 2.2 关键：ObjectStreamClass（类的 “序列化描述”）</h3>
<p><code>ObjectStreamClass</code> 负责：</p>
<ul>
<li>判断类是否 <code>Serializable</code></li>
<li>计算 / 读取 <code>serialVersionUID</code></li>
<li>收集可序列化字段（排除 <code>static</code>、排除 <code>transient</code>）</li>
<li>解析是否存在私有钩子方法：<code>writeObject</code> / <code>readObject</code> / <code>readResolve</code> / <code>writeReplace</code></li>
</ul>
<p>伪源码：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> ObjectStreamClass <span class="title function_">lookup</span><span class="params">(Class&lt;?&gt; cl)</span> {</span><br><span class="line">    <span class="comment">// cache</span></span><br><span class="line">    <span class="comment">// if implements Serializable =&gt; build descriptor:</span></span><br><span class="line">    <span class="comment">//   - serialVersionUID</span></span><br><span class="line">    <span class="comment">//   - fields (ObjectStreamField[])</span></span><br><span class="line">    <span class="comment">//   - hasWriteObjectMethod?</span></span><br><span class="line">    <span class="comment">//   - hasReadObjectMethod?</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="23-写普通对象writeordinaryobject"><a class="markdownIt-Anchor" href="#23-写普通对象writeordinaryobject"></a> 2.3 写普通对象：writeOrdinaryObject</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">writeOrdinaryObject</span><span class="params">(Object obj, ObjectStreamClass desc)</span> {</span><br><span class="line">    writeClassDesc(desc);</span><br><span class="line">    handles.assign(obj);           <span class="comment">// 先登记，解决循环引用</span></span><br><span class="line">    desc.writeObject(obj, <span class="built_in">this</span>);   <span class="comment">// 默认字段 or 自定义 writeObject</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="24-两条路线默认字段序列化-vs-自定义-writeobject"><a class="markdownIt-Anchor" href="#24-两条路线默认字段序列化-vs-自定义-writeobject"></a> 2.4 两条路线：默认字段序列化 vs 自定义 writeObject</h3>
<ul>
<li><strong>默认序列化</strong>（没有定义 <code>private void writeObject</code>）：<code>defaultWriteFields(obj, desc)</code>，按收集到的字段顺序写出。</li>
<li><strong>自定义序列化</strong>（类里定义了 <code>private void writeObject(ObjectOutputStream out)</code>）：JDK 用反射调用你的私有方法，你通常会先 <code>out.defaultWriteObject()</code>，再写自定义数据。</li>
</ul>
<hr>
<h2 id="三-读回流程objectinputstream从-readobject-到对象还原"><a class="markdownIt-Anchor" href="#三-读回流程objectinputstream从-readobject-到对象还原"></a> 三、读回流程（ObjectInputStream）：从 readObject 到对象还原</h2>
<h3 id="31-入口readobject"><a class="markdownIt-Anchor" href="#31-入口readobject"></a> 3.1 入口：readObject ()</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> (<span class="type">ObjectInputStream</span> <span class="variable">ois</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectInputStream</span>(in)) {</span><br><span class="line">    <span class="type">Object</span> <span class="variable">obj</span> <span class="operator">=</span> ois.readObject();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>伪源码结构：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> Object <span class="title function_">readObject</span><span class="params">()</span> {</span><br><span class="line">    <span class="keyword">return</span> readObject0(<span class="literal">false</span>);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> Object <span class="title function_">readObject0</span><span class="params">(<span class="type">boolean</span> unshared)</span> {</span><br><span class="line">    <span class="type">byte</span> <span class="variable">tc</span> <span class="operator">=</span> readTC();</span><br><span class="line">    <span class="keyword">switch</span> (tc) {</span><br><span class="line">        <span class="keyword">case</span> TC_NULL: <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">case</span> TC_REFERENCE: <span class="keyword">return</span> handles.lookup(readHandle());</span><br><span class="line">        <span class="keyword">case</span> TC_STRING: <span class="keyword">return</span> readString();</span><br><span class="line">        <span class="keyword">case</span> TC_ARRAY: <span class="keyword">return</span> readArray();</span><br><span class="line">        <span class="keyword">case</span> TC_ENUM: <span class="keyword">return</span> readEnum();</span><br><span class="line">        <span class="keyword">case</span> TC_OBJECT: <span class="keyword">return</span> readOrdinaryObject(unshared);</span><br><span class="line">        ...</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="32-读普通对象readordinaryobject"><a class="markdownIt-Anchor" href="#32-读普通对象readordinaryobject"></a> 3.2 读普通对象：readOrdinaryObject</h3>
<p>关键步骤：读 classDesc → 创建实例（通常不调用你的构造方法）→ 先塞进 handle 表 → 读字段 → 若有 <code>readObject</code> 则回调 → 若有 <code>readResolve</code> 则替换返回对象。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Object <span class="title function_">readOrdinaryObject</span><span class="params">(<span class="type">boolean</span> unshared)</span> {</span><br><span class="line">    <span class="type">ObjectStreamClass</span> <span class="variable">desc</span> <span class="operator">=</span> readClassDesc();</span><br><span class="line">    <span class="type">Object</span> <span class="variable">obj</span> <span class="operator">=</span> desc.newInstance();</span><br><span class="line">    handles.assign(obj);</span><br><span class="line">    desc.readObject(obj, <span class="built_in">this</span>);</span><br><span class="line">    obj = desc.invokeReadResolve(obj);</span><br><span class="line">    <span class="keyword">return</span> obj;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="33-serialversionuid-校验"><a class="markdownIt-Anchor" href="#33-serialversionuid-校验"></a> 3.3 serialVersionUID 校验</h3>
<p>在读 classDesc、把流中的描述映射到本地类时，会比较流里写入的 <code>serialVersionUID</code> 与本地类的 <code>serialVersionUID</code>，不一致就抛 <code>InvalidClassException</code>。</p>
<hr>
<h2 id="四-句柄表handletable循环引用与重复引用"><a class="markdownIt-Anchor" href="#四-句柄表handletable循环引用与重复引用"></a> 四、句柄表（HandleTable）：循环引用与重复引用</h2>
<h3 id="41-为什么需要句柄"><a class="markdownIt-Anchor" href="#41-为什么需要句柄"></a> 4.1 为什么需要句柄？</h3>
<ul>
<li><strong>重复引用</strong>要保持同一性：<code>a.x == a.y</code> 指向同一对象时，反序列化后也必须 <code>==</code>。</li>
<li><strong>对象图可能有环</strong>：若每次遇到对象都 “深拷贝写一遍”，有环会无限递归、栈溢出。</li>
</ul>
<p>所以需要 “已见对象登记表”<strong>HandleTable</strong>：给每个<strong>第一次出现</strong>的对象分配递增的整数 id（handle），之后再遇到同一对象就写 <strong>TC_REFERENCE + handle</strong>。</p>
<h3 id="42-handletable-数据结构源码风格"><a class="markdownIt-Anchor" href="#42-handletable-数据结构源码风格"></a> 4.2 HandleTable 数据结构（源码风格）</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">HandleTable</span> {</span><br><span class="line">    <span class="type">int</span>[] spine;       <span class="comment">// 哈希桶头结点</span></span><br><span class="line">    <span class="type">int</span>[] next;        <span class="comment">// 链表 next</span></span><br><span class="line">    Object[] objs;     <span class="comment">// 对象引用</span></span><br><span class="line">    <span class="type">int</span> size;          <span class="comment">// 已分配句柄数</span></span><br><span class="line">    <span class="type">int</span> threshold;</span><br><span class="line">    <span class="type">float</span> loadFactor;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> <span class="title function_">assign</span><span class="params">(Object obj)</span> { ... }   <span class="comment">// 登记并返回 handle</span></span><br><span class="line">    <span class="type">int</span> <span class="title function_">lookup</span><span class="params">(Object obj)</span> { ... }   <span class="comment">// 查 handle，找不到返回 -1</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>按<strong>对象 identity</strong> 追踪：用 <code>System.identityHashCode(obj)</code> 做 hash，用 <code>==</code> 做相等判断（不是 <code>equals()</code>）。</li>
<li>哈希桶 + 链式冲突：<code>spine[bucket]</code> 存该桶链表第一个 entry 的下标，<code>next[i]</code> 连接链表。</li>
</ul>
<h3 id="43-lookup判断这个对象以前写过没"><a class="markdownIt-Anchor" href="#43-lookup判断这个对象以前写过没"></a> 4.3 lookup：判断 “这个对象以前写过没？”</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">lookup</span><span class="params">(Object obj)</span> {</span><br><span class="line">    <span class="type">int</span> <span class="variable">h</span> <span class="operator">=</span> System.identityHashCode(obj);</span><br><span class="line">    <span class="type">int</span> <span class="variable">bucket</span> <span class="operator">=</span> (h &amp; <span class="number">0x7fffffff</span>) % spine.length;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> spine[bucket]; i &gt;= <span class="number">0</span>; i = next[i]) {</span><br><span class="line">        <span class="keyword">if</span> (objs[i] == obj)   <span class="comment">// identity 比较</span></span><br><span class="line">            <span class="keyword">return</span> i;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="44-assign第一次见到对象时登记"><a class="markdownIt-Anchor" href="#44-assign第一次见到对象时登记"></a> 4.4 assign：第一次见到对象时登记</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">assign</span><span class="params">(Object obj)</span> {</span><br><span class="line">    <span class="keyword">if</span> (size &gt;= next.length) growEntries();</span><br><span class="line">    <span class="keyword">if</span> (size &gt;= threshold) growSpine();</span><br><span class="line">    <span class="type">int</span> <span class="variable">h</span> <span class="operator">=</span> System.identityHashCode(obj);</span><br><span class="line">    <span class="type">int</span> <span class="variable">bucket</span> <span class="operator">=</span> (h &amp; <span class="number">0x7fffffff</span>) % spine.length;</span><br><span class="line">    objs[size] = obj;</span><br><span class="line">    next[size] = spine[bucket];</span><br><span class="line">    spine[bucket] = size;</span><br><span class="line">    <span class="keyword">return</span> size++;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="45-写入时何时写-tc_reference何时-assign"><a class="markdownIt-Anchor" href="#45-写入时何时写-tc_reference何时-assign"></a> 4.5 写入时：何时写 TC_REFERENCE？何时 assign？</h3>
<ul>
<li><strong>重复引用</strong>：<code>lookup(obj) != -1</code> 时，写 <code>TC_REFERENCE + (baseWireHandle + handle)</code>。</li>
<li><strong>写对象本体前必须先 assign</strong>：这样在写字段时若再次遇到自己（如 <code>n.next = n</code>），<code>lookup</code> 会命中，直接写引用，不会无限递归。</li>
</ul>
<h3 id="46-identityhashcode-会冲突吗"><a class="markdownIt-Anchor" href="#46-identityhashcode-会冲突吗"></a> 4.6 identityHashCode 会冲突吗？</h3>
<p>会。<code>System.identityHashCode(obj)</code> 是 32 位 int，不同对象可能得到相同值。HandleTable <strong>不依赖 hash 唯一性</strong>：用 hash 选桶，用 <strong><code>==</code></strong> 做最终确认；冲突只会让同一桶内链表变长，不会误判。</p>
<hr>
<h2 id="五-descwriteobjectobj-this-源码与输出内容"><a class="markdownIt-Anchor" href="#五-descwriteobjectobj-this-源码与输出内容"></a> 五、desc.writeObject(obj, this) 源码与输出内容</h2>
<p>在 <code>ObjectStreamClass</code> 里：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> <span class="title function_">writeObject</span><span class="params">(Object obj, ObjectOutputStream out)</span> <span class="keyword">throws</span> IOException {</span><br><span class="line">    <span class="keyword">if</span> (writeObjectMethod != <span class="literal">null</span>) {</span><br><span class="line">        invokeWriteObject(obj, out);   <span class="comment">// 反射调用 private writeObject</span></span><br><span class="line">    } <span class="keyword">else</span> {</span><br><span class="line">        defaultWriteFields(obj, out);   <span class="comment">// 按字段类型逐个写出</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li><strong>defaultWriteFields</strong>：遍历 <code>ObjectStreamField[]</code>，用反射取字段值，按类型调用 <code>out.writeInt()</code> / <code>out.writeLong()</code> / <code>out.writeObject()</code> 等。</li>
<li><strong>invokeWriteObject</strong>：<code>writeObjectMethod.invoke(obj, new Object[]{out})</code>，你在方法里可以 <code>out.defaultWriteObject()</code> 再写额外数据。</li>
</ul>
<h3 id="简单示例默认序列化输出什么"><a class="markdownIt-Anchor" href="#简单示例默认序列化输出什么"></a> 简单示例：默认序列化输出什么？</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SimpleDemo</span> {</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Person</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> {</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>;</span><br><span class="line">        <span class="type">int</span> age;</span><br><span class="line">        String name;</span><br><span class="line">        Person(<span class="type">int</span> age, String name) { <span class="built_in">this</span>.age = age; <span class="built_in">this</span>.name = name; }</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception {</span><br><span class="line">        <span class="type">Person</span> <span class="variable">p</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="number">18</span>, <span class="string">"Alice"</span>);</span><br><span class="line">        <span class="type">ByteArrayOutputStream</span> <span class="variable">bos</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ByteArrayOutputStream</span>();</span><br><span class="line">        <span class="type">ObjectOutputStream</span> <span class="variable">oos</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectOutputStream</span>(bos);</span><br><span class="line">        oos.writeObject(p);</span><br><span class="line">        oos.close();</span><br><span class="line">        <span class="type">byte</span>[] data = bos.toByteArray();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">byte</span> b : data) System.out.printf(<span class="string">"%02X "</span>, b);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>典型输出（节选）：</p>
<ul>
<li><code>AC ED 00 05</code>：STREAM_MAGIC + 版本</li>
<li><code>73</code>：TC_OBJECT</li>
<li><code>72</code>：TC_CLASSDESC，后跟类名、serialVersionUID、flags、字段元数据（如 <code>I age</code>、<code>L name</code>）、TC_ENDBLOCKDATA、父类描述</li>
<li>接着是字段数据：<code>00 00 00 12</code>（age = 18），以及表示 “Alice” 的 TC_STRING 等</li>
</ul>
<p>若有自定义 <code>writeObject</code> 里多写了 <code>out.writeInt(999)</code>，流里会多出 <code>00 00 03 E7</code>。</p>
<hr>
<h2 id="六-完整示例默认字段-transient-自定义-writeobjectreadobject"><a class="markdownIt-Anchor" href="#六-完整示例默认字段-transient-自定义-writeobjectreadobject"></a> 六、完整示例：默认字段 + transient + 自定义 writeObject / readObject</h2>
<p>下面可直接运行，体现 “默认字段块 + 额外数据 + transient 手动处理”：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.*;</span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SerializableDemo</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">User</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> {</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">private</span> String name;</span><br><span class="line">        <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">transient</span> String password;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">transient</span> <span class="type">int</span> checksum;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="title function_">User</span><span class="params">(String name, <span class="type">int</span> age, String password)</span> {</span><br><span class="line">            <span class="built_in">this</span>.name = name;</span><br><span class="line">            <span class="built_in">this</span>.age = age;</span><br><span class="line">            <span class="built_in">this</span>.password = password;</span><br><span class="line">            <span class="built_in">this</span>.checksum = calcChecksum();</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">        <span class="keyword">private</span> <span class="type">int</span> <span class="title function_">calcChecksum</span><span class="params">()</span> {</span><br><span class="line">            <span class="keyword">return</span> (name == <span class="literal">null</span> ? <span class="number">0</span> : name.hashCode()) ^ age;</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">writeObject</span><span class="params">(ObjectOutputStream out)</span> <span class="keyword">throws</span> IOException {</span><br><span class="line">            out.defaultWriteObject();</span><br><span class="line">            out.writeObject(password);</span><br><span class="line">            out.writeInt(calcChecksum());</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">readObject</span><span class="params">(ObjectInputStream in)</span> <span class="keyword">throws</span> IOException, ClassNotFoundException {</span><br><span class="line">            in.defaultReadObject();</span><br><span class="line">            <span class="built_in">this</span>.password = (String) in.readObject();</span><br><span class="line">            <span class="type">int</span> <span class="variable">cs</span> <span class="operator">=</span> in.readInt();</span><br><span class="line">            <span class="built_in">this</span>.checksum = cs;</span><br><span class="line">            <span class="keyword">if</span> (cs != calcChecksum()) {</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InvalidObjectException</span>(<span class="string">"checksum mismatch!"</span>);</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> String <span class="title function_">toString</span><span class="params">()</span> {</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"User{name='"</span> + name + <span class="string">"', age="</span> + age + <span class="string">", password='"</span> + password + <span class="string">"', checksum="</span> + checksum + <span class="string">"}"</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception {</span><br><span class="line">        <span class="type">User</span> <span class="variable">u1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="string">"Alice"</span>, <span class="number">20</span>, <span class="string">"p@ss"</span>);</span><br><span class="line">        <span class="type">byte</span>[] data;</span><br><span class="line">        <span class="keyword">try</span> (<span class="type">ByteArrayOutputStream</span> <span class="variable">bos</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ByteArrayOutputStream</span>();</span><br><span class="line">             <span class="type">ObjectOutputStream</span> <span class="variable">oos</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectOutputStream</span>(bos)) {</span><br><span class="line">            oos.writeObject(u1);</span><br><span class="line">            oos.flush();</span><br><span class="line">            data = bos.toByteArray();</span><br><span class="line">        }</span><br><span class="line">        System.out.println(<span class="string">"serialized bytes len="</span> + data.length);</span><br><span class="line"></span><br><span class="line">        User u2;</span><br><span class="line">        <span class="keyword">try</span> (<span class="type">ObjectInputStream</span> <span class="variable">ois</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectInputStream</span>(<span class="keyword">new</span> <span class="title class_">ByteArrayInputStream</span>(data))) {</span><br><span class="line">            u2 = (User) ois.readObject();</span><br><span class="line">        }</span><br><span class="line">        System.out.println(<span class="string">"u1="</span> + u1);</span><br><span class="line">        System.out.println(<span class="string">"u2="</span> + u2);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>可看到：transient 的 <code>password</code> 通过自定义 write / read 补回；校验失败在 <code>readObject</code> 里抛 <code>InvalidObjectException</code>。</p>
<hr>
<h2 id="七-验证句柄表重复引用与循环引用"><a class="markdownIt-Anchor" href="#七-验证句柄表重复引用与循环引用"></a> 七、验证句柄表：重复引用与循环引用</h2>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HandleTableProof</span> {</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Node</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> {</span><br><span class="line">        String name;</span><br><span class="line">        Node next;</span><br><span class="line">        Node(String name) { <span class="built_in">this</span>.name = name; }</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception {</span><br><span class="line">        <span class="type">Node</span> <span class="variable">a</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Node</span>(<span class="string">"A"</span>);</span><br><span class="line">        <span class="type">Node</span> <span class="variable">b</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Node</span>(<span class="string">"B"</span>);</span><br><span class="line">        a.next = b;</span><br><span class="line">        b.next = a;   <span class="comment">// A &lt;-&gt; B 形成环</span></span><br><span class="line"></span><br><span class="line">        <span class="type">Node</span> <span class="variable">root</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Node</span>(<span class="string">"ROOT"</span>);</span><br><span class="line">        root.next = a;</span><br><span class="line"></span><br><span class="line">        <span class="type">byte</span>[] data;</span><br><span class="line">        <span class="keyword">try</span> (<span class="type">ByteArrayOutputStream</span> <span class="variable">bos</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ByteArrayOutputStream</span>();</span><br><span class="line">             <span class="type">ObjectOutputStream</span> <span class="variable">oos</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectOutputStream</span>(bos)) {</span><br><span class="line">            oos.writeObject(root);</span><br><span class="line">            data = bos.toByteArray();</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">        Node root2;</span><br><span class="line">        <span class="keyword">try</span> (<span class="type">ObjectInputStream</span> <span class="variable">ois</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectInputStream</span>(<span class="keyword">new</span> <span class="title class_">ByteArrayInputStream</span>(data))) {</span><br><span class="line">            root2 = (Node) ois.readObject();</span><br><span class="line">        }</span><br><span class="line">        <span class="type">Node</span> <span class="variable">a2</span> <span class="operator">=</span> root2.next;</span><br><span class="line">        <span class="type">Node</span> <span class="variable">b2</span> <span class="operator">=</span> a2.next;</span><br><span class="line">        System.out.println(<span class="string">"b2.next == a2 ? "</span> + (b2.next == a2));  <span class="comment">// true，环被还原</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>没有 “先 assign 再写 / 读字段” 的策略，写时会无限递归，或读回来环断掉（<code>b2.next != a2</code>）。</p>
<hr>
<h2 id="八-四个钩子点serializable-的威力"><a class="markdownIt-Anchor" href="#八-四个钩子点serializable-的威力"></a> 八、四个钩子点（Serializable 的威力）</h2>
<table>
<thead>
<tr>
<th>钩子</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>private void writeObject(ObjectOutputStream out)</code></td>
<td>自定义写出流程</td>
</tr>
<tr>
<td><code>private void readObject(ObjectInputStream in)</code></td>
<td>自定义读回流程</td>
</tr>
<tr>
<td><code>private Object writeReplace()</code></td>
<td>写出前用另一对象替代（代理、单例、版本迁移）</td>
</tr>
<tr>
<td><code>private Object readResolve()</code></td>
<td>读回后用另一对象替代（单例常用）</td>
</tr>
</tbody>
</table>
<p>单例防反序列化破坏示例：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Object <span class="title function_">readResolve</span><span class="params">()</span> {</span><br><span class="line">    <span class="keyword">return</span> INSTANCE;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>另外，<strong>serialVersionUID</strong> 建议显式声明，避免改字段后 JDK 自动计算的 UID 变化导致旧数据读不回。</p>
<hr>
<h2 id="九-serializable-vs-externalizable简要"><a class="markdownIt-Anchor" href="#九-serializable-vs-externalizable简要"></a> 九、Serializable vs Externalizable（简要）</h2>
<ul>
<li><strong>Serializable</strong>：默认字段序列化 + 可选钩子（更常用）。</li>
<li><strong>Externalizable</strong>：必须实现 <code>writeExternal</code>/<code>readExternal</code>，完全自己控制格式；反序列化时会调用<strong>无参构造</strong>。</li>
</ul>
<p>若需要跨版本稳定、可控二进制格式，实践中常用自定义协议（Kryo/Protobuf/JSON）或 Externalizable / 手写 DataOutput/DataInput。</p>
<hr>
<h2 id="十-调用链小结"><a class="markdownIt-Anchor" href="#十-调用链小结"></a> 十、调用链小结</h2>
<p><strong>写出</strong>：<code>writeObject(obj)</code> → <code>writeObject0</code> → <code>writeOrdinaryObject</code> → <code>writeClassDesc</code> → <code>handles.assign(obj)</code> → <code>desc.writeObject(obj, this)</code> → <code>defaultWriteFields</code> 或 <code>invokeWriteObject</code>。</p>
<p><strong>读回</strong>：<code>readObject()</code> → <code>readObject0</code> → 根据 TC 分派 → <code>readOrdinaryObject</code> → <code>readClassDesc</code> → <code>desc.newInstance()</code> → <code>handles.assign(obj)</code> → <code>desc.readObject(obj, this)</code> → <code>invokeReadResolve</code>。</p>
<p>读 OpenJDK 时可重点看：<code>writeObject0</code> 的重复引用分支、<code>writeOrdinaryObject</code> 里 assign 的时机、HandleTable 的 lookup / assign、以及 OIS 里 TC_REFERENCE 与 readOrdinaryObject 的顺序。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>源码解读</category>
      </categories>
      <tags>
        <tag>源码解读</tag>
        <tag>java</tag>
        <tag>序列化</tag>
      </tags>
  </entry>
  <entry>
    <title>SPI 机制与跨项目实现（含 Spring 与完整示例）</title>
    <url>/2025/03/01/2025-03-02-java-spi-spring-cross-project/</url>
    <content><![CDATA[<h1 id="spi-机制与跨项目实现含-spring-与完整示例"><a class="markdownIt-Anchor" href="#spi-机制与跨项目实现含-spring-与完整示例"></a> SPI 机制与跨项目实现（含 Spring 与完整示例）</h1>
<p>本文说明 <strong>SPI（Service Provider Interface）</strong> 是什么、如何在<strong>跨项目 / 多模块</strong>下实现，以及如何与 <strong>Spring</strong> 结合使用，并给出可运行的示例（含目录结构）。</p>
<hr>
<h2 id="一-什么是-spi"><a class="markdownIt-Anchor" href="#一-什么是-spi"></a> 一、什么是 SPI</h2>
<p><strong>SPI（Service Provider Interface）</strong> 是 “接口由一方定义，实现由多方提供，在运行时通过约定方式被发现与加载” 的机制。</p>
<ul>
<li><strong>接口方</strong>：定义标准接口（如 <code>javax.sql.Driver</code>、日志门面 API），不绑定具体实现。</li>
<li><strong>实现方</strong>：提供具体实现类，并按约定在 <code>META-INF/services/</code> 下声明 “接口全限定名 → 实现类全限定名”。</li>
<li><strong>使用方</strong>：通过 <strong>ServiceLoader</strong> 或 <strong>SpringFactoriesLoader</strong> 加载当前 classpath 下所有实现，无需硬编码实现类名。</li>
</ul>
<p><strong>典型用途</strong>：驱动加载（JDBC、日志）、插件化、多实现可替换（如多种加密、多种序列化）。</p>
<hr>
<h2 id="二-java-原生-spiserviceloader"><a class="markdownIt-Anchor" href="#二-java-原生-spiserviceloader"></a> 二、Java 原生 SPI：ServiceLoader</h2>
<h3 id="21-约定"><a class="markdownIt-Anchor" href="#21-约定"></a> 2.1 约定</h3>
<ul>
<li>在 <strong>META-INF/services/</strong> 下新建文件，<strong>文件名为接口的全限定类名</strong>。</li>
<li>文件内容为<strong>实现类的全限定类名</strong>，一行一个；<code>#</code> 开头为注释。</li>
<li>实现类必须有<strong>无参构造器</strong>（ServiceLoader 通过反射实例化）。</li>
</ul>
<h3 id="22-使用方式"><a class="markdownIt-Anchor" href="#22-使用方式"></a> 2.2 使用方式</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">ServiceLoader&lt;MyService&gt; loader = ServiceLoader.load(MyService.class);</span><br><span class="line"><span class="keyword">for</span> (MyService impl : loader) {</span><br><span class="line">    impl.doSomething();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>或：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">Iterator&lt;MyService&gt; it = ServiceLoader.load(MyService.class).iterator();</span><br><span class="line"><span class="keyword">while</span> (it.hasNext()) {</span><br><span class="line">    <span class="type">MyService</span> <span class="variable">impl</span> <span class="operator">=</span> it.next();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><strong>注意</strong>：<code>ServiceLoader.load(Class, ClassLoader)</code> 使用传入的 ClassLoader（通常为当前线程上下文类加载器）加载实现类；跨项目时需保证接口与实现都在同一 classpath 或可被同一 ClassLoader 看到。</p>
<hr>
<h2 id="三-跨项目-多模块实现-spi"><a class="markdownIt-Anchor" href="#三-跨项目-多模块实现-spi"></a> 三、跨项目 / 多模块实现 SPI</h2>
<h3 id="31-模块划分"><a class="markdownIt-Anchor" href="#31-模块划分"></a> 3.1 模块划分</h3>
<table>
<thead>
<tr>
<th>模块</th>
<th>职责</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>api</strong></td>
<td>定义 SPI 接口（及可选常量、异常），供 “使用方” 和 “实现方” 依赖</td>
</tr>
<tr>
<td><strong>impl-a / impl-b</strong></td>
<td>实现接口，并在本模块 resources 下提供 <code>META-INF/services/接口全限定名</code></td>
</tr>
<tr>
<td><strong>app</strong></td>
<td>业务应用，依赖 api + 若干 impl 模块（或通过依赖传递 / 插件包引入），通过 ServiceLoader 加载实现</td>
</tr>
</tbody>
</table>
<h3 id="32-目录与文件示例"><a class="markdownIt-Anchor" href="#32-目录与文件示例"></a> 3.2 目录与文件示例</h3>
<p>假设接口为 <code>com.example.spi.Greeter</code>，两个实现为 <code>com.example.impl.HelloGreeter</code> 和 <code>com.example.impl.HiGreeter</code>。</p>
<p><strong>api 模块</strong>（只含接口）：</p>
<figure class="highlight text"><table><tbody><tr><td class="code"><pre><span class="line">api/</span><br><span class="line">  src/main/java/com/example/spi/Greeter.java</span><br></pre></td></tr></tbody></table></figure>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.spi;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Greeter</span> {</span><br><span class="line">    String <span class="title function_">greet</span><span class="params">(String name)</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><strong>impl-a 模块</strong>（实现 + 声明）：</p>
<figure class="highlight text"><table><tbody><tr><td class="code"><pre><span class="line">impl-a/</span><br><span class="line">  src/main/java/com/example/impl/HelloGreeter.java</span><br><span class="line">  src/main/resources/META-INF/services/com.example.spi.Greeter</span><br></pre></td></tr></tbody></table></figure>
<p><code>HelloGreeter.java</code>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.impl;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.example.spi.Greeter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloGreeter</span> <span class="keyword">implements</span> <span class="title class_">Greeter</span> {</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">greet</span><span class="params">(String name)</span> {</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Hello, "</span> + name;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><code>META-INF/services/com.example.spi.Greeter</code> 文件内容（仅一行）：</p>
<figure class="highlight text"><table><tbody><tr><td class="code"><pre><span class="line">com.example.impl.HelloGreeter</span><br></pre></td></tr></tbody></table></figure>
<p><strong>impl-b 模块</strong>：同理，实现类如 <code>HiGreeter</code>，并在 <code>META-INF/services/com.example.spi.Greeter</code> 中写 <code>com.example.impl.HiGreeter</code>。</p>
<p><strong>app 模块</strong>：依赖 api 以及需要启用的 impl-a / impl-b，主类中：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">ServiceLoader&lt;Greeter&gt; loader = ServiceLoader.load(Greeter.class);</span><br><span class="line">loader.forEach(g -&gt; System.out.println(g.greet(<span class="string">"World"</span>)));</span><br></pre></td></tr></tbody></table></figure>
<p><strong>依赖关系</strong>：app 依赖 api + impl-a（或 impl-a、impl-b 都依赖）；<strong>谁在 classpath 谁就会被 ServiceLoader 发现</strong>。因此 “跨项目” 只需：接口在 api 工程，实现在各自工程并带好 <code>META-INF/services</code>，使用方工程通过 Maven / Gradle 依赖需要的实现模块即可。</p>
<hr>
<h2 id="四-spring-中的-spi-形态"><a class="markdownIt-Anchor" href="#四-spring-中的-spi-形态"></a> 四、Spring 中的 SPI 形态</h2>
<h3 id="41-springfactoriesloaderspringfactories"><a class="markdownIt-Anchor" href="#41-springfactoriesloaderspringfactories"></a> 4.1 SpringFactoriesLoader（spring.factories）</h3>
<p>Spring 在 <code>spring-core</code> 中提供 <strong>SpringFactoriesLoader</strong>，约定从 <strong>META-INF/spring.factories</strong> 读取配置，格式为：</p>
<figure class="highlight properties"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment"># key 为接口或抽象类全限定名，value 为实现类全限定名列表（逗号/换行分隔）</span></span><br><span class="line"><span class="attr">com.example.MyFactory</span>=<span class="string">\</span></span><br><span class="line"><span class="string">com.example.impl.MyFactoryImpl1,\</span></span><br><span class="line"><span class="string">com.example.impl.MyFactoryImpl2</span></span><br></pre></td></tr></tbody></table></figure>
<p><strong>加载方式</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">List&lt;MyFactory&gt; list = SpringFactoriesLoader.loadFactories(MyFactory.class, classLoader);</span><br></pre></td></tr></tbody></table></figure>
<p>常用于 <strong>Spring Boot 自动配置</strong>：key 为 <code>org.springframework.boot.autoconfigure.EnableAutoConfiguration</code>，value 为各 <code>@Configuration</code> / <code>@AutoConfiguration</code> 类。</p>
<h3 id="42-spring-boot-2x-与-3x-的差异"><a class="markdownIt-Anchor" href="#42-spring-boot-2x-与-3x-的差异"></a> 4.2 Spring Boot 2.x 与 3.x 的差异</h3>
<table>
<thead>
<tr>
<th>版本</th>
<th>自动配置声明位置</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Spring Boot 2.x</strong></td>
<td><code>META-INF/spring.factories</code>，key：<code>org.springframework.boot.autoconfigure.EnableAutoConfiguration</code></td>
</tr>
<tr>
<td><strong>Spring Boot 3.x</strong></td>
<td><strong>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</strong>，每行一个配置类全限定名</td>
</tr>
</tbody>
</table>
<p>Boot 3 仍支持在 <code>spring.factories</code> 里写其他 key（如自定义 Factory），但 <strong>EnableAutoConfiguration</strong> 已改为从 <code>.imports</code> 读取。</p>
<p><strong>Boot 3 示例</strong>（<code>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</code>）：</p>
<figure class="highlight text"><table><tbody><tr><td class="code"><pre><span class="line">com.example.mystarter.MyAutoConfiguration</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="五-在-spring-中集成-java-spi-实现"><a class="markdownIt-Anchor" href="#五-在-spring-中集成-java-spi-实现"></a> 五、在 Spring 中集成 Java SPI 实现</h2>
<p>若实现是通过 <strong>Java SPI</strong>（META - INF / services）提供的，可以在 Spring 里统一加载并注册为 Bean，供注入使用。</p>
<h3 id="51-将-serviceloader-结果注册为-bean"><a class="markdownIt-Anchor" href="#51-将-serviceloader-结果注册为-bean"></a> 5.1 将 ServiceLoader 结果注册为 Bean</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SpiConfiguration</span> {</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> List&lt;Greeter&gt; <span class="title function_">greeters</span><span class="params">()</span> {</span><br><span class="line">        List&lt;Greeter&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        ServiceLoader.load(Greeter.class).forEach(list::add);</span><br><span class="line">        <span class="keyword">return</span> list;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>业务代码中直接 <code>@Autowired List&lt;Greeter&gt; greeters</code> 使用即可。</p>
<h3 id="52-按类型注入单个实现当只有一个时"><a class="markdownIt-Anchor" href="#52-按类型注入单个实现当只有一个时"></a> 5.2 按类型注入单个实现（当只有一个时）</h3>
<p>若 classpath 下仅有一个 SPI 实现，可封装为单 Bean：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SpiConfiguration</span> {</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@ConditionalOnSingleCandidate(Greeter.class)</span></span><br><span class="line">    <span class="keyword">public</span> Greeter <span class="title function_">greeter</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">return</span> ServiceLoader.load(Greeter.class).findFirst()</span><br><span class="line">            .orElseThrow(() -&gt; <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(<span class="string">"No Greeter implementation found"</span>));</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="53-与-spring-boot-starter-结合"><a class="markdownIt-Anchor" href="#53-与-spring-boot-starter-结合"></a> 5.3 与 Spring Boot Starter 结合</h3>
<p>自定义 Starter 时，通常用 <strong>Spring 的 SPI</strong>（spring.factories 或 Boot 3 的 .imports）声明自动配置类；在自动配置类里再按需使用 <strong>Java ServiceLoader</strong> 加载你的 SPI 实现并注册为 Bean。例如：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">@AutoConfiguration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyAutoConfiguration</span> {</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> List&lt;Greeter&gt; <span class="title function_">greeters</span><span class="params">()</span> {</span><br><span class="line">        List&lt;Greeter&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        ServiceLoader.load(Greeter.class).forEach(list::add);</span><br><span class="line">        <span class="keyword">return</span> list;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>并在 <code>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</code> 中写上该类全限定名，这样引入你的 Starter 即可自动注册这些 Bean。</p>
<hr>
<h2 id="六-完整示例三模块-spring-boot"><a class="markdownIt-Anchor" href="#六-完整示例三模块-spring-boot"></a> 六、完整示例：三模块 + Spring Boot</h2>
<h3 id="61-项目结构maven"><a class="markdownIt-Anchor" href="#61-项目结构maven"></a> 6.1 项目结构（Maven）</h3>
<figure class="highlight text"><table><tbody><tr><td class="code"><pre><span class="line">spi-demo/</span><br><span class="line">  api/           # 接口</span><br><span class="line">  impl-hello/   # 实现 Hello</span><br><span class="line">  impl-hi/      # 实现 Hi</span><br><span class="line">  app/          # Spring Boot 主应用，依赖 api + impl-hello + impl-hi</span><br></pre></td></tr></tbody></table></figure>
<h3 id="62-api-模块"><a class="markdownIt-Anchor" href="#62-api-模块"></a> 6.2 api 模块</h3>
<p><strong>pom.xml</strong>：仅需 <code>junit</code> 等（或无依赖）。</p>
<p><strong>Greeter.java</strong>（同上）：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.spi;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Greeter</span> {</span><br><span class="line">    String <span class="title function_">greet</span><span class="params">(String name)</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="63-impl-hello-模块"><a class="markdownIt-Anchor" href="#63-impl-hello-模块"></a> 6.3 impl-hello 模块</h3>
<ul>
<li>依赖 <strong>api</strong>。</li>
<li><strong>HelloGreeter.java</strong> 实现 <code>Greeter</code>（同上）。</li>
<li><strong>META-INF/services/com.example.spi.Greeter</strong> 内容：<code>com.example.impl.HelloGreeter</code>（注意 impl-hello 里包名可为 <code>com.example.impl</code>，与 impl-hi 若同包则类名不可重复，如改为 <code>com.example.impl.hello.HelloGreeter</code> 更稳妥）。</li>
</ul>
<h3 id="64-impl-hi-模块"><a class="markdownIt-Anchor" href="#64-impl-hi-模块"></a> 6.4 impl-hi 模块</h3>
<ul>
<li>依赖 <strong>api</strong>。</li>
<li><strong>HiGreeter.java</strong>：<code>return "Hi, " + name;</code></li>
<li><strong>META-INF/services/com.example.spi.Greeter</strong> 内容：<code>com.example.impl.HiGreeter</code>（或 <code>com.example.impl.hi.HiGreeter</code>）。</li>
</ul>
<h3 id="65-app-模块spring-boot"><a class="markdownIt-Anchor" href="#65-app-模块spring-boot"></a> 6.5 app 模块（Spring Boot）</h3>
<p><strong>pom.xml</strong>：依赖 <code>api</code>、<code>impl-hello</code>、<code>impl-hi</code> 以及 <code>spring-boot-starter</code>。</p>
<p><strong>SpiConfiguration.java</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.app;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.example.spi.Greeter;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="keyword">import</span> java.util.ServiceLoader;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SpiConfiguration</span> {</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> List&lt;Greeter&gt; <span class="title function_">greeters</span><span class="params">()</span> {</span><br><span class="line">        List&lt;Greeter&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        ServiceLoader.load(Greeter.class).forEach(list::add);</span><br><span class="line">        <span class="keyword">return</span> list;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><strong>AppRunner.java</strong>（或 Controller）：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.app;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.example.spi.Greeter;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.CommandLineRunner;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Component;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AppRunner</span> <span class="keyword">implements</span> <span class="title class_">CommandLineRunner</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> List&lt;Greeter&gt; greeters;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">AppRunner</span><span class="params">(List&lt;Greeter&gt; greeters)</span> {</span><br><span class="line">        <span class="built_in">this</span>.greeters = greeters;</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">(String... args)</span> {</span><br><span class="line">        greeters.forEach(g -&gt; System.out.println(g.greet(<span class="string">"SPI"</span>)));</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>运行后控制台会输出两类问候（来自 HelloGreeter 与 HiGreeter），说明跨模块 SPI 已被 Spring 加载并注入。</p>
<hr>
<h2 id="七-小结"><a class="markdownIt-Anchor" href="#七-小结"></a> 七、小结</h2>
<table>
<thead>
<tr>
<th>要点</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Java SPI</strong></td>
<td>接口 + META - INF/services/接口全限定名 文件（内容为实现类全限定名），通过 <strong>ServiceLoader</strong> 加载</td>
</tr>
<tr>
<td><strong>跨项目</strong></td>
<td>接口放在 api 模块，各实现放在不同模块并各自提供 META - INF / services；使用方依赖 api 与需要的实现模块，classpath 中有谁就加载谁</td>
</tr>
<tr>
<td><strong>Spring 的 SPI</strong></td>
<td><strong>SpringFactoriesLoader</strong> + <code>spring.factories</code>；Boot 3 自动配置改用 <strong>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</strong></td>
</tr>
<tr>
<td><strong>Spring 中用 Java SPI</strong></td>
<td>在 <code>@Configuration</code> 里用 <strong>ServiceLoader.load(接口)</strong> 得到实现列表，注册为 List Bean 或单 Bean，即可 <code>@Autowired</code> 使用</td>
</tr>
<tr>
<td><strong>Starter</strong></td>
<td>自动配置类中可同时使用 Spring 的 SPI（声明配置类）和 Java SPI（加载业务实现并注册 Bean）</td>
</tr>
</tbody>
</table>
<p>按上述方式即可在跨项目场景下实现 SPI，并在 Spring / Spring Boot 中统一使用这些实现。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Spring</tag>
        <tag>SPI</tag>
      </tags>
  </entry>
  <entry>
    <title>String、StringBuffer、StringBuilder 源码解读与 Java 9+ 改动</title>
    <url>/2025/03/01/2025-03-02-java-string-stringbuffer-stringbuilder-source/</url>
    <content><![CDATA[<h1 id="string-stringbuffer-stringbuilder-源码解读与-java-9-改动"><a class="markdownIt-Anchor" href="#string-stringbuffer-stringbuilder-源码解读与-java-9-改动"></a> String、StringBuffer、StringBuilder 源码解读与 Java 9+ 改动</h1>
<p>本文从 JDK 源码出发，说明 <strong>String</strong>、<strong>StringBuffer</strong>、<strong>StringBuilder</strong> 三者的实现结构、核心逻辑与使用场景，并单独梳理 <strong>Java 9 及以后版本</strong> 对这三类的改动（紧凑字符串、byte [] 存储、字符串拼接编译方式等）。</p>
<hr>
<h2 id="一-三者关系与定位"><a class="markdownIt-Anchor" href="#一-三者关系与定位"></a> 一、三者关系与定位</h2>
<table>
<thead>
<tr>
<th>类</th>
<th>可变性</th>
<th>线程安全</th>
<th>典型场景</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>String</strong></td>
<td>不可变</td>
<td>天然线程安全</td>
<td>常量、键、单次赋值</td>
</tr>
<tr>
<td><strong>StringBuffer</strong></td>
<td>可变</td>
<td>同步（synchronized）</td>
<td>多线程下可变的字符序列</td>
</tr>
<tr>
<td><strong>StringBuilder</strong></td>
<td>可变</td>
<td>非线程安全</td>
<td>单线程下大量拼接、构造字符串</td>
</tr>
</tbody>
</table>
<p><strong>继承关系</strong>：<code>StringBuffer</code> 与 <code>StringBuilder</code> 都继承 <strong>AbstractStringBuilder</strong>，没有继承关系；<code>String</code> 独立实现，与后两者无继承关系。</p>
<hr>
<h2 id="二-string-源码解读"><a class="markdownIt-Anchor" href="#二-string-源码解读"></a> 二、String 源码解读</h2>
<h3 id="21-内部存储java-8-vs-java-9"><a class="markdownIt-Anchor" href="#21-内部存储java-8-vs-java-9"></a> 2.1 内部存储（Java 8 vs Java 9+）</h3>
<p><strong>Java 8 及以前</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">String</span> <span class="keyword">implements</span> <span class="title class_">java</span>.io.Serializable, Comparable&lt;String&gt;, CharSequence {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">char</span>[] value;   <span class="comment">// UTF-16，每字符 2 字节</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> hash;            <span class="comment">// 缓存 hashCode</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>用 <strong>char[]</strong> 存字符，每个 char 为 UTF-16 码元，占 <strong>2 字节</strong>。</li>
<li><strong>value 为 final</strong>，引用不变，且对外不暴露可变接口，因此 <strong>String 不可变</strong>。</li>
</ul>
<p><strong>Java 9 及以后（JEP 254 Compact Strings）</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">String</span> <span class="keyword">implements</span> <span class="title class_">java</span>.io.Serializable, Comparable&lt;String&gt;, CharSequence {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">byte</span>[] value;  <span class="comment">// 按编码可能是 1 字节/字符 或 2 字节/字符</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">byte</span> coder;     <span class="comment">// 0 = LATIN1，1 = UTF16</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> hash;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>改为 <strong>byte[] + coder</strong>：
<ul>
<li><strong>LATIN1（coder=0）</strong>：仅含 Latin-1 范围内字符时，每字符 <strong>1 字节</strong>，节省约一半内存。</li>
<li><strong>UTF16（coder=1）</strong>：含其它 Unicode 时，每字符 <strong>2 字节</strong>，与原来语义一致。</li>
</ul>
</li>
<li>仍为 <strong>final byte[]</strong>，String 依然 <strong>不可变</strong>。</li>
<li>可通过 JVM 参数 <code>-XX:-CompactStrings</code> 关闭紧凑字符串（部分版本）。</li>
<li>内部常量化：<code>static final byte LATIN1 = 0;</code>、<code>static final byte UTF16 = 1;</code>，与 <code>StringLatin1</code>/<code>StringUTF16</code> 工具类配合实现 indexOf、concat 等。</li>
</ul>
<h3 id="22-常用构造与-lengthcodepoint"><a class="markdownIt-Anchor" href="#22-常用构造与-lengthcodepoint"></a> 2.2 常用构造与 length / codePoint</h3>
<ul>
<li><strong>length()</strong>：返回的是 <strong>字符个数</strong>（对 UTF-16 即 code unit 个数），在 Java 9+ 内部按 coder 从 byte [] 计算。</li>
<li><strong>codePointCount 等</strong>：按 Unicode 码点处理，与 length 可能不同（存在代理对时）。</li>
</ul>
<p>构造时若传入 <code>char[]</code>，在 Java 9+ 会按内容选择 LATIN1 或 UTF16 编码再存入 byte []。</p>
<h3 id="23-不可变与常量池"><a class="markdownIt-Anchor" href="#23-不可变与常量池"></a> 2.3 不可变与常量池</h3>
<ul>
<li>所有 “修改” 操作（concat、replace、trim 等）都 <strong>返回新 String</strong>，不修改原对象。</li>
<li><strong>字面量</strong> 和 <strong>intern()</strong> 进入 <strong>字符串常量池</strong>，相同内容的 intern 可共享实例，减少重复对象。</li>
</ul>
<h3 id="24-string-的-hashcode-是干什么用的"><a class="markdownIt-Anchor" href="#24-string-的-hashcode-是干什么用的"></a> 2.4 String 的 hashCode 是干什么用的</h3>
<p>源码里 String 有一个字段 <strong><code>private int hash</code></strong>（默认 0），用来<strong>缓存</strong> <code>hashCode()</code> 的返回值。</p>
<p><strong>作用一：满足 “基于哈希的容器” 的约定</strong></p>
<ul>
<li>和 <code>equals</code> 一起用：若 <code>a.equals(b)</code> 为 true，则必须 <code>a.hashCode() == b.hashCode()</code>。</li>
<li>这样 <strong>HashMap、HashSet、Hashtable</strong> 等才能按 “先算 hash → 再按桶找 → 再 equals” 正确找到同一个逻辑对象。</li>
</ul>
<p><strong>作用二：实际用在哪</strong></p>
<ul>
<li><strong>HashMap / HashSet</strong>：用 <code>key.hashCode()</code> 算桶下标，减少逐个 <code>equals</code> 比较。</li>
<li>字符串做 key 时（如 <code>Map&lt;String, User&gt;</code>、<code>Set&lt;String&gt;</code>）都依赖 String 的 hashCode 做快速查找、去重。</li>
<li>任何用 “对象做 key” 或 “按对象去重” 的哈希结构都会用到该对象的 hashCode，String 是最常用的一种。</li>
</ul>
<p><strong>为什么要缓存 <code>hash</code>？</strong></p>
<ul>
<li>String <strong>不可变</strong>，算过一次 hashCode 后结果永远不变。</li>
<li>把结果存进 <code>hash</code>（0 表示 “还没算过”），下次再调 <code>hashCode()</code> 直接返回缓存，<strong>避免重复计算</strong>。</li>
<li>同一个 String 常会多次参与 HashMap 的 put / get，缓存能明显减少重复计算。</li>
</ul>
<p><strong>一句话</strong>：String 的 hashCode 用于在 HashMap、HashSet 等容器里<strong>快速定位和比较</strong>“是不是同一个字符串”；<code>hash</code> 字段用来<strong>缓存</strong>这个值，因为字符串不可变，算一次即可。</p>
<hr>
<h2 id="三-abstractstringbuilder-源码解读"><a class="markdownIt-Anchor" href="#三-abstractstringbuilder-源码解读"></a> 三、AbstractStringBuilder 源码解读</h2>
<p><code>StringBuffer</code> 和 <code>StringBuilder</code> 的公共逻辑都在 <strong>AbstractStringBuilder</strong> 中。</p>
<h3 id="31-内部存储java-8-vs-java-9"><a class="markdownIt-Anchor" href="#31-内部存储java-8-vs-java-9"></a> 3.1 内部存储（Java 8 vs Java 9+）</h3>
<p><strong>Java 8</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">AbstractStringBuilder</span> {</span><br><span class="line">    <span class="type">char</span>[] value;   <span class="comment">// 可扩容，非 final</span></span><br><span class="line">    <span class="type">int</span> count;      <span class="comment">// 已用字符数（逻辑长度）</span></span><br><span class="line">    <span class="comment">// 容量即 value.length</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><strong>Java 9+</strong>：与 String 对齐，改为 <strong>byte[] + coder</strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">AbstractStringBuilder</span> {</span><br><span class="line">    <span class="type">byte</span>[] value;</span><br><span class="line">    <span class="type">byte</span> coder;</span><br><span class="line">    <span class="type">int</span> count;      <span class="comment">// 字符数（按 code unit 计）</span></span><br><span class="line">    <span class="comment">// 容量为 value.length &gt;&gt; coder 或按字节语义</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li><strong>value 非 final</strong>，可扩容，因此是 <strong>可变</strong> 字符序列。</li>
<li>扩容、append、getChars 等都会根据 <strong>coder</strong> 走对应实现（如 StringLatin1 / StringUTF16 的静态方法），保证与 String 的 Compact Strings 一致。</li>
</ul>
<h3 id="32-初始容量与扩容"><a class="markdownIt-Anchor" href="#32-初始容量与扩容"></a> 3.2 初始容量与扩容</h3>
<ul>
<li><strong>无参构造</strong>：初始容量一般为 <strong>16</strong>（字符数）。</li>
<li><strong>带初始字符串 / 容量构造</strong>：容量 = 字符串长度 + 16 或指定值。</li>
</ul>
<p><strong>扩容逻辑</strong>（概念一致，Java 9+ 中按 byte 容量换算）：</p>
<ol>
<li>若 <code>count + 待追加长度 &gt; value 容量</code>，则扩容。</li>
<li>新容量通常为 <strong>旧容量 * 2 + 2</strong>（<code>(capacity &lt;&lt; 1) + 2</code>）。</li>
<li>若仍不足，则新容量 = 所需最小容量。</li>
<li>上限约为 <strong>Integer.MAX_VALUE - 8</strong>（或实现定义上限），否则抛 <code>OutOfMemoryError</code>。</li>
<li>使用 <strong>Arrays.copyOf</strong> 复制到新数组。</li>
</ol>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 扩容逻辑（伪代码，Java 8 风格便于理解）</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">ensureCapacityInternal</span><span class="params">(<span class="type">int</span> minimumCapacity)</span> {</span><br><span class="line">    <span class="keyword">if</span> (minimumCapacity - value.length &gt; <span class="number">0</span>) {</span><br><span class="line">        <span class="type">int</span> <span class="variable">newCapacity</span> <span class="operator">=</span> (value.length &lt;&lt; <span class="number">1</span>) + <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span> (newCapacity - minimumCapacity &lt; <span class="number">0</span>) newCapacity = minimumCapacity;</span><br><span class="line">        <span class="keyword">if</span> (newCapacity &lt; <span class="number">0</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">OutOfMemoryError</span>();</span><br><span class="line">        value = Arrays.copyOf(value, newCapacity);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="33-append-insert-delete"><a class="markdownIt-Anchor" href="#33-append-insert-delete"></a> 3.3 append / insert / delete</h3>
<ul>
<li><strong>append(String str)</strong>：先 <strong>ensureCapacity(count + str.length())</strong>，再把 str 的字符（通过 getChars 或等价逻辑）拷贝到 value，更新 count，返回 this。</li>
<li><strong>insert / delete</strong>：同样先保证容量，再移动区间内元素，更新 count。</li>
<li>若传入 null，通常当作 <code>"null"</code> 字符串处理（appendNull）。</li>
</ul>
<hr>
<h2 id="四-stringbuffer-与-stringbuilder-的差异"><a class="markdownIt-Anchor" href="#四-stringbuffer-与-stringbuilder-的差异"></a> 四、StringBuffer 与 StringBuilder 的差异</h2>
<p>两者都继承 AbstractStringBuilder，<strong>API 完全一致</strong>，区别只有 <strong>线程安全</strong>：</p>
<ul>
<li><strong>StringBuffer</strong>：对外方法（append、insert、delete、replace 等）都加 <strong>synchronized</strong>，多线程下安全，但存在锁开销。</li>
<li><strong>StringBuilder</strong>：无同步，单线程下更快，<strong>推荐在单线程场景使用</strong>。</li>
</ul>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// StringBuffer 典型方法（简化）</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">synchronized</span> StringBuffer <span class="title function_">append</span><span class="params">(String str)</span> {</span><br><span class="line">    <span class="built_in">super</span>.append(str);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// StringBuilder 典型方法（简化）</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> StringBuilder <span class="title function_">append</span><span class="params">(String str)</span> {</span><br><span class="line">    <span class="built_in">super</span>.append(str);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="五-java-9-及以后对三者的改动汇总"><a class="markdownIt-Anchor" href="#五-java-9-及以后对三者的改动汇总"></a> 五、Java 9 及以后对三者的改动汇总</h2>
<h3 id="51-紧凑字符串jep-254-string-abstractstringbuilder-均受益"><a class="markdownIt-Anchor" href="#51-紧凑字符串jep-254-string-abstractstringbuilder-均受益"></a> 5.1 紧凑字符串（JEP 254）—— String、AbstractStringBuilder 均受益</h3>
<table>
<thead>
<tr>
<th>项目</th>
<th>Java 8</th>
<th>Java 9+</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>String</strong></td>
<td><code>char[] value</code>，2 字节 / 字符</td>
<td><code>byte[] value</code> + <code>byte coder</code>，LATIN1 时 1 字节 / 字符</td>
</tr>
<tr>
<td><strong>AbstractStringBuilder</strong>（及 StringBuffer / StringBuilder）</td>
<td><code>char[] value</code></td>
<td><code>byte[] value</code> + <code>byte coder</code></td>
</tr>
</tbody>
</table>
<ul>
<li><strong>动机</strong>：大量字符串只含 Latin-1 字符，堆上 String 占比高，改为按需 1 字节 / 字符可显著 <strong>降低内存与 GC 压力</strong>。</li>
<li><strong>对 StringBuffer / StringBuilder</strong>：内部存储与 String 统一，转换与拷贝时无需再在 char [] 与 byte [] 间多做转换，实现更一致。</li>
</ul>
<h3 id="52-字符串拼接编译方式jep-280-indify-string-concatenation"><a class="markdownIt-Anchor" href="#52-字符串拼接编译方式jep-280-indify-string-concatenation"></a> 5.2 字符串拼接编译方式（JEP 280 Indify String Concatenation）</h3>
<ul>
<li><strong>Java 8 及以前</strong>：<code>"a" + b + "c"</code> 编译为 <strong>显式 StringBuilder</strong>：new StringBuilder → 多次 append → toString ()。</li>
<li><strong>Java 9+</strong>：改为 <strong>invokedynamic</strong>（如 <code>makeConcatWithConstants</code>），由 JVM 在运行时选择拼接策略（可优化为直接使用 byte []、避免中间 StringBuilder 等），<strong>字节码更简洁</strong>，且未来可继续优化而无需重新编译应用。</li>
<li><strong>注意</strong>：早期版本（如 Java 11–18）存在 “先求值再转字符串” 的顺序与旧行为不一致的 bug，<strong>Java 19 已修复</strong>。</li>
</ul>
<p>这主要影响 <strong>编译期</strong> 对 <code>+</code> 拼接的翻译，不改变 String / StringBuilder 的公开 API，但运行时生成的实现会随 JDK 版本演进。</p>
<h3 id="53-后续版本中的小补充"><a class="markdownIt-Anchor" href="#53-后续版本中的小补充"></a> 5.3 后续版本中的小补充</h3>
<ul>
<li><strong>Java 11</strong>：String 增加 <strong>isBlank()、strip()、repeat()、lines()</strong> 等，内部实现基于 byte []+coder。</li>
<li><strong>Java 17</strong>：<strong>String.format()</strong> 等实现有优化，性能有提升。</li>
<li><strong>Java 21</strong>：<strong>字符串模板</strong>（JEP 430 预览）提供新的字符串构建方式；String 构造器在处理 <strong>可变数组参数</strong> 时的拷贝与健壮性增强，避免调用方在构造后修改数组影响 String 内容。</li>
</ul>
<hr>
<h2 id="六-使用建议与小结"><a class="markdownIt-Anchor" href="#六-使用建议与小结"></a> 六、使用建议与小结</h2>
<table>
<thead>
<tr>
<th>场景</th>
<th>推荐</th>
</tr>
</thead>
<tbody>
<tr>
<td>常量、键、单次赋值、不需要修改</td>
<td><strong>String</strong></td>
</tr>
<tr>
<td>单线程下多次拼接、构造大字符串</td>
<td><strong>StringBuilder</strong></td>
</tr>
<tr>
<td>多线程下共享可变字符序列</td>
<td><strong>StringBuffer</strong></td>
</tr>
<tr>
<td>简单少量拼接（如 <code>"a"+b</code>）</td>
<td>直接 <code>+</code>，Java 9+ 由 invokedynamic 优化</td>
</tr>
</tbody>
</table>
<p><strong>源码层面的要点</strong>：</p>
<ul>
<li><strong>String</strong>：不可变，Java 9+ 用 byte []+coder 做紧凑存储；所有 “修改” 都是新对象。</li>
<li><strong>AbstractStringBuilder</strong>：可变，Java 9+ 同样 byte []+coder，扩容规则 *2 + 2，与 String 编码一致。</li>
<li><strong>StringBuffer</strong> = AbstractStringBuilder + synchronized；<strong>StringBuilder</strong> = AbstractStringBuilder，无同步。</li>
<li><strong>Java 9+</strong>：三者存储统一为 byte []+coder（Compact Strings），字符串 <code>+</code> 的编译从 StringBuilder 链式调用改为 invokedynamic，后续版本继续在 format、构造器、模板等方面做增强。</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
        <category>源码解读</category>
      </categories>
      <tags>
        <tag>源码解读</tag>
        <tag>java</tag>
        <tag>String</tag>
        <tag>StringBuilder</tag>
        <tag>StringBuffer</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 动态代理原理与 Java 9+ 改动</title>
    <url>/2025/03/01/2025-03-02-java-dynamic-proxy-and-java9-changes/</url>
    <content><![CDATA[<h1 id="java-动态代理原理与-java-9-改动"><a class="markdownIt-Anchor" href="#java-动态代理原理与-java-9-改动"></a> Java 动态代理原理与 Java 9+ 改动</h1>
<p>本文说明 <strong>JDK 动态代理</strong>（<code>java.lang.reflect.Proxy</code> + <code>InvocationHandler</code>）的原理、用法与限制，并单独梳理 <strong>Java 9 及以后版本</strong> 对动态代理的改动（模块化、代理类包与可见性、ProxyGenerator 位置等）。</p>
<hr>
<h2 id="一-什么是动态代理"><a class="markdownIt-Anchor" href="#一-什么是动态代理"></a> 一、什么是动态代理</h2>
<p><strong>动态代理</strong>：在运行时动态生成 “代理类” 的字节码并加载，由代理类把方法调用统一转交给一个 <strong>InvocationHandler</strong> 处理，从而在不改业务类的前提下做增强（日志、事务、权限等）。</p>
<p>JDK 自带的动态代理基于 <strong>接口</strong>：只能为 “实现了若干接口” 的目标生成代理，代理对象实现相同接口，方法调用最终进入 <code>InvocationHandler.invoke()</code>。</p>
<hr>
<h2 id="二-核心-api"><a class="markdownIt-Anchor" href="#二-核心-api"></a> 二、核心 API</h2>
<h3 id="21-proxy"><a class="markdownIt-Anchor" href="#21-proxy"></a> 2.1 Proxy</h3>
<ul>
<li><strong>Proxy.newProxyInstance(ClassLoader loader, Class&lt;?&gt;[] interfaces, InvocationHandler h)</strong><br>
在运行时生成并加载代理类，返回实现 <code>interfaces</code> 的代理实例，该实例上所有接口方法调用都会交给 <code>h</code> 处理。</li>
</ul>
<h3 id="22-invocationhandler"><a class="markdownIt-Anchor" href="#22-invocationhandler"></a> 2.2 InvocationHandler</h3>
<ul>
<li><strong>Object invoke(Object proxy, Method method, Object[] args)</strong><br>
代理实例上 “对接口方法的调用” 都会转发到这里。
<ul>
<li><code>proxy</code>：代理对象本身（不要用它在 handler 里再调接口方法，否则会递归进 handler）。</li>
<li><code>method</code>：被调用的方法。</li>
<li><code>args</code>：方法参数。<br>
返回值作为该次方法调用的结果。</li>
</ul>
</li>
</ul>
<hr>
<h2 id="三-使用示例"><a class="markdownIt-Anchor" href="#三-使用示例"></a> 三、使用示例</h2>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.reflect.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Hello</span> {</span><br><span class="line">    String <span class="title function_">say</span><span class="params">(String name)</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">HelloImpl</span> <span class="keyword">implements</span> <span class="title class_">Hello</span> {</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">say</span><span class="params">(String name)</span> {</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Hello, "</span> + name;</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ProxyDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="type">Hello</span> <span class="variable">target</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HelloImpl</span>();</span><br><span class="line">        <span class="type">InvocationHandler</span> <span class="variable">handler</span> <span class="operator">=</span> (proxy, method, args) -&gt; {</span><br><span class="line">            System.out.println(<span class="string">"Before: "</span> + method.getName());</span><br><span class="line">            <span class="type">Object</span> <span class="variable">result</span> <span class="operator">=</span> method.invoke(target, args);</span><br><span class="line">            System.out.println(<span class="string">"After: "</span> + method.getName());</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        };</span><br><span class="line"></span><br><span class="line">        <span class="type">Hello</span> <span class="variable">proxy</span> <span class="operator">=</span> (Hello) Proxy.newProxyInstance(</span><br><span class="line">            Hello.class.getClassLoader(),</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Class</span>&lt;?&gt;[]{Hello.class},</span><br><span class="line">            handler</span><br><span class="line">        );</span><br><span class="line"></span><br><span class="line">        System.out.println(proxy.say(<span class="string">"World"</span>));</span><br><span class="line">        <span class="comment">// Before: say</span></span><br><span class="line">        <span class="comment">// After: say</span></span><br><span class="line">        <span class="comment">// Hello, World</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li>目标类必须 <strong>实现接口</strong>，代理的是 “接口类型”，实际调用仍转发到 <code>target</code>。</li>
<li>若要对 “没有接口的类” 做代理，需用 <strong>CGLIB</strong> 等基于子类的方案，见后文对比。</li>
</ul>
<hr>
<h2 id="四-工作原理源码层面"><a class="markdownIt-Anchor" href="#四-工作原理源码层面"></a> 四、工作原理（源码层面）</h2>
<h3 id="41-newproxyinstance-流程概要"><a class="markdownIt-Anchor" href="#41-newproxyinstance-流程概要"></a> 4.1 newProxyInstance 流程概要</h3>
<ol>
<li>校验：<code>loader</code>、<code>interfaces</code> 非空，接口均为接口类型且由同一 loader 加载等。</li>
<li><strong>查找或生成代理类</strong>：以 <code>(loader, interfaces)</code> 为 key 查缓存；若无则用 <strong>ProxyGenerator</strong>（或内部等价实现）生成代理类字节码，再通过 <code>Unsafe.defineClass</code> 或 <code>ClassLoader.defineClass</code> 加载。</li>
<li><strong>实例化</strong>：取代理类的唯一构造器，参数类型为 <code>InvocationHandler</code>，传入 <code>h</code>，<code>newInstance(h)</code> 得到代理实例。</li>
<li>返回该实例。</li>
</ol>
<h3 id="42-生成的代理类长什么样"><a class="markdownIt-Anchor" href="#42-生成的代理类长什么样"></a> 4.2 生成的代理类长什么样</h3>
<ul>
<li>类名：通常为 <strong>com.sun.proxy.$ProxyN</strong>（N 为递增数字），Java 9+ 在模块化下可能处于 “动态模块”，见第五节。</li>
<li><strong>继承</strong>：<code>extends java.lang.reflect.Proxy</code>（因此代理类也实现 <code>Serializable</code>）。</li>
<li><strong>实现</strong>：你传入的 <code>interfaces</code>。</li>
<li><strong>字段</strong>：从 Proxy 继承的 <code>protected InvocationHandler h</code>。</li>
<li><strong>方法</strong>：每个接口方法（以及从 Object 转发的 <code>equals</code>、<code>hashCode</code>、<code>toString</code>）内部都类似：</li>
</ul>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> String <span class="title function_">say</span><span class="params">(String name)</span> {</span><br><span class="line">    <span class="keyword">try</span> {</span><br><span class="line">        <span class="keyword">return</span> (String) <span class="built_in">super</span>.h.invoke(<span class="built_in">this</span>, m_say, <span class="keyword">new</span> <span class="title class_">Object</span>[]{name});</span><br><span class="line">    } <span class="keyword">catch</span> (RuntimeException | Error e) {</span><br><span class="line">        <span class="keyword">throw</span> e;</span><br><span class="line">    } <span class="keyword">catch</span> (Throwable t) {</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">UndeclaredThrowableException</span>(t);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>即：把调用转给 <code>InvocationHandler.invoke(proxy, method, args)</code>，检查异常后返回或包装为 <code>UndeclaredThrowableException</code>。</p>
<h3 id="43-重要限制"><a class="markdownIt-Anchor" href="#43-重要限制"></a> 4.3 重要限制</h3>
<ul>
<li><strong>只支持接口</strong>：不能指定 “代理某个类”，只能指定若干接口，代理类实现这些接口。</li>
<li><strong>Object 方法</strong>：<code>equals</code>、<code>hashCode</code>、<code>toString</code> 也会转发到 handler；其他 Object 方法（如 <code>getClass()</code>）不转发，由代理类自身实现。</li>
</ul>
<hr>
<h2 id="五-java-9-及以后对动态代理的改动"><a class="markdownIt-Anchor" href="#五-java-9-及以后对动态代理的改动"></a> 五、Java 9 及以后对动态代理的改动</h2>
<h3 id="51-模块系统与代理类的包-可见性"><a class="markdownIt-Anchor" href="#51-模块系统与代理类的包-可见性"></a> 5.1 模块系统与代理类的包、可见性</h3>
<p>Java 9 引入模块后，代理类的 <strong>包与可见性</strong> 受 “代理接口所在包是否被当前模块导出 / 打开” 影响：</p>
<table>
<thead>
<tr>
<th>情况</th>
<th>代理类所在位置 / 可见性</th>
</tr>
</thead>
<tbody>
<tr>
<td>所有代理接口都在<strong>已导出或已打开</strong>的包中</td>
<td>代理类在 “未命名包” 或实现定义的包中（如 com.sun.proxy），公共接口时一般为 public</td>
</tr>
<tr>
<td>至少一个代理接口在<strong>未导出且未打开</strong>的包中</td>
<td>代理类在 <strong>动态模块</strong> 中；若通过 <code>Constructor.newInstance()</code> 在代理类上反射创建实例，可能抛 <code>IllegalAccessException</code>，应使用 <strong>Proxy.newProxyInstance()</strong> 创建</td>
</tr>
</tbody>
</table>
<p>要点：当接口来自未导出包时，代理类被放在 “动态模块” 里，与普通模块的访问规则一致，不能随意反射实例化，必须走 <code>Proxy.newProxyInstance()</code>。</p>
<h3 id="52-proxygenerator-位置与保存代理类字节码"><a class="markdownIt-Anchor" href="#52-proxygenerator-位置与保存代理类字节码"></a> 5.2 ProxyGenerator 位置与保存代理类字节码</h3>
<table>
<thead>
<tr>
<th>项目</th>
<th>Java 8</th>
<th>Java 9+</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>ProxyGenerator 所在包</strong></td>
<td><code>sun.misc.ProxyGenerator</code>（不推荐依赖）</td>
<td>移至 <strong>jdk.internal</strong>（如 <code>jdk.internal.reflect</code>），非导出 API</td>
</tr>
<tr>
<td><strong>保存生成的 .class 文件</strong></td>
<td><code>-Dsun.misc.ProxyGenerator.saveGeneratedFiles=true</code></td>
<td><strong><code>-Djdk.proxy.ProxyGenerator.saveGeneratedFiles=true</code></strong></td>
</tr>
</tbody>
</table>
<p>调试时若要把运行时生成的代理类 dump 到磁盘，Java 9+ 需使用新系统属性 <strong><code>jdk.proxy.ProxyGenerator.saveGeneratedFiles=true</code></strong>；不要依赖 <code>sun.misc.ProxyGenerator</code>，内部实现可能随时调整。</p>
<h3 id="53-核心-api-保持不变"><a class="markdownIt-Anchor" href="#53-核心-api-保持不变"></a> 5.3 核心 API 保持不变</h3>
<ul>
<li><strong>Proxy.newProxyInstance(ClassLoader, Class&lt;?&gt;[], InvocationHandler)</strong> 的签名与语义未变。</li>
<li><strong>InvocationHandler.invoke</strong> 的约定未变。</li>
<li>代理类仍继承 <code>Proxy</code>、实现指定接口，方法转发到 handler。<br>
因此 “怎么用” 在 8 与 9+ 一致，变化主要在 “代理类放在哪个模块/包、如何生成与加载” 以及调试/内部实现细节。</li>
</ul>
<hr>
<h2 id="六-与-cglib-的简要对比"><a class="markdownIt-Anchor" href="#六-与-cglib-的简要对比"></a> 六、与 CGLIB 的简要对比</h2>
<table>
<thead>
<tr>
<th>对比项</th>
<th>JDK 动态代理</th>
<th>CGLIB</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>代理对象</strong></td>
<td>实现指定接口</td>
<td>继承目标类（子类）</td>
</tr>
<tr>
<td><strong>前提</strong></td>
<td>必须有接口</td>
<td>无接口也可，目标类 / 方法不能为 final</td>
</tr>
<tr>
<td><strong>实现方式</strong></td>
<td>反射 + 运行时生成实现接口的类</td>
<td>ASM 等字节码生成子类</td>
</tr>
<tr>
<td><strong>典型用法</strong></td>
<td>Spring 中 “Bean 实现了接口” 时的默认代理</td>
<td>Spring 中 “Bean 无接口” 或 <code>proxy-target-class=true</code></td>
</tr>
</tbody>
</table>
<p>需要代理 “没有接口的类” 或 “类上的方法” 时，只能用 CGLIB（或类似字节码方案），不能用 JDK Proxy。</p>
<hr>
<h2 id="七-小结"><a class="markdownIt-Anchor" href="#七-小结"></a> 七、小结</h2>
<ul>
<li><strong>JDK 动态代理</strong>：通过 <strong>Proxy.newProxyInstance</strong> 传入类加载器、接口数组和 <strong>InvocationHandler</strong>，在运行时生成实现这些接口的代理类，所有接口方法（以及 equals/hashCode/toString）调用都转到 <strong>handler.invoke</strong>，常用于 AOP、RPC 客户端等。</li>
<li><strong>限制</strong>：只能基于接口；代理类继承 <code>Proxy</code>，由内部 ProxyGenerator 生成字节码并加载。</li>
<li><strong>Java 9+</strong>：模块化下代理类可能位于 “动态模块”，未导出包中的接口会影响代理类的可访问性；ProxyGenerator 移入 jdk.internal，保存生成文件改用 <strong><code>jdk.proxy.ProxyGenerator.saveGeneratedFiles=true</code></strong>；对外 API 不变，现有基于 Proxy + InvocationHandler 的代码可继续使用。</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
        <category>源码解读</category>
      </categories>
      <tags>
        <tag>源码解读</tag>
        <tag>java</tag>
        <tag>动态代理</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 线程池知识点与 Java 9+ 改动</title>
    <url>/2025/03/01/2025-03-02-java-thread-pool-complete-and-java9-changes/</url>
    <content><![CDATA[<h1 id="java-线程池知识点与-java-9-改动"><a class="markdownIt-Anchor" href="#java-线程池知识点与-java-9-改动"></a> Java 线程池知识点与 Java 9+ 改动</h1>
<p>本文梳理 <strong>Java 线程池</strong> 的完整知识点：体系结构、核心参数、任务流程、拒绝策略、Executors 工厂方法、ForkJoinPool 与定时任务，以及 <strong>Java 9 及以后版本</strong> 对线程池与相关 API 的改动（CompletableFuture 增强、虚拟线程 Executor 等）。</p>
<hr>
<h2 id="一-体系结构"><a class="markdownIt-Anchor" href="#一-体系结构"></a> 一、体系结构</h2>
<figure class="highlight plaintext"><table><tbody><tr><td class="code"><pre><span class="line">Executor（接口）</span><br><span class="line">    └── ExecutorService（接口）</span><br><span class="line">            └── AbstractExecutorService（抽象类）</span><br><span class="line">                    ├── ThreadPoolExecutor（通用线程池）</span><br><span class="line">                    ├── ScheduledThreadPoolExecutor（定时/周期任务）</span><br><span class="line">                    └── ForkJoinPool（分治/工作窃取）</span><br></pre></td></tr></tbody></table></figure>
<ul>
<li><strong>Executor</strong>：只定义 <code>void execute(Runnable command)</code>，表示 “执行任务”。</li>
<li><strong>ExecutorService</strong>：扩展了提交任务（submit 返回 Future）、关闭（shutdown/shutdownNow）、批量执行（invokeAll/invokeAny）等。</li>
<li><strong>ThreadPoolExecutor</strong>：最常用的实现，通过<strong>核心线程数、最大线程数、队列、拒绝策略</strong>等控制行为。</li>
</ul>
<hr>
<h2 id="二-threadpoolexecutor-核心参数"><a class="markdownIt-Anchor" href="#二-threadpoolexecutor-核心参数"></a> 二、ThreadPoolExecutor 核心参数</h2>
<p>构造方法（7 个参数）：</p>
<figure class="highlight text"><table><tbody><tr><td class="code"><pre><span class="line">ThreadPoolExecutor(</span><br><span class="line">    int corePoolSize,</span><br><span class="line">    int maximumPoolSize,</span><br><span class="line">    long keepAliveTime,</span><br><span class="line">    TimeUnit unit,</span><br><span class="line">    BlockingQueue&lt;Runnable&gt; workQueue,</span><br><span class="line">    ThreadFactory threadFactory,</span><br><span class="line">    RejectedExecutionHandler handler</span><br><span class="line">)</span><br></pre></td></tr></tbody></table></figure>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>corePoolSize</strong></td>
<td>核心线程数，常驻池中，空闲时默认不回收（除非开启 allowCoreThreadTimeOut）</td>
</tr>
<tr>
<td><strong>maximumPoolSize</strong></td>
<td>最大线程数，池中线程总数上限</td>
</tr>
<tr>
<td><strong>keepAliveTime</strong></td>
<td>非核心线程空闲超过该时间会被回收；单位由 unit 指定</td>
</tr>
<tr>
<td><strong>unit</strong></td>
<td>keepAliveTime 的时间单位</td>
</tr>
<tr>
<td><strong>workQueue</strong></td>
<td>任务队列，存放尚未被线程执行的任务</td>
</tr>
<tr>
<td><strong>threadFactory</strong></td>
<td>创建新线程的工厂，可自定义线程名、优先级等</td>
</tr>
<tr>
<td><strong>handler</strong></td>
<td>当 “线程数已达 maximum 且队列已满” 时的拒绝策略</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="三-任务提交流程"><a class="markdownIt-Anchor" href="#三-任务提交流程"></a> 三、任务提交流程</h2>
<p><code>execute(Runnable command)</code> 的大致逻辑：</p>
<ol>
<li><strong>当前线程数 &lt; corePoolSize</strong>：创建新<strong>核心线程</strong>执行该任务。</li>
<li><strong>当前线程数 ≥ corePoolSize</strong>：尝试将任务<strong>放入 workQueue</strong>。</li>
<li><strong>队列已满</strong>：尝试创建<strong>非核心线程</strong>（不超过 maximumPoolSize）执行该任务。</li>
<li><strong>无法创建新线程</strong>（已达 maximum 且队列满）：执行 <strong>RejectedExecutionHandler</strong>（拒绝策略）。</li>
</ol>
<p>因此：<strong>先占满核心线程 → 再填队列 → 再开非核心线程 → 再拒绝</strong>。<br>
使用<strong>无界队列</strong>时，不会走到 “队列满”，也就不会创建非核心线程，maximumPoolSize 形同虚设。</p>
<hr>
<h2 id="四-任务队列workqueue常见类型"><a class="markdownIt-Anchor" href="#四-任务队列workqueue常见类型"></a> 四、任务队列（workQueue）常见类型</h2>
<table>
<thead>
<tr>
<th>队列类型</th>
<th>特点</th>
<th>典型用法</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>LinkedBlockingQueue</strong>（无界）</td>
<td>容量可视为无限，不触发拒绝</td>
<td>任务量可控、希望 “削峰” 时</td>
</tr>
<tr>
<td><strong>ArrayBlockingQueue</strong>（有界）</td>
<td>固定容量，满则走非核心 / 拒绝</td>
<td>需要限制排队数量时</td>
</tr>
<tr>
<td><strong>SynchronousQueue</strong></td>
<td>不存元素，移交即被取走</td>
<td>配合 “大 maximumPoolSize” 做直接交接</td>
</tr>
<tr>
<td><strong>DelayQueue</strong></td>
<td>按延迟时间取出</td>
<td>延迟任务（也可用 ScheduledExecutorService）</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="五-拒绝策略rejectedexecutionhandler"><a class="markdownIt-Anchor" href="#五-拒绝策略rejectedexecutionhandler"></a> 五、拒绝策略（RejectedExecutionHandler）</h2>
<p>当 “线程数 = maximumPoolSize 且 workQueue 满” 时触发。</p>
<table>
<thead>
<tr>
<th>策略</th>
<th>行为</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>AbortPolicy</strong>（默认）</td>
<td>抛出 <strong>RejectedExecutionException</strong></td>
</tr>
<tr>
<td><strong>CallerRunsPolicy</strong></td>
<td>由<strong>提交任务的线程</strong>直接执行该任务，相当于 “调用者自己跑”</td>
</tr>
<tr>
<td><strong>DiscardPolicy</strong></td>
<td>静默丢弃任务，不抛异常</td>
</tr>
<tr>
<td><strong>DiscardOldestPolicy</strong></td>
<td>丢弃队列中<strong>最早</strong>的一个任务，再把当前任务入队</td>
</tr>
</tbody>
</table>
<p>可自定义实现 <strong>RejectedExecutionHandler</strong>，做打日志、入库、转存等。</p>
<hr>
<h2 id="六-executors-工厂方法及注意点"><a class="markdownIt-Anchor" href="#六-executors-工厂方法及注意点"></a> 六、Executors 工厂方法及注意点</h2>
<table>
<thead>
<tr>
<th>方法</th>
<th>内部实现要点</th>
<th>常见问题</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>newFixedThreadPool(n)</strong></td>
<td>核心 = 最大 = n，<strong>无界</strong> LinkedBlockingQueue</td>
<td>任务过多时队列无限增长，易 OOM</td>
</tr>
<tr>
<td><strong>newCachedThreadPool()</strong></td>
<td>核心 = 0，最大 = Integer.MAX_VALUE，<strong>SynchronousQueue</strong></td>
<td>任务多时线程数暴增，易耗尽资源</td>
</tr>
<tr>
<td><strong>newSingleThreadExecutor()</strong></td>
<td>核心 = 最大 = 1，无界队列</td>
<td>同上，队列无界</td>
</tr>
<tr>
<td><strong>newScheduledThreadPool(n)</strong></td>
<td>定时任务线程池，DelayedWorkQueue</td>
<td>核心线程数可设为 0 或较小，需结合业务</td>
</tr>
</tbody>
</table>
<p><strong>建议</strong>：生产环境尽量用 <strong>ThreadPoolExecutor 构造器</strong> 显式指定有界队列、合理 maximum、自定义 ThreadFactory 和拒绝策略，避免直接用上述 “无界” 工厂。</p>
<hr>
<h2 id="七-关闭shutdown-与-shutdownnow"><a class="markdownIt-Anchor" href="#七-关闭shutdown-与-shutdownnow"></a> 七、关闭：shutdown 与 shutdownNow</h2>
<ul>
<li><strong>shutdown()</strong>：不再接受新任务，已提交的任务会继续执行完；池不会立刻退出。</li>
<li><strong>shutdownNow()</strong>：不再接受新任务，并<strong>尝试中断</strong>正在执行的任务，返回<strong>尚未执行的任务列表</strong>；对不响应中断的任务无法强制停止。</li>
<li><strong>awaitTermination(long timeout, TimeUnit unit)</strong>：阻塞当前线程，直到池关闭或超时；常与 shutdown 配合使用，先 shutdown 再 awaitTermination 等待一段时间。</li>
</ul>
<p>典型用法：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">executor.shutdown();</span><br><span class="line"><span class="keyword">if</span> (!executor.awaitTermination(<span class="number">60</span>, TimeUnit.SECONDS)) {</span><br><span class="line">    executor.shutdownNow();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="八-扩展点与其它配置"><a class="markdownIt-Anchor" href="#八-扩展点与其它配置"></a> 八、扩展点与其它配置</h2>
<ul>
<li><strong>allowCoreThreadTimeOut(boolean)</strong>：为 true 时，核心线程在空闲 keepAliveTime 后也会被回收；默认 false。</li>
<li><strong>beforeExecute(Thread t, Runnable r)</strong> / <strong>afterExecute(Runnable r, Throwable t)</strong>：子类可重写，在任务执行前后打日志、统计等。</li>
<li><strong>setCorePoolSize</strong> / <strong>setMaximumPoolSize</strong>：运行时可动态调整（需注意与当前队列、线程数的关系）。</li>
</ul>
<hr>
<h2 id="九-forkjoinpool-与-scheduledexecutorservice"><a class="markdownIt-Anchor" href="#九-forkjoinpool-与-scheduledexecutorservice"></a> 九、ForkJoinPool 与 ScheduledExecutorService</h2>
<h3 id="91-forkjoinpool"><a class="markdownIt-Anchor" href="#91-forkjoinpool"></a> 9.1 ForkJoinPool</h3>
<ul>
<li>面向<strong>分治、可递归拆分的任务</strong>（如大数组并行计算），使用 <strong>work-stealing</strong>：空闲线程从其它线程的队列尾部 “偷” 任务。</li>
<li><strong>ForkJoinPool.commonPool()</strong>：JVM 内共享的通用池，默认线程数约 CPU 核数 - 1；<strong>CompletableFuture</strong> 默认使用该池，不适合大量 I / O 或阻塞任务，建议显式传入自定义 Executor。</li>
</ul>
<h3 id="92-scheduledexecutorservice"><a class="markdownIt-Anchor" href="#92-scheduledexecutorservice"></a> 9.2 ScheduledExecutorService</h3>
<ul>
<li><strong>schedule(command, delay, unit)</strong>：延迟一次执行。</li>
<li><strong>scheduleAtFixedRate</strong> / <strong>scheduleWithFixedDelay</strong>：周期执行。</li>
<li>实现类 <strong>ScheduledThreadPoolExecutor</strong> 继承 ThreadPoolExecutor，使用 <strong>DelayedWorkQueue</strong>；核心线程数可设为 0 或较小，一般不建议对调度池开启 allowCoreThreadTimeOut，以免无线程可用。</li>
</ul>
<hr>
<h2 id="十-completablefuture-与线程池"><a class="markdownIt-Anchor" href="#十-completablefuture-与线程池"></a> 十、CompletableFuture 与线程池</h2>
<ul>
<li><strong>CompletableFuture.supplyAsync(supplier)</strong> / <strong>runAsync(runnable)</strong> 等<strong>不传 Executor</strong> 时，使用 <strong>ForkJoinPool.commonPool()</strong>。</li>
<li>commonPool 线程数少，且针对 CPU 密集型设计；<strong>I / O 多或阻塞多</strong>时易成为瓶颈，甚至低核数机器上退化为 “每任务一线程”。</li>
<li><strong>建议</strong>：<strong>始终传入业务专用线程池</strong>，例如 <code>CompletableFuture.supplyAsync(supplier, customExecutor)</code>，且 thenApplyAsync 等也尽量传同一 Executor，避免混用 commonPool。</li>
</ul>
<hr>
<h2 id="十一-java-9-及以后对线程池相关-api-的改动"><a class="markdownIt-Anchor" href="#十一-java-9-及以后对线程池相关-api-的改动"></a> 十一、Java 9 及以后对线程池相关 API 的改动</h2>
<h3 id="111-completablefuture-增强java-9"><a class="markdownIt-Anchor" href="#111-completablefuture-增强java-9"></a> 11.1 CompletableFuture 增强（Java 9）</h3>
<ul>
<li><strong>completeOnTimeout(value, timeout, unit)</strong>：超时未完成则用给定值完成。</li>
<li><strong>orTimeout(timeout, unit)</strong>：超时未完成则以 <strong>TimeoutException</strong> 异常完成。</li>
<li><strong>defaultExecutor()</strong>：子类可重写，自定义默认执行器，不必每次传 Executor。</li>
<li><strong>newIncompleteFuture()</strong>：工厂方法，便于子类扩展。</li>
<li><strong>completedStage()</strong> / <strong>failedStage()</strong> / <strong>failedFuture()</strong> 等：创建已完成或已失败的 CompletableFuture / CompletionStage。</li>
</ul>
<p>这些改动不改变 “线程池” 本身，但让异步任务与超时、默认 Executor 的配合更灵活。</p>
<h3 id="112-虚拟线程与-executorjava-21"><a class="markdownIt-Anchor" href="#112-虚拟线程与-executorjava-21"></a> 11.2 虚拟线程与 Executor（Java 21）</h3>
<ul>
<li><strong>Executors.newVirtualThreadPerTaskExecutor()</strong>：为<strong>每个任务</strong>创建一个<strong>虚拟线程</strong>，任务结束后虚拟线程可被回收；适合 “一任务一线程” 的模型，无需再维护传统线程池的队列与最大线程数上限。</li>
<li><strong>Executors.newThreadPerTaskExecutor(ThreadFactory)</strong>：按给定 <strong>ThreadFactory</strong> 为每个任务创建新线程；若传入 <strong>Thread.ofVirtual().factory()</strong>，效果与上面类似（虚拟线程）。</li>
<li>虚拟线程由 JVM 调度，可创建大量虚拟线程而只占少量平台线程，适合高并发 I / O；<strong>现有 ExecutorService 接口不变</strong>，只是实现从 “固定线程 + 队列” 变为 “每任务一虚拟线程”。</li>
</ul>
<p>示例：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> (<span class="type">var</span> <span class="variable">executor</span> <span class="operator">=</span> Executors.newVirtualThreadPerTaskExecutor()) {</span><br><span class="line">    IntStream.range(<span class="number">0</span>, <span class="number">10_000</span>).forEach(i -&gt;</span><br><span class="line">        executor.submit(() -&gt; {</span><br><span class="line">            Thread.sleep(Duration.ofSeconds(<span class="number">1</span>));</span><br><span class="line">            <span class="keyword">return</span> i;</span><br><span class="line">        })</span><br><span class="line">    );</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="113-其它版本"><a class="markdownIt-Anchor" href="#113-其它版本"></a> 11.3 其它版本</h3>
<ul>
<li><strong>Java 8</strong>：Lambda、CompletableFuture 的引入使 “异步 + 线程池” 成为标配。</li>
<li><strong>Java 9+</strong>：模块化后 <code>java.util.concurrent</code> 仍在 java.base，无 API 破坏性变更；线程池本身行为稳定，改动主要集中在 CompletableFuture（9）和虚拟线程 Executor（21）。</li>
</ul>
<hr>
<h2 id="十二-小结"><a class="markdownIt-Anchor" href="#十二-小结"></a> 十二、小结</h2>
<table>
<thead>
<tr>
<th>主题</th>
<th>要点</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>体系</strong></td>
<td>Executor → ExecutorService → ThreadPoolExecutor / ScheduledThreadPoolExecutor / ForkJoinPool</td>
</tr>
<tr>
<td><strong>核心参数</strong></td>
<td>corePoolSize、maximumPoolSize、keepAliveTime、workQueue、threadFactory、handler</td>
</tr>
<tr>
<td><strong>流程</strong></td>
<td>先占满核心线程 → 再入队 → 再开非核心线程 → 再拒绝</td>
</tr>
<tr>
<td><strong>队列</strong></td>
<td>无界队列易 OOM；有界队列 + 合理 maximum + 拒绝策略更稳妥</td>
</tr>
<tr>
<td><strong>拒绝策略</strong></td>
<td>AbortPolicy、CallerRunsPolicy、DiscardPolicy、DiscardOldestPolicy 及自定义</td>
</tr>
<tr>
<td><strong>关闭</strong></td>
<td>shutdown + awaitTermination，必要时 shutdownNow</td>
</tr>
<tr>
<td><strong>CompletableFuture</strong></td>
<td>务必传自定义 Executor，避免 commonPool；Java 9 增加超时与默认 Executor 扩展</td>
</tr>
<tr>
<td><strong>Java 21</strong></td>
<td>newVirtualThreadPerTaskExecutor、newThreadPerTaskExecutor (ThreadFactory) 支持虚拟线程，适合高并发 I / O 场景</td>
</tr>
</tbody>
</table>
<p>掌握以上内容即可覆盖日常开发与面试中对 Java 线程池的要求，并在 Java 9+ 中正确使用 CompletableFuture 与虚拟线程 Executor。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>并发</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>虚拟线程</tag>
        <tag>线程池</tag>
        <tag>ThreadPoolExecutor</tag>
        <tag>Executor</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 11 新特性总结与示例</title>
    <url>/2025/03/01/2025-03-02-java11-features-summary/</url>
    <content><![CDATA[<h1 id="java-11-新特性总结与示例"><a class="markdownIt-Anchor" href="#java-11-新特性总结与示例"></a> Java 11 新特性总结与示例</h1>
<p>Java 11 于 <strong>2018 年 9 月 25 日</strong> 正式发布，是 <strong>Java 8 之后首个长期支持（LTS）版本</strong>，支持至 2026 年 9 月。本文按语言、API、工具、GC 等分类梳理主要新特性，并配有示例（基于 JDK 11）。</p>
<hr>
<h2 id="一-语言层面"><a class="markdownIt-Anchor" href="#一-语言层面"></a> 一、语言层面</h2>
<h3 id="11-lambda-参数支持-varjep-323"><a class="markdownIt-Anchor" href="#11-lambda-参数支持-varjep-323"></a> 1.1 Lambda 参数支持 var（JEP 323）</h3>
<p>在 Lambda 形参中可以使用 <code>var</code>，便于添加注解或统一风格（注意：Java 10 已在局部变量中引入 var，Java 11 扩展到 Lambda 参数）。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.function.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">VarInLambda</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="comment">// Lambda 参数可使用 var，便于加注解或统一风格</span></span><br><span class="line">        BiFunction&lt;Integer, Integer, Integer&gt; add = (<span class="keyword">var</span> a, <span class="keyword">var</span> b) -&gt; a + b;</span><br><span class="line">        System.out.println(add.apply(<span class="number">1</span>, <span class="number">2</span>));  <span class="comment">// 3</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="12-嵌套类访问控制jep-181"><a class="markdownIt-Anchor" href="#12-嵌套类访问控制jep-181"></a> 1.2 嵌套类访问控制（JEP 181）</h3>
<p><code>Class</code> 新增 <code>getNestHost()</code>、<code>getNestMembers()</code> 等方法，嵌套类（内部类等）的访问控制语义更清晰，反射可正确识别 “巢” 关系。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">NestDemo</span> {</span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">Inner</span> { }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        Class&lt;?&gt; host = NestDemo.Inner.class.getNestHost();</span><br><span class="line">        System.out.println(host.getName());  <span class="comment">// NestDemo</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="二-string-增强"><a class="markdownIt-Anchor" href="#二-string-增强"></a> 二、String 增强</h2>
<p>新增方法均针对 “空白” 和 “首尾空格” 的现代需求（比 <code>trim()</code> 正确处理 Unicode 等）。</p>
<table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>isBlank()</code></td>
<td>是否为空或仅包含空白字符</td>
</tr>
<tr>
<td><code>strip()</code> / <code>stripLeading()</code> / <code>stripTrailing()</code></td>
<td>去除首尾/仅首/仅尾空白</td>
</tr>
<tr>
<td><code>repeat(int n)</code></td>
<td>将字符串重复 n 次</td>
</tr>
<tr>
<td><code>lines()</code></td>
<td>按行拆分为 <code>Stream&lt;String&gt;</code></td>
</tr>
</tbody>
</table>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StringDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="type">String</span> <span class="variable">s</span> <span class="operator">=</span> <span class="string">"  hello  "</span>;</span><br><span class="line">        System.out.println(s.isBlank());       <span class="comment">// false</span></span><br><span class="line">        System.out.println(<span class="string">"   "</span>.isBlank());   <span class="comment">// true</span></span><br><span class="line">        System.out.println(s.strip());         <span class="comment">// "hello"</span></span><br><span class="line">        System.out.println(<span class="string">"ab"</span>.repeat(<span class="number">3</span>));    <span class="comment">// "ababab"</span></span><br><span class="line"></span><br><span class="line">        <span class="string">"a\nb\nc"</span>.lines().forEach(System.out::println);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-集合不可变工厂"><a class="markdownIt-Anchor" href="#三-集合不可变工厂"></a> 三、集合：不可变工厂</h2>
<p><code>List</code>、<code>Set</code>、<code>Map</code> 提供 <code>of(...)</code> 和 <code>copyOf(...)</code>，创建不可变集合，避免手写 <code>Collections.unmodifiableXXX</code>。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CollectionDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        List&lt;String&gt; list = List.of(<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>);</span><br><span class="line">        Set&lt;Integer&gt; set = Set.of(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line">        Map&lt;String, Integer&gt; map = Map.of(<span class="string">"x"</span>, <span class="number">1</span>, <span class="string">"y"</span>, <span class="number">2</span>);</span><br><span class="line">        Map&lt;String, Integer&gt; map2 = Map.ofEntries(</span><br><span class="line">            Map.entry(<span class="string">"a"</span>, <span class="number">1</span>),</span><br><span class="line">            Map.entry(<span class="string">"b"</span>, <span class="number">2</span>)</span><br><span class="line">        );</span><br><span class="line"></span><br><span class="line">        List&lt;String&gt; copy = List.copyOf(list);</span><br><span class="line">        <span class="comment">// list.add("d");  // UnsupportedOperationException</span></span><br><span class="line">        <span class="comment">// copy.add("d");  // UnsupportedOperationException</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="四-stream-optional-增强"><a class="markdownIt-Anchor" href="#四-stream-optional-增强"></a> 四、Stream / Optional 增强</h2>
<ul>
<li><strong>Stream</strong>：<code>ofNullable(T t)</code>（null 则空流）、<code>takeWhile</code>/<code>dropWhile</code>、<code>iterate</code> 重载（带 Predicate）。</li>
<li><strong>Optional</strong>：无新增重要方法（部分文档提到的 <code>isEmpty()</code> 在 Java 11 中为 Optional 的增强，实际在后续版本更常见；Optional 在 11 中已有 <code>isEmpty()</code> 的讨论，部分实现可能在 11 或 12）。</li>
</ul>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.stream.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StreamDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="comment">// ofNullable：null 得到空流</span></span><br><span class="line">        <span class="type">long</span> <span class="variable">c</span> <span class="operator">=</span> Stream.ofNullable(<span class="literal">null</span>).count();</span><br><span class="line">        System.out.println(c);  <span class="comment">// 0</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// takeWhile / dropWhile</span></span><br><span class="line">        Stream.of(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>)</span><br><span class="line">            .takeWhile(n -&gt; n &lt; <span class="number">4</span>)</span><br><span class="line">            .forEach(System.out::print);  <span class="comment">// 123</span></span><br><span class="line">        System.out.println();</span><br><span class="line">        Stream.of(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>)</span><br><span class="line">            .dropWhile(n -&gt; n &lt; <span class="number">4</span>)</span><br><span class="line">            .forEach(System.out::print);  <span class="comment">// 45</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="五-http-client-标准化jep-321"><a class="markdownIt-Anchor" href="#五-http-client-标准化jep-321"></a> 五、HTTP Client 标准化（JEP 321）</h2>
<p>从 Java 9 的孵化模块迁至 <strong><code>java.net.http</code></strong>，支持同步/异步、HTTP/2，可替代 <code>HttpURLConnection</code> 或部分第三方 HTTP 库。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.net.URI;</span><br><span class="line"><span class="keyword">import</span> java.net.http.*;</span><br><span class="line"><span class="keyword">import</span> java.net.http.HttpClient.Version;</span><br><span class="line"><span class="keyword">import</span> java.time.Duration;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HttpClientDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception {</span><br><span class="line">        <span class="type">HttpClient</span> <span class="variable">client</span> <span class="operator">=</span> HttpClient.newBuilder()</span><br><span class="line">            .connectTimeout(Duration.ofSeconds(<span class="number">10</span>))</span><br><span class="line">            .version(Version.HTTP_2)</span><br><span class="line">            .build();</span><br><span class="line"></span><br><span class="line">        <span class="type">HttpRequest</span> <span class="variable">request</span> <span class="operator">=</span> HttpRequest.newBuilder(URI.create(<span class="string">"https://example.com"</span>))</span><br><span class="line">            .timeout(Duration.ofSeconds(<span class="number">5</span>))</span><br><span class="line">            .GET()</span><br><span class="line">            .build();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 同步</span></span><br><span class="line">        HttpResponse&lt;String&gt; response = client.send(request, HttpResponse.BodyHandlers.ofString());</span><br><span class="line">        System.out.println(response.statusCode());</span><br><span class="line">        System.out.println(response.body().substring(<span class="number">0</span>, Math.min(<span class="number">100</span>, response.body().length())));</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 异步</span></span><br><span class="line">        client.sendAsync(request, HttpResponse.BodyHandlers.ofString())</span><br><span class="line">            .thenApply(HttpResponse::body)</span><br><span class="line">            .thenAccept(b -&gt; System.out.println(<span class="string">"Async body length: "</span> + b.length()));</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="六-单文件源码运行jep-330"><a class="markdownIt-Anchor" href="#六-单文件源码运行jep-330"></a> 六、单文件源码运行（JEP 330）</h2>
<p>无需先 <code>javac</code>，可直接运行单个 <code>.java</code> 文件，适合脚本式使用或入门教学。</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment"># 直接运行</span></span><br><span class="line">java Hello.java</span><br><span class="line"></span><br><span class="line"><span class="comment"># 带参数</span></span><br><span class="line">java --<span class="built_in">source</span> 11 Main.java arg1 arg2</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="七-gc-与运行时常量"><a class="markdownIt-Anchor" href="#七-gc-与运行时常量"></a> 七、GC 与运行时常量</h2>
<table>
<thead>
<tr>
<th>特性</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>ZGC（JEP 333，实验）</strong></td>
<td>可扩展低延迟 GC，目标暂停时间 &lt; 10ms</td>
</tr>
<tr>
<td><strong>Epsilon GC（JEP 318）</strong></td>
<td>无操作 GC，只分配不回收，用于性能测试等</td>
</tr>
<tr>
<td><strong>动态类文件常量（JEP 309）</strong></td>
<td>字节码新增 <code>CONSTANT_Dynamic</code></td>
</tr>
<tr>
<td><strong>移除 Java EE / CORBA 模块（JEP 320）</strong></td>
<td>正式移除，需要时改用 Jakarta EE 等</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="八-小结"><a class="markdownIt-Anchor" href="#八-小结"></a> 八、小结</h2>
<table>
<thead>
<tr>
<th>类别</th>
<th>特性</th>
<th>一句话</th>
</tr>
</thead>
<tbody>
<tr>
<td>语言</td>
<td>Lambda 参数 var、嵌套类</td>
<td>语法与反射增强</td>
</tr>
<tr>
<td>API</td>
<td>String / 集合 / Stream</td>
<td>不可变集合、String 工具、流式增强</td>
</tr>
<tr>
<td>网络</td>
<td>HTTP Client</td>
<td>标准库 HTTP / 2 同步与异步</td>
</tr>
<tr>
<td>工具</td>
<td>单文件运行</td>
<td><code>java xxx.java</code></td>
</tr>
<tr>
<td>GC / 平台</td>
<td>ZGC、Epsilon、移除 EE</td>
<td>低延迟 GC、瘦身 JDK</td>
</tr>
</tbody>
</table>
<p>以上示例均在 JDK 11 下可运行。Java 11 作为 LTS，是许多团队从 8 升级的首选版本。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>新版本特性</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Java11</tag>
      </tags>
  </entry>
  <entry>
    <title>VarHandle 详解与 Java 8 之后的发展</title>
    <url>/2025/03/01/2025-03-02-java-varhandle-and-post-jdk8/</url>
    <content><![CDATA[<h1 id="varhandle-详解与-java-8-之后的发展"><a class="markdownIt-Anchor" href="#varhandle-详解与-java-8-之后的发展"></a> VarHandle 详解与 Java 8 之后的发展</h1>
<p><strong>说明</strong>：<code>VarHandle</code> 在 <strong>Java 9</strong> 才通过 <strong>JEP 193</strong> 正式引入标准库。<strong>Java 8 及以前没有 <code>VarHandle</code></strong>；本文先讲 VarHandle 是什么、怎么用，再按时间线说明 <strong>Java 9 起</strong> 的能力扩展与在 JDK 内部的落地。</p>
<hr>
<h2 id="一-为什么会出现-varhandle"><a class="markdownIt-Anchor" href="#一-为什么会出现-varhandle"></a> 一、为什么会出现 VarHandle</h2>
<p>在 VarHandle 之前，若要对<strong>普通对象的字段</strong>或<strong>数组元素</strong>做带顺序语义的读 / 写、<code>compareAndSet</code>、<code>getAndAdd</code> 等操作，常见做法是：</p>
<ul>
<li>用 <strong><code>AtomicInteger</code></strong> 等专用原子类（多一层引用与间接访问），或</li>
<li>用 <strong><code>Atomic*FieldUpdater</code></strong>（写法和性能上往往不理想），或</li>
<li>使用 <strong><code>sun.misc.Unsafe</code></strong>（不安全、非标准、不可移植）。</li>
</ul>
<p><strong>JEP 193（Java 9）</strong> 引入 <strong><code>java.lang.invoke.VarHandle</code></strong>，目标包括：</p>
<ul>
<li><strong>安全</strong>：类型匹配、边界检查、<strong><code>final</code> 实例字段不可写</strong> 等规则与字节码一致，不会把 JVM 搞到不可靠状态。</li>
<li><strong>性能</strong>：意图与 <code>Unsafe</code> 类操作接近，HotSpot 可做 <strong>intrinsic</strong>，生成与 Unsafe 相近的机器码（在可行处）。</li>
<li><strong>可用性</strong>：API 比 Unsafe 清晰；统一的<strong>访问模式</strong>表达不同内存排序语义。</li>
</ul>
<hr>
<h2 id="二-varhandle-是什么"><a class="markdownIt-Anchor" href="#二-varhandle-是什么"></a> 二、VarHandle 是什么</h2>
<p><code>VarHandle</code> 是<strong>对某一 “变量位置” 的类型化引用</strong>：该位置可以是<strong>实例字段、静态字段、数组元素</strong>，以及 <code>ByteBuffer</code> 视图等（视 JDK 版本与支持类型而定）。</p>
<p>对变量的访问不直接暴露 “地址”，而是通过 <strong>签名多态（signature polymorphic）</strong> 的方法，例如：</p>
<ul>
<li><code>get</code> / <code>set</code>：普通（plain）读写。</li>
<li><code>getVolatile</code> / <code>setVolatile</code>：与 <code>volatile</code> 类似的可见性与有序性。</li>
<li><code>getAcquire</code> / <code>setRelease</code>：常用于与 release / acquire 配对。</li>
<li><code>compareAndSet</code>、<code>weakCompareAndSet</code>、<code>compareAndExchange</code>：CAS。</li>
<li><code>getAndSet</code>、<code>getAndAdd</code>：读 - 改 - 写。</li>
<li><strong>Java 11 起</strong>：<code>getAndBitwiseOr</code>、<code>getAndBitwiseAnd</code>、<code>getAndBitwiseXor</code> 及其 acquire / release 变体等。</li>
<li><strong>内存栅栏</strong>：<code>fullFence</code>、<code>acquireFence</code>、<code>releaseFence</code>、<code>loadLoadFence</code>、<code>storeStoreFence</code>（静态方法）。</li>
<li><strong>可达性</strong>：<code>reachabilityFence</code> 保证对象在语义上仍被强引用（与 GC 交互场景有用）。</li>
</ul>
<hr>
<h2 id="三-如何获取-varhandle"><a class="markdownIt-Anchor" href="#三-如何获取-varhandle"></a> 三、如何获取 VarHandle</h2>
<p>通常通过 <strong><code>MethodHandles.Lookup</code></strong>：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.invoke.MethodHandles;</span><br><span class="line"><span class="keyword">import</span> java.lang.invoke.VarHandle;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Demo</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">int</span> x;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> VarHandle X_HANDLE;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> {</span><br><span class="line">        <span class="keyword">try</span> {</span><br><span class="line">            X_HANDLE = MethodHandles.lookup()</span><br><span class="line">                .findVarHandle(Demo.class, <span class="string">"x"</span>, <span class="type">int</span>.class);</span><br><span class="line">        } <span class="keyword">catch</span> (ReflectiveOperationException e) {</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ExceptionInInitializerError</span>(e);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">casDemo</span><span class="params">()</span> {</span><br><span class="line">        <span class="comment">// 等价思想：原子地把 x 从 0 改成 1</span></span><br><span class="line">        <span class="type">boolean</span> <span class="variable">ok</span> <span class="operator">=</span> X_HANDLE.compareAndSet(<span class="built_in">this</span>, <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p><strong>注意</strong>：</p>
<ul>
<li><code>Lookup</code> 的访问规则与反射一致（模块导出/ <code>opens</code>、<strong>private</strong> 需 <code>privateLookupIn</code> 等）。</li>
<li>对 <strong>数组</strong> 用 <code>MethodHandles.arrayElementVarHandle(int[].class)</code> 等。</li>
</ul>
<hr>
<h2 id="四-访问模式与内存语义概念"><a class="markdownIt-Anchor" href="#四-访问模式与内存语义概念"></a> 四、访问模式与内存语义（概念）</h2>
<p>不同方法对应 JMM 中不同的读 / 写与同步效果，可粗分为：</p>
<table>
<thead>
<tr>
<th>类别</th>
<th>典型方法</th>
<th>含义（简化理解）</th>
</tr>
</thead>
<tbody>
<tr>
<td>Plain</td>
<td><code>get</code> / <code>set</code></td>
<td>无额外同步承诺，可被重排序优化</td>
</tr>
<tr>
<td>Volatile</td>
<td><code>getVolatile</code> / <code>setVolatile</code></td>
<td>与 volatile 字段类似的可见性与 hb 关系</td>
</tr>
<tr>
<td>Acquire/Release</td>
<td><code>getAcquire</code> / <code>setRelease</code></td>
<td>单侧排序，常用于与另一端的 release / acquire 配对</td>
</tr>
<tr>
<td>Opaque</td>
<td><code>getOpaque</code> / <code>setOpaque</code></td>
<td>可见性保证、排序弱于 volatile，具体见 Javadoc</td>
</tr>
<tr>
<td>RMW</td>
<td><code>compareAndSet</code>、<code>getAndAdd</code> 等</td>
<td>原子读 - 改 - 写，方法上注明各自的内存排序</td>
</tr>
</tbody>
</table>
<p>编写并发代码时应<strong>以 Javadoc 与 JLS 为准</strong>，本节只做导读。</p>
<hr>
<h2 id="五-java-8-之后的发展时间线"><a class="markdownIt-Anchor" href="#五-java-8-之后的发展时间线"></a> 五、Java 8 之后的发展时间线</h2>
<h3 id="51-java-8-及以前"><a class="markdownIt-Anchor" href="#51-java-8-及以前"></a> 5.1 Java 8 及以前</h3>
<ul>
<li><strong>无 <code>VarHandle</code></strong>。</li>
<li>并发原子操作用 <strong><code>java.util.concurrent.atomic</code></strong>、<strong>Unsafe</strong>（内部）、或 <strong><code>synchronized</code> / <code>volatile</code></strong>。</li>
</ul>
<h3 id="52-java-9jep-193varhandle-首次发布"><a class="markdownIt-Anchor" href="#52-java-9jep-193varhandle-首次发布"></a> 5.2 Java 9：JEP 193，VarHandle 首次发布</h3>
<ul>
<li><strong><code>java.lang.invoke.VarHandle</code></strong> 与配套 Lookup API。</li>
<li>覆盖实例 / 静态字段、数组元素等；提供上述多类访问模式与 fence、<code>reachabilityFence</code>。</li>
<li>为后续 <strong>JUC、JDK 内部逐步替换 Unsafe 字段访问</strong> 打下基础。</li>
</ul>
<h3 id="53-java-11位运算原子-api-内部广泛使用"><a class="markdownIt-Anchor" href="#53-java-11位运算原子-api-内部广泛使用"></a> 5.3 Java 11：位运算原子 API + 内部广泛使用</h3>
<ul>
<li>VarHandle 增加 <strong><code>getAndBitwiseOr</code> / <code>And</code> / <code>Xor</code></strong> 及带 <strong>Acquire/Release</strong> 语义的变体，以及按位相关的单比特操作等（具体以当前 JDK <code>VarHandle</code> Javadoc 列举为准），更贴近硬件上的原子位操作能力。</li>
<li><strong>AbstractQueuedSynchronizer（AQS）</strong> 等对<strong>同步状态</strong>的 CAS 等实现，迁移到以 <strong>VarHandle</strong> 实现原子更新（如 <strong>JDK-8149644</strong> 一类变更），减少对 <code>Unsafe</code> 的依赖，<strong>应用层 <code>ReentrantLock</code> 等 API 不变</strong>。</li>
</ul>
<h3 id="54-java-12-及以后"><a class="markdownIt-Anchor" href="#54-java-12-及以后"></a> 5.4 Java 12 及以后</h3>
<ul>
<li><code>VarHandle</code> 作为稳定 API，随版本补充文档与小改进；<strong>核心模型仍是 JEP 193 + Java 11 的位运算扩展</strong>。</li>
<li>应用层若未使用新增方法，一般无需改代码。</li>
<li>与 <strong>Foreign Memory / Panama（如 <code>MemorySegment</code>）</strong> 等走的是另一套 API（不同 JEP），和 “堆内字段上的 VarHandle” 互补而不是替代关系。</li>
</ul>
<h3 id="55-小结表"><a class="markdownIt-Anchor" href="#55-小结表"></a> 5.5 小结表</h3>
<table>
<thead>
<tr>
<th>版本</th>
<th>与 VarHandle 相关的内容</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>≤ 8</strong></td>
<td>无 VarHandle</td>
</tr>
<tr>
<td><strong>9</strong></td>
<td>JEP 193：VarHandle 与全套基础访问模式</td>
</tr>
<tr>
<td><strong>11</strong></td>
<td>位运算类原子方法；AQS 等内部改用 VarHandle</td>
</tr>
<tr>
<td><strong>12+</strong></td>
<td>持续维护；与 Panama 等并存</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="六-简单示例getandadd"><a class="markdownIt-Anchor" href="#六-简单示例getandadd"></a> 六、简单示例：getAndAdd</h2>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.invoke.MethodHandles;</span><br><span class="line"><span class="keyword">import</span> java.lang.invoke.VarHandle;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CounterHolder</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">long</span> counter;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> VarHandle COUNTER;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> {</span><br><span class="line">        <span class="keyword">try</span> {</span><br><span class="line">            COUNTER = MethodHandles.lookup()</span><br><span class="line">                .findVarHandle(CounterHolder.class, <span class="string">"counter"</span>, <span class="type">long</span>.class);</span><br><span class="line">        } <span class="keyword">catch</span> (ReflectiveOperationException e) {</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ExceptionInInitializerError</span>(e);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">long</span> <span class="title function_">increment</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">return</span> (<span class="type">long</span>) COUNTER.getAndAdd(<span class="built_in">this</span>, <span class="number">1L</span>);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>（生产环境可继续优先用 <strong><code>AtomicLong</code></strong>；VarHandle 更适合<strong>自定义数据结构</strong>或<strong>与现有字段布局强绑定</strong>的场景。）</p>
<hr>
<h2 id="七-何时用-varhandle"><a class="markdownIt-Anchor" href="#七-何时用-varhandle"></a> 七、何时用 VarHandle</h2>
<table>
<thead>
<tr>
<th>更适合 VarHandle</th>
<th>更适合 Atomic / synchronized</th>
</tr>
</thead>
<tbody>
<tr>
<td>需要对<strong>现有类字段</strong>做 CAS / 有序访问且不想单独包一层原子类</td>
<td>单一计数器、累加器等，<strong>Atomic*</strong> 更直观</td>
</tr>
<tr>
<td>实现<strong>无锁数据结构</strong>、与内存排序精细控制</td>
<td>普通互斥，<strong><code>synchronized</code></strong> 或 <strong>Lock</strong> 足够</td>
</tr>
<tr>
<td>库 / 框架作者，依赖 <strong>标准 API</strong> 替代 Unsafe</td>
<td>业务代码很少需要直接碰 VarHandle</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="八-小结"><a class="markdownIt-Anchor" href="#八-小结"></a> 八、小结</h2>
<ul>
<li><strong>VarHandle 自 Java 9 起存在</strong>，不是 Java 8 的 API；Java 8 之后的发展即 <strong>9 起引入、11 增强位运算与 JDK 内部迁移</strong>。</li>
<li>它提供<strong>类型安全、有 JMM 语义说明</strong>的字段 / 元素原子与有序访问，是 <strong><code>Unsafe</code> 字段操作</strong> 的标准替代方向之一。</li>
<li><strong>Java 11</strong> 在 VarHandle 上扩展了<strong>按位原子</strong>能力，并推动 <strong>AQS</strong> 等核心类用 VarHandle 实现状态更新；之后版本以<strong>稳定演进</strong>为主。</li>
<li>日常业务优先 <strong><code>java.util.concurrent</code></strong> 与 <strong><code>synchronized</code></strong>；<strong>VarHandle</strong> 面向需要<strong>精细内存语义</strong>或 <strong>JDK / 框架级</strong> 实现的场景。</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
        <category>并发</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>并发</tag>
        <tag>VarHandle</tag>
        <tag>内存模型</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 21 新特性总结与示例</title>
    <url>/2025/03/01/2025-03-02-java21-features-summary/</url>
    <content><![CDATA[<h1 id="java-21-新特性总结与示例"><a class="markdownIt-Anchor" href="#java-21-新特性总结与示例"></a> Java 21 新特性总结与示例</h1>
<p>Java 21 于 <strong>2023 年 9 月 19 日</strong> 正式发布，是 <strong>当前最新的 LTS（长期支持）版本</strong>，支持至 2031 年 9 月。本文按语言、API、并发、GC 等分类梳理主要新特性，并配有示例（基于 JDK 21）。</p>
<hr>
<h2 id="一-虚拟线程virtual-threadsjep-444"><a class="markdownIt-Anchor" href="#一-虚拟线程virtual-threadsjep-444"></a> 一、虚拟线程（Virtual Threads，JEP 444）</h2>
<p><strong>作用</strong>：由 JVM 管理的轻量级线程，用少量 OS 线程承载大量虚拟线程，适合 “一请求一线程” 的高吞吐场景，无需改业务逻辑，只需改变线程创建方式。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.concurrent.*;</span><br><span class="line"><span class="keyword">import</span> java.util.stream.IntStream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">VirtualThreadDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception {</span><br><span class="line">        <span class="comment">// 创建虚拟线程</span></span><br><span class="line">        <span class="type">Thread</span> <span class="variable">vt</span> <span class="operator">=</span> Thread.ofVirtual().start(() -&gt; System.out.println(<span class="string">"Virtual: "</span> + Thread.currentThread()));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> (<span class="type">var</span> <span class="variable">executor</span> <span class="operator">=</span> Executors.newVirtualThreadPerTaskExecutor()) {</span><br><span class="line">            IntStream.range(<span class="number">0</span>, <span class="number">5</span>).forEach(i -&gt;</span><br><span class="line">                executor.submit(() -&gt; {</span><br><span class="line">                    System.out.println(Thread.currentThread() + <span class="string">" -&gt; "</span> + i);</span><br><span class="line">                    <span class="keyword">return</span> i;</span><br><span class="line">                })</span><br><span class="line">            );</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">        vt.join();</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>输出中可见线程名为 <code>VirtualThread[...]</code>，且可创建大量线程而不会像平台线程那样占满系统资源。</p>
<hr>
<h2 id="二-记录模式record-patternsjep-440"><a class="markdownIt-Anchor" href="#二-记录模式record-patternsjep-440"></a> 二、记录模式（Record Patterns，JEP 440）</h2>
<p><strong>作用</strong>：在 <code>instanceof</code> 和 <code>switch</code> 中直接对 record 做模式匹配并解构出组件，避免先强转再 getter。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">record</span> <span class="title class_">Point</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> {}</span><br><span class="line"><span class="keyword">record</span> <span class="title class_">Line</span><span class="params">(Point p1, Point p2)</span> {}</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RecordPatternDemo</span> {</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">print</span><span class="params">(Object obj)</span> {</span><br><span class="line">        <span class="keyword">if</span> (obj <span class="keyword">instanceof</span> <span class="title function_">Point</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span>) {</span><br><span class="line">            System.out.println(<span class="string">"Point: "</span> + x + <span class="string">", "</span> + y);</span><br><span class="line">        } <span class="keyword">else</span> <span class="keyword">if</span> (obj <span class="keyword">instanceof</span> <span class="title function_">Line</span><span class="params">(Point(<span class="type">int</span> x1, <span class="type">int</span> y1)</span>, Point(<span class="type">int</span> x2, <span class="type">int</span> y2))) {</span><br><span class="line">            System.out.println(<span class="string">"Line: ("</span> + x1 + <span class="string">","</span> + y1 + <span class="string">") -&gt; ("</span> + x2 + <span class="string">","</span> + y2 + <span class="string">")"</span>);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> String <span class="title function_">formatter</span><span class="params">(Object obj)</span> {</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">switch</span> (obj) {</span><br><span class="line">            <span class="keyword">case</span> <span class="title function_">Point</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> -&gt; <span class="string">"Point("</span> + x + <span class="string">","</span> + y + <span class="string">")"</span>;</span><br><span class="line">            <span class="keyword">case</span> <span class="title function_">Line</span><span class="params">(Point a, Point b)</span> -&gt; <span class="string">"Line("</span> + a + <span class="string">","</span> + b + <span class="string">")"</span>;</span><br><span class="line">            <span class="keyword">case</span> <span class="literal">null</span> -&gt; <span class="string">"null"</span>;</span><br><span class="line">            <span class="keyword">default</span> -&gt; obj.toString();</span><br><span class="line">        };</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        print(<span class="keyword">new</span> <span class="title class_">Point</span>(<span class="number">1</span>, <span class="number">2</span>));</span><br><span class="line">        print(<span class="keyword">new</span> <span class="title class_">Line</span>(<span class="keyword">new</span> <span class="title class_">Point</span>(<span class="number">0</span>, <span class="number">0</span>), <span class="keyword">new</span> <span class="title class_">Point</span>(<span class="number">1</span>, <span class="number">1</span>)));</span><br><span class="line">        System.out.println(formatter(<span class="keyword">new</span> <span class="title class_">Point</span>(<span class="number">3</span>, <span class="number">4</span>)));</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>需要补上 <code>import java.util.stream.IntStream;</code> 若使用上面 VirtualThreadDemo 里的 IntStream。</p>
<hr>
<h2 id="三-switch-模式匹配jep-441转正"><a class="markdownIt-Anchor" href="#三-switch-模式匹配jep-441转正"></a> 三、Switch 模式匹配（JEP 441，转正）</h2>
<p><strong>作用</strong>：与 Java 17 的预览一脉相承，在 switch 的 case 中使用类型模式、null 分支、守卫条件等，并支持穷尽性检查（尤其配合密封类）。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SwitchDemo</span> {</span><br><span class="line">    <span class="keyword">static</span> String <span class="title function_">describe</span><span class="params">(Object obj)</span> {</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">switch</span> (obj) {</span><br><span class="line">            <span class="keyword">case</span> String s <span class="keyword">when</span> s.length() &gt; <span class="number">5</span> -&gt; <span class="string">"long string: "</span> + s;</span><br><span class="line">            <span class="keyword">case</span> String s -&gt; <span class="string">"string: "</span> + s;</span><br><span class="line">            <span class="keyword">case</span> Integer i -&gt; <span class="string">"int: "</span> + i;</span><br><span class="line">            <span class="keyword">case</span> <span class="literal">null</span> -&gt; <span class="string">"null"</span>;</span><br><span class="line">            <span class="keyword">default</span> -&gt; <span class="string">"other: "</span> + obj.getClass().getSimpleName();</span><br><span class="line">        };</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        System.out.println(describe(<span class="string">"hello"</span>));</span><br><span class="line">        System.out.println(describe(<span class="string">"hello world"</span>));</span><br><span class="line">        System.out.println(describe(<span class="number">42</span>));</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="四-有序集合接口sequencedcollectionjep-431"><a class="markdownIt-Anchor" href="#四-有序集合接口sequencedcollectionjep-431"></a> 四、有序集合接口（SequencedCollection，JEP 431）</h2>
<p><strong>作用</strong>：为 “有顺序” 的集合定义统一接口，提供 <code>getFirst()</code>、<code>getLast()</code>、<code>reversed()</code>、<code>addFirst</code>/<code>addLast</code> 等，便于编写通用顺序敏感代码。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SequencedDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        SequencedCollection&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(List.of(<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>));</span><br><span class="line">        System.out.println(list.getFirst());   <span class="comment">// a</span></span><br><span class="line">        System.out.println(list.getLast());     <span class="comment">// c</span></span><br><span class="line">        System.out.println(list.reversed());   <span class="comment">// [c, b, a]</span></span><br><span class="line"></span><br><span class="line">        SequencedMap&lt;String, Integer&gt; map = <span class="keyword">new</span> <span class="title class_">LinkedHashMap</span>&lt;&gt;(Map.of(<span class="string">"x"</span>, <span class="number">1</span>, <span class="string">"y"</span>, <span class="number">2</span>));</span><br><span class="line">        System.out.println(map.firstEntry());  <span class="comment">// x=1</span></span><br><span class="line">        System.out.println(map.lastEntry());   <span class="comment">// y=2</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="五-未命名类与实例-main-方法jep-445预览"><a class="markdownIt-Anchor" href="#五-未命名类与实例-main-方法jep-445预览"></a> 五、未命名类与实例 main 方法（JEP 445，预览）</h2>
<p><strong>作用</strong>：允许没有显式类声明的单文件程序，以及实例方法形式的 <code>main</code>，降低入门门槛。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 无需 public class，可直接写顶层代码；main 也可以是实例方法</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">main</span><span class="params">()</span> {</span><br><span class="line">    System.out.println(<span class="string">"Hello, Java 21!"</span>);</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>需用 <code>java --enable-preview --source 21 UnnamedMain.java</code> 运行（文件名随意）。</p>
<hr>
<h2 id="六-作用域值scoped-valuesjep-446预览"><a class="markdownIt-Anchor" href="#六-作用域值scoped-valuesjep-446预览"></a> 六、作用域值（Scoped Values，JEP 446，预览）</h2>
<p><strong>作用</strong>：可替代部分 ThreadLocal 场景，在线程内不可变、可继承地传递上下文，更适合虚拟线程。</p>
<hr>
<h2 id="七-字符串模板jep-430预览"><a class="markdownIt-Anchor" href="#七-字符串模板jep-430预览"></a> 七、字符串模板（JEP 430，预览）</h2>
<p><strong>作用</strong>：简化字符串插值，使用 STR 等模板处理器。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 预览特性，需 --enable-preview --source 21</span></span><br><span class="line"><span class="comment">// String name = "Java";</span></span><br><span class="line"><span class="comment">// String msg = STR."Hello, \{name}!";</span></span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="八-其它重要-jep-概览"><a class="markdownIt-Anchor" href="#八-其它重要-jep-概览"></a> 八、其它重要 JEP 概览</h2>
<table>
<thead>
<tr>
<th>JEP</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>439</strong></td>
<td>分代式 ZGC，降低 GC 开销</td>
</tr>
<tr>
<td><strong>442</strong></td>
<td>外部函数与内存 API（第三次预览）</td>
</tr>
<tr>
<td><strong>443</strong></td>
<td>未命名模式与变量（预览）</td>
</tr>
<tr>
<td><strong>448</strong></td>
<td>向量 API（第六轮孵化）</td>
</tr>
<tr>
<td><strong>452</strong></td>
<td>密钥封装机制 API</td>
</tr>
<tr>
<td><strong>453</strong></td>
<td>结构化并发（预览）</td>
</tr>
<tr>
<td><strong>449</strong></td>
<td>弃用 Windows 32-bit x86 移植</td>
</tr>
<tr>
<td><strong>451</strong></td>
<td>准备禁用代理的动态加载（安全）</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="九-小结"><a class="markdownIt-Anchor" href="#九-小结"></a> 九、小结</h2>
<table>
<thead>
<tr>
<th>类别</th>
<th>特性</th>
<th>一句话</th>
</tr>
</thead>
<tbody>
<tr>
<td>并发</td>
<td>虚拟线程</td>
<td>轻量线程，一请求一线程模型友好</td>
</tr>
<tr>
<td>语言</td>
<td>记录模式</td>
<td>instanceof / switch 中解构 record</td>
</tr>
<tr>
<td>语言</td>
<td>Switch 模式匹配</td>
<td>case 类型模式、null、守卫，穷尽检查</td>
</tr>
<tr>
<td>API</td>
<td>有序集合</td>
<td>getFirst/getLast/reversed 等统一接口</td>
</tr>
<tr>
<td>语言 / 预览</td>
<td>未命名类与 main、作用域值、字符串模板</td>
<td>简化入门与上下文传递</td>
</tr>
<tr>
<td>GC / 安全</td>
<td>分代 ZGC、KEM、平台与代理</td>
<td>性能与安全增强</td>
</tr>
</tbody>
</table>
<p>以上示例在 JDK 21 下可运行（预览特性需加 <code>--enable-preview --source 21</code>）。Java 21 是当前首选 LTS，虚拟线程与模式匹配是升级的最大动力。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>新版本特性</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Java21</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 25 新特性总结与示例</title>
    <url>/2025/03/01/2025-03-02-java25-features-summary/</url>
    <content><![CDATA[<h1 id="java-25-新特性总结与示例"><a class="markdownIt-Anchor" href="#java-25-新特性总结与示例"></a> Java 25 新特性总结与示例</h1>
<p>Java 25 于 <strong>2025 年 9 月 16 日</strong> 正式发布，是 <strong>新的长期支持（LTS）版本</strong>。本文按语言、API、并发、GC 与平台等分类梳理主要新特性，并配有示例（基于 JDK 25）。</p>
<hr>
<h2 id="一-语言层面"><a class="markdownIt-Anchor" href="#一-语言层面"></a> 一、语言层面</h2>
<h3 id="11-模式中的基本类型jep-507第三轮预览"><a class="markdownIt-Anchor" href="#11-模式中的基本类型jep-507第三轮预览"></a> 1.1 模式中的基本类型（JEP 507，第三轮预览）</h3>
<p><strong>作用</strong>：在 <code>instanceof</code> 和 <code>switch</code> 的模式中直接支持基本类型，避免不必要的装箱 / 拆箱。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PrimitivePatternDemo</span> {</span><br><span class="line">    <span class="keyword">static</span> String <span class="title function_">describe</span><span class="params">(Object obj)</span> {</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">switch</span> (obj) {</span><br><span class="line">            <span class="keyword">case</span> <span class="type">int</span> i -&gt; <span class="string">"int: "</span> + i;</span><br><span class="line">            <span class="keyword">case</span> <span class="type">long</span> l -&gt; <span class="string">"long: "</span> + l;</span><br><span class="line">            <span class="keyword">case</span> <span class="type">double</span> d -&gt; <span class="string">"double: "</span> + d;</span><br><span class="line">            <span class="keyword">case</span> <span class="type">boolean</span> b -&gt; <span class="string">"boolean: "</span> + b;</span><br><span class="line">            <span class="keyword">case</span> <span class="literal">null</span> -&gt; <span class="string">"null"</span>;</span><br><span class="line">            <span class="keyword">default</span> -&gt; <span class="string">"other: "</span> + obj.getClass().getSimpleName();</span><br><span class="line">        };</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        System.out.println(describe(<span class="number">42</span>));</span><br><span class="line">        System.out.println(describe(<span class="number">3.14</span>));</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>（实际语法以 JDK 25 最终规范为准，可能需 <code>--enable-preview</code>。）</p>
<h3 id="12-模块导入声明jep-511预览"><a class="markdownIt-Anchor" href="#12-模块导入声明jep-511预览"></a> 1.2 模块导入声明（JEP 511，预览）</h3>
<p><strong>作用</strong>：使用 <code>import module</code> 一次性导入某模块导出的所有包，简化模块化库的复用。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 预览特性示例（语法以最终规范为准）</span></span><br><span class="line"><span class="comment">// import module java.sql;</span></span><br></pre></td></tr></tbody></table></figure>
<h3 id="13-紧凑源文件与实例-main-方法jep-512转正"><a class="markdownIt-Anchor" href="#13-紧凑源文件与实例-main-方法jep-512转正"></a> 1.3 紧凑源文件与实例 main 方法（JEP 512，转正）</h3>
<p><strong>作用</strong>：无显式类声明的顶层代码、实例 main 方法成为正式特性；<code>IO</code> 类用于控制台 I / O，位于 <code>java.lang</code> 并隐式导入，便于入门与脚本式程序。</p>
<h3 id="14-灵活的构造器体jep-513转正"><a class="markdownIt-Anchor" href="#14-灵活的构造器体jep-513转正"></a> 1.4 灵活的构造器体（JEP 513，转正）</h3>
<p><strong>作用</strong>：允许在显式 <code>super()</code> 或 <code>this()</code> 调用之前执行语句，放宽构造器体的书写顺序，便于做参数校验或辅助计算后再调用父类或本类构造器。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FlexibleConstructorDemo</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">int</span> value;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">FlexibleConstructorDemo</span><span class="params">(<span class="type">int</span> value)</span> {</span><br><span class="line">        <span class="comment">// 可在 super()/this() 前执行逻辑</span></span><br><span class="line">        <span class="keyword">if</span> (value &lt; <span class="number">0</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">"value &gt;= 0"</span>);</span><br><span class="line">        <span class="built_in">this</span>.value = value;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="二-api-与并发"><a class="markdownIt-Anchor" href="#二-api-与并发"></a> 二、API 与并发</h2>
<h3 id="21-作用域值scoped-valuesjep-506转正"><a class="markdownIt-Anchor" href="#21-作用域值scoped-valuesjep-506转正"></a> 2.1 作用域值（Scoped Values，JEP 506，转正）</h3>
<p><strong>作用</strong>：线程内不可变的、可继承的上下文传递，替代部分 ThreadLocal 场景，更适合虚拟线程。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ScopedValueDemo</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> ScopedValue&lt;String&gt; USER = ScopedValue.newInstance();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception {</span><br><span class="line">        ScopedValue.where(USER, <span class="string">"alice"</span>).run(() -&gt; {</span><br><span class="line">            System.out.println(USER.get());  <span class="comment">// alice</span></span><br><span class="line">        });</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="22-密钥派生函数-apijep-510转正"><a class="markdownIt-Anchor" href="#22-密钥派生函数-apijep-510转正"></a> 2.2 密钥派生函数 API（JEP 510，转正）</h3>
<p><strong>作用</strong>：提供标准化的密钥派生（KDF）API，用于密钥拉伸、从密码派生密钥等密码学场景。</p>
<h3 id="23-结构化并发jep-505第五轮预览"><a class="markdownIt-Anchor" href="#23-结构化并发jep-505第五轮预览"></a> 2.3 结构化并发（JEP 505，第五轮预览）</h3>
<p><strong>作用</strong>：用结构化方式管理并发任务的生命周期和异常传播，与虚拟线程配合使用，代码更清晰。</p>
<hr>
<h2 id="三-向量-apijep-508第十轮孵化"><a class="markdownIt-Anchor" href="#三-向量-apijep-508第十轮孵化"></a> 三、向量 API（JEP 508，第十轮孵化）</h2>
<p><strong>作用</strong>：与平台无关的 SIMD 风格向量运算 API，便于利用 CPU 向量指令做高性能计算（仍为孵化 API，包名可能含 <code>incubator</code>）。</p>
<hr>
<h2 id="四-gc-与运行时"><a class="markdownIt-Anchor" href="#四-gc-与运行时"></a> 四、GC 与运行时</h2>
<table>
<thead>
<tr>
<th>JEP</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>521</strong></td>
<td>分代式 Shenandoah（转正），优化 GC 行为</td>
</tr>
<tr>
<td><strong>519</strong></td>
<td>紧凑对象头（转正），减少对象内存占用</td>
</tr>
<tr>
<td><strong>520</strong></td>
<td>JFR 方法计时与追踪（转正），增强 profiling 能力</td>
</tr>
<tr>
<td><strong>503</strong></td>
<td>移除 32-bit x86 移植，平台瘦身</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="五-小结"><a class="markdownIt-Anchor" href="#五-小结"></a> 五、小结</h2>
<table>
<thead>
<tr>
<th>类别</th>
<th>特性</th>
<th>一句话</th>
</tr>
</thead>
<tbody>
<tr>
<td>语言</td>
<td>模式中的基本类型</td>
<td>instanceof / switch 直接匹配基本类型</td>
</tr>
<tr>
<td>语言</td>
<td>模块导入、紧凑源文件、灵活构造器</td>
<td>模块化与语法便利</td>
</tr>
<tr>
<td>并发 / API</td>
<td>作用域值、结构化并发</td>
<td>上下文传递与并发结构规范化</td>
</tr>
<tr>
<td>安全 / API</td>
<td>密钥派生函数</td>
<td>标准化 KDF</td>
</tr>
<tr>
<td>性能 / 平台</td>
<td>向量 API、分代 Shenandoah、紧凑对象头、JFR、移除 32-bit</td>
<td>性能与可观测性、平台精简</td>
</tr>
</tbody>
</table>
<p>以上示例在 JDK 25 下可运行（预览 / 孵化特性需相应编译与运行参数）。Java 25 作为新 LTS，适合作为新一轮升级与长期支持的目标版本。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>新版本特性</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Java25</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 17 新特性总结与示例</title>
    <url>/2025/03/01/2025-03-02-java17-features-summary/</url>
    <content><![CDATA[<h1 id="java-17-新特性总结与示例"><a class="markdownIt-Anchor" href="#java-17-新特性总结与示例"></a> Java 17 新特性总结与示例</h1>
<p>Java 17 于 <strong>2021 年 9 月 14 日</strong> 正式发布，是 <strong>继 Java 8 以来重要的长期支持（LTS）版本</strong>，支持至 2029 年 9 月。Spring 6.x / Spring Boot 3.x 的最低支持版本即为 Java 17。本文按语言、API、平台等分类梳理主要新特性，并配有示例（基于 JDK 17）。</p>
<hr>
<h2 id="一-密封类sealed-classesjep-409"><a class="markdownIt-Anchor" href="#一-密封类sealed-classesjep-409"></a> 一、密封类（Sealed Classes，JEP 409）</h2>
<p><strong>作用</strong>：用 <code>sealed</code> + <code>permits</code> 限制 “谁可以继承 / 实现” 当前类或接口，编译器可做穷尽性检查，提高类型安全。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 密封接口：只允许 Circle、Square、Rectangle 实现</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">sealed</span> <span class="keyword">interface</span> <span class="title class_">Shape</span> <span class="keyword">permits</span> Circle, Square, Rectangle {</span><br><span class="line">    <span class="type">double</span> <span class="title function_">area</span><span class="params">()</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">Circle</span> <span class="keyword">implements</span> <span class="title class_">Shape</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">double</span> r;</span><br><span class="line">    Circle(<span class="type">double</span> r) { <span class="built_in">this</span>.r = r; }</span><br><span class="line">    <span class="meta">@Override</span> <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">area</span><span class="params">()</span> { <span class="keyword">return</span> Math.PI * r * r; }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">Square</span> <span class="keyword">implements</span> <span class="title class_">Shape</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">double</span> side;</span><br><span class="line">    Square(<span class="type">double</span> side) { <span class="built_in">this</span>.side = side; }</span><br><span class="line">    <span class="meta">@Override</span> <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">area</span><span class="params">()</span> { <span class="keyword">return</span> side * side; }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">Rectangle</span> <span class="keyword">implements</span> <span class="title class_">Shape</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">double</span> w, h;</span><br><span class="line">    Rectangle(<span class="type">double</span> w, <span class="type">double</span> h) { <span class="built_in">this</span>.w = w; <span class="built_in">this</span>.h = h; }</span><br><span class="line">    <span class="meta">@Override</span> <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">area</span><span class="params">()</span> { <span class="keyword">return</span> w * h; }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>子类必须为 <code>final</code>、<code>sealed</code> 或 <code>non-sealed</code> 之一。与 switch 模式匹配结合时，编译器可检查是否覆盖所有 permitted 类型，无需 default。</p>
<hr>
<h2 id="二-switch-模式匹配jep-406预览-后续版本转正"><a class="markdownIt-Anchor" href="#二-switch-模式匹配jep-406预览-后续版本转正"></a> 二、Switch 模式匹配（JEP 406，预览 → 后续版本转正）</h2>
<p><strong>作用</strong>：在 switch 的 case 中使用类型模式，并可直接绑定变量，简化 “按类型分派” 的代码。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SwitchPatternDemo</span> {</span><br><span class="line">    <span class="keyword">static</span> String <span class="title function_">formatter</span><span class="params">(Object obj)</span> {</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">switch</span> (obj) {</span><br><span class="line">            <span class="keyword">case</span> Integer i -&gt; <span class="string">"int: "</span> + i;</span><br><span class="line">            <span class="keyword">case</span> Long l    -&gt; <span class="string">"long: "</span> + l;</span><br><span class="line">            <span class="keyword">case</span> Double d -&gt; <span class="string">"double: "</span> + d;</span><br><span class="line">            <span class="keyword">case</span> String s  -&gt; <span class="string">"string: "</span> + s;</span><br><span class="line">            <span class="keyword">case</span> <span class="literal">null</span>      -&gt; <span class="string">"null"</span>;</span><br><span class="line">            <span class="keyword">default</span>        -&gt; obj.getClass().getSimpleName();</span><br><span class="line">        };</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        System.out.println(formatter(<span class="number">1</span>));       <span class="comment">// int: 1</span></span><br><span class="line">        System.out.println(formatter(<span class="string">"hi"</span>));    <span class="comment">// string: hi</span></span><br><span class="line">        System.out.println(formatter(<span class="literal">null</span>));   <span class="comment">// null</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<p>与密封类结合时，对密封类型的 switch 可做穷尽性检查，无需 default。</p>
<hr>
<h2 id="三-强封装-jdk-内部-apijep-403"><a class="markdownIt-Anchor" href="#三-强封装-jdk-内部-apijep-403"></a> 三、强封装 JDK 内部 API（JEP 403）</h2>
<p>默认不再允许反射访问 JDK 内部 API（如 <code>sun.*</code>），除非使用 <code>--add-opens</code> 等打开模块。目的是提高稳定性和安全性，避免依赖未承诺的内部实现。</p>
<hr>
<h2 id="四-其它重要-jep-概览"><a class="markdownIt-Anchor" href="#四-其它重要-jep-概览"></a> 四、其它重要 JEP 概览</h2>
<table>
<thead>
<tr>
<th>JEP</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>410</strong></td>
<td>移除实验性 AOT / JIT 编译器</td>
</tr>
<tr>
<td><strong>411</strong></td>
<td>弃用安全管理器（计划删除）</td>
</tr>
<tr>
<td><strong>407</strong></td>
<td>移除 RMI 激活机制</td>
</tr>
<tr>
<td><strong>398</strong></td>
<td>弃用 Applet API</td>
</tr>
<tr>
<td><strong>382</strong></td>
<td>新 macOS 渲染管道</td>
</tr>
<tr>
<td><strong>391</strong></td>
<td>macOS / AArch64（Apple Silicon）支持</td>
</tr>
<tr>
<td><strong>306</strong></td>
<td>恢复 “始终严格” 的浮点语义</td>
</tr>
<tr>
<td><strong>415</strong></td>
<td>上下文反序列化过滤器（安全）</td>
</tr>
<tr>
<td><strong>412</strong></td>
<td>外部函数与内存 API（孵化）</td>
</tr>
<tr>
<td><strong>414</strong></td>
<td>向量 API（第二轮孵化）</td>
</tr>
<tr>
<td><strong>356</strong></td>
<td>增强的伪随机数生成器</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="五-伪随机数生成器jep-356"><a class="markdownIt-Anchor" href="#五-伪随机数生成器jep-356"></a> 五、伪随机数生成器（JEP 356）</h2>
<p>新 API 位于 <code>java.util.random</code>，提供 <code>RandomGenerator</code> 接口及多种算法实现（如 <code>L32X64MixRandom</code>），便于在测试与生产中选择可复现或高性能的随机源。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.random.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RandomDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="type">RandomGenerator</span> <span class="variable">gen</span> <span class="operator">=</span> RandomGenerator.of(<span class="string">"L32X64MixRandom"</span>);</span><br><span class="line">        System.out.println(gen.nextInt(<span class="number">100</span>));</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="六-小结"><a class="markdownIt-Anchor" href="#六-小结"></a> 六、小结</h2>
<table>
<thead>
<tr>
<th>类别</th>
<th>特性</th>
<th>一句话</th>
</tr>
</thead>
<tbody>
<tr>
<td>语言</td>
<td>密封类</td>
<td>限制子类型，便于穷尽 switch</td>
</tr>
<tr>
<td>语言</td>
<td>Switch 模式匹配</td>
<td>case 中类型模式 + 绑定变量</td>
</tr>
<tr>
<td>平台</td>
<td>强封装、移除 / 弃用</td>
<td>内部 API 不可随意反射，RMI 激活 / Applet 等移除或弃用</td>
</tr>
<tr>
<td>平台</td>
<td>macOS / AArch64、新渲染</td>
<td>支持 Apple Silicon 等</td>
</tr>
<tr>
<td>API / 孵化</td>
<td>新随机数、FFM、向量 API</td>
<td>随机数增强，本地 / 向量为孵化</td>
</tr>
</tbody>
</table>
<p>以上示例在 JDK 17 下可运行。Java 17 是当前主流 LTS 之一，适合作为新项目或从 8 / 11 升级的目标版本。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>新版本特性</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Java17</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 8 新特性总结与示例</title>
    <url>/2025/03/01/2025-03-02-java8-features-summary/</url>
    <content><![CDATA[<h1 id="java-8-新特性总结与示例"><a class="markdownIt-Anchor" href="#java-8-新特性总结与示例"></a> Java 8 新特性总结与示例</h1>
<p>Java 8（2014 年 3 月发布）是自 Java 5 以来变化最大的版本，引入了 Lambda、Stream、新日期时间 API 等，奠定了现代 Java 的写法。本文按「语言特性 → 类库 → 其它」梳理所有主要新特性，并配有可运行示例（基于 JDK 8）。</p>
<hr>
<h2 id="一-lambda-表达式与函数式接口"><a class="markdownIt-Anchor" href="#一-lambda-表达式与函数式接口"></a> 一、Lambda 表达式与函数式接口</h2>
<h3 id="11-lambda-表达式"><a class="markdownIt-Anchor" href="#11-lambda-表达式"></a> 1.1 Lambda 表达式</h3>
<p><strong>作用</strong>：把 “一段行为” 当作参数传递，减少匿名内部类样板代码。</p>
<p>语法形式：<code>(参数列表) -&gt; 表达式或代码块</code>。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"><span class="keyword">import</span> java.util.function.Consumer;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LambdaDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        List&lt;String&gt; list = Arrays.asList(<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 旧写法：匿名内部类</span></span><br><span class="line">        list.forEach(<span class="keyword">new</span> <span class="title class_">Consumer</span>&lt;String&gt;() {</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">accept</span><span class="params">(String s)</span> {</span><br><span class="line">                System.out.println(s);</span><br><span class="line">            }</span><br><span class="line">        });</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Java 8：Lambda</span></span><br><span class="line">        list.forEach(s -&gt; System.out.println(s));</span><br><span class="line">        list.forEach(System.out::println);  <span class="comment">// 方法引用，见下节</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 多参数、多行</span></span><br><span class="line">        List&lt;Integer&gt; nums = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line">        nums.sort((a, b) -&gt; {</span><br><span class="line">            <span class="type">int</span> <span class="variable">diff</span> <span class="operator">=</span> a - b;</span><br><span class="line">            <span class="keyword">return</span> Integer.compare(a, b);</span><br><span class="line">        });</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="12-函数式接口functionalinterface"><a class="markdownIt-Anchor" href="#12-函数式接口functionalinterface"></a> 1.2 函数式接口（@FunctionalInterface）</h3>
<p><strong>定义</strong>：有且仅有一个抽象方法的接口，可用 <code>@FunctionalInterface</code> 标记，便于编译器检查。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">MyCalculator</span> {</span><br><span class="line">    <span class="type">int</span> <span class="title function_">compute</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span>;</span><br><span class="line">    <span class="comment">// 只能有一个抽象方法；default/static 不计入</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line"><span class="type">MyCalculator</span> <span class="variable">add</span> <span class="operator">=</span> (a, b) -&gt; a + b;</span><br><span class="line"><span class="type">MyCalculator</span> <span class="variable">mul</span> <span class="operator">=</span> (a, b) -&gt; a * b;</span><br><span class="line">System.out.println(add.compute(<span class="number">2</span>, <span class="number">3</span>));  <span class="comment">// 5</span></span><br><span class="line">System.out.println(mul.compute(<span class="number">2</span>, <span class="number">3</span>));  <span class="comment">// 6</span></span><br></pre></td></tr></tbody></table></figure>
<p>常见内置函数式接口见第五节。</p>
<hr>
<h2 id="二-方法引用与构造器引用"><a class="markdownIt-Anchor" href="#二-方法引用与构造器引用"></a> 二、方法引用与构造器引用</h2>
<p><strong>作用</strong>：当 Lambda 只是 “调用已有方法” 时，用 <code>::</code> 更简洁。</p>
<p>四种形式：静态方法、实例方法（指定对象）、实例方法（任意对象）、构造器。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.function.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MethodRefDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="comment">// 1. 静态方法引用：类::静态方法</span></span><br><span class="line">        Function&lt;String, Integer&gt; p1 = Integer::parseInt;</span><br><span class="line">        System.out.println(p1.apply(<span class="string">"42"</span>));  <span class="comment">// 42</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 实例方法引用（指定对象）：对象::实例方法</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">s</span> <span class="operator">=</span> <span class="string">"hello"</span>;</span><br><span class="line">        Supplier&lt;Integer&gt; p2 = s::length;</span><br><span class="line">        System.out.println(p2.get());  <span class="comment">// 5</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 实例方法引用（任意对象）：类::实例方法</span></span><br><span class="line">        BiPredicate&lt;String, String&gt; p3 = String::equals;</span><br><span class="line">        System.out.println(p3.test(<span class="string">"a"</span>, <span class="string">"a"</span>));  <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 构造器引用：类::new</span></span><br><span class="line">        Supplier&lt;List&lt;String&gt;&gt; listSupplier = ArrayList::<span class="keyword">new</span>;</span><br><span class="line">        List&lt;String&gt; list = listSupplier.get();</span><br><span class="line"></span><br><span class="line">        Function&lt;Integer, <span class="type">int</span>[]&gt; arrayCreator = <span class="type">int</span>[]::<span class="keyword">new</span>;</span><br><span class="line">        <span class="type">int</span>[] arr = arrayCreator.apply(<span class="number">10</span>);  <span class="comment">// 长度为 10 的 int 数组</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="三-接口的默认方法与静态方法"><a class="markdownIt-Anchor" href="#三-接口的默认方法与静态方法"></a> 三、接口的默认方法与静态方法</h2>
<h3 id="31-默认方法default"><a class="markdownIt-Anchor" href="#31-默认方法default"></a> 3.1 默认方法（default）</h3>
<p><strong>作用</strong>：在接口里写带实现的方法，实现类不强制重写，便于接口演进而不破坏已有实现。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Vehicle</span> {</span><br><span class="line">    <span class="keyword">default</span> <span class="keyword">void</span> <span class="title function_">start</span><span class="params">()</span> {</span><br><span class="line">        System.out.println(<span class="string">"Vehicle started"</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Car</span> <span class="keyword">implements</span> <span class="title class_">Vehicle</span> {</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> {</span><br><span class="line">        System.out.println(<span class="string">"Car running"</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// 可不重写 start()，直接继承默认实现</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<h3 id="32-静态方法static"><a class="markdownIt-Anchor" href="#32-静态方法static"></a> 3.2 静态方法（static）</h3>
<p><strong>作用</strong>：接口也可有静态方法，常用于工具方法。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">MathUtil</span> {</span><br><span class="line">    <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> {</span><br><span class="line">        <span class="keyword">return</span> a + b;</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="variable">sum</span> <span class="operator">=</span> MathUtil.add(<span class="number">1</span>, <span class="number">2</span>);  <span class="comment">// 3</span></span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="四-optional"><a class="markdownIt-Anchor" href="#四-optional"></a> 四、Optional</h2>
<p><strong>作用</strong>：显式表达 “可能没有值”，减少 NPE 和散落的 <code>if (x != null)</code>。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Optional;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OptionalDemo</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="comment">// 创建</span></span><br><span class="line">        Optional&lt;String&gt; empty = Optional.empty();</span><br><span class="line">        Optional&lt;String&gt; present = Optional.of(<span class="string">"hello"</span>);</span><br><span class="line">        Optional&lt;String&gt; nullable = Optional.ofNullable(getMaybeNull());</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 判断与取值</span></span><br><span class="line">        <span class="keyword">if</span> (present.isPresent()) {</span><br><span class="line">            System.out.println(present.get());</span><br><span class="line">        }</span><br><span class="line">        present.ifPresent(System.out::println);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 默认值</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">v1</span> <span class="operator">=</span> empty.orElse(<span class="string">"default"</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">v2</span> <span class="operator">=</span> empty.orElseGet(() -&gt; <span class="string">"computed"</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 链式转换与过滤</span></span><br><span class="line">        Optional&lt;String&gt; upper = present.map(String::toUpperCase);</span><br><span class="line">        Optional&lt;String&gt; filtered = present.filter(s -&gt; s.length() &gt; <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 否则抛异常</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">must</span> <span class="operator">=</span> present.orElseThrow(() -&gt; <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(<span class="string">"missing"</span>));</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> String <span class="title function_">getMaybeNull</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">return</span> Math.random() &gt; <span class="number">0.5</span> ? <span class="string">"ok"</span> : <span class="literal">null</span>;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="五-javautilfunction-常用函数式接口"><a class="markdownIt-Anchor" href="#五-javautilfunction-常用函数式接口"></a> 五、java.util.function 常用函数式接口</h2>
<table>
<thead>
<tr>
<th>接口</th>
<th>抽象方法</th>
<th>典型用途</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>Predicate&lt;T&gt;</code></td>
<td><code>boolean test(T t)</code></td>
<td>过滤、判断</td>
</tr>
<tr>
<td><code>Function&lt;T,R&gt;</code></td>
<td><code>R apply(T t)</code></td>
<td>转换、映射</td>
</tr>
<tr>
<td><code>Consumer&lt;T&gt;</code></td>
<td><code>void accept(T t)</code></td>
<td>消费、副作用</td>
</tr>
<tr>
<td><code>Supplier&lt;T&gt;</code></td>
<td><code>T get()</code></td>
<td>无参生成值</td>
</tr>
<tr>
<td><code>UnaryOperator&lt;T&gt;</code></td>
<td><code>T apply(T t)</code></td>
<td>一元运算</td>
</tr>
<tr>
<td><code>BiFunction&lt;T,U,R&gt;</code></td>
<td><code>R apply(T t, U u)</code></td>
<td>二元函数</td>
</tr>
</tbody>
</table>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">Predicate&lt;Integer&gt; even = n -&gt; n % <span class="number">2</span> == <span class="number">0</span>;</span><br><span class="line">Function&lt;String, Integer&gt; len = String::length;</span><br><span class="line">Consumer&lt;String&gt; print = System.out::println;</span><br><span class="line">Supplier&lt;Double&gt; random = Math::random;</span><br><span class="line"></span><br><span class="line">System.out.println(even.test(<span class="number">4</span>));       <span class="comment">// true</span></span><br><span class="line">System.out.println(len.apply(<span class="string">"abc"</span>));   <span class="comment">// 3</span></span><br><span class="line">print.accept(<span class="string">"hi"</span>);</span><br><span class="line">System.out.println(random.get());</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="六-stream-api"><a class="markdownIt-Anchor" href="#六-stream-api"></a> 六、Stream API</h2>
<p><strong>作用</strong>：对集合做声明式 “流式” 操作（过滤、映射、排序、归约等），支持顺序与并行。</p>
<h3 id="61-创建流"><a class="markdownIt-Anchor" href="#61-创建流"></a> 6.1 创建流</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"><span class="keyword">import</span> java.util.stream.*;</span><br><span class="line"></span><br><span class="line">List&lt;String&gt; list = Arrays.asList(<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>);</span><br><span class="line">Stream&lt;String&gt; s1 = list.stream();</span><br><span class="line">Stream&lt;String&gt; s2 = list.parallelStream();</span><br><span class="line">Stream&lt;String&gt; s3 = Stream.of(<span class="string">"x"</span>, <span class="string">"y"</span>);</span><br><span class="line">Stream&lt;Integer&gt; s4 = Stream.iterate(<span class="number">0</span>, n -&gt; n + <span class="number">1</span>).limit(<span class="number">5</span>);  <span class="comment">// 0,1,2,3,4</span></span><br><span class="line">Stream&lt;Double&gt; s5 = Stream.generate(Math::random).limit(<span class="number">3</span>);</span><br></pre></td></tr></tbody></table></figure>
<h3 id="62-中间操作与终止操作"><a class="markdownIt-Anchor" href="#62-中间操作与终止操作"></a> 6.2 中间操作与终止操作</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 过滤、映射、排序、去重、跳过、限制</span></span><br><span class="line">List&lt;Integer&gt; nums = Arrays.asList(<span class="number">3</span>, <span class="number">1</span>, <span class="number">4</span>, <span class="number">1</span>, <span class="number">5</span>, <span class="number">9</span>, <span class="number">2</span>, <span class="number">6</span>);</span><br><span class="line"></span><br><span class="line">List&lt;Integer&gt; result = nums.stream()</span><br><span class="line">    .filter(n -&gt; n &gt; <span class="number">2</span>)</span><br><span class="line">    .map(n -&gt; n * <span class="number">2</span>)</span><br><span class="line">    .distinct()</span><br><span class="line">    .sorted()</span><br><span class="line">    .skip(<span class="number">1</span>)</span><br><span class="line">    .limit(<span class="number">3</span>)</span><br><span class="line">    .collect(Collectors.toList());</span><br><span class="line"><span class="comment">// 结果依赖顺序，此处示例为 [8, 10, 12] 一类</span></span><br></pre></td></tr></tbody></table></figure>
<h3 id="63-常用终止操作"><a class="markdownIt-Anchor" href="#63-常用终止操作"></a> 6.3 常用终止操作</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="type">long</span> <span class="variable">count</span> <span class="operator">=</span> nums.stream().count();</span><br><span class="line">Optional&lt;Integer&gt; max = nums.stream().max(Integer::compareTo);</span><br><span class="line">Optional&lt;Integer&gt; min = nums.stream().min(Integer::compareTo);</span><br><span class="line"><span class="type">boolean</span> <span class="variable">any</span> <span class="operator">=</span> nums.stream().anyMatch(n -&gt; n &gt; <span class="number">5</span>);</span><br><span class="line"><span class="type">boolean</span> <span class="variable">all</span> <span class="operator">=</span> nums.stream().allMatch(n -&gt; n &gt; <span class="number">0</span>);</span><br><span class="line">Optional&lt;Integer&gt; first = nums.stream().findFirst();</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="variable">sum</span> <span class="operator">=</span> nums.stream().mapToInt(Integer::intValue).sum();</span><br><span class="line">Optional&lt;Integer&gt; reduce = nums.stream().reduce((a, b) -&gt; a + b);</span><br></pre></td></tr></tbody></table></figure>
<h3 id="64-收集器-collectors"><a class="markdownIt-Anchor" href="#64-收集器-collectors"></a> 6.4 收集器 Collectors</h3>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">List&lt;String&gt; names = Arrays.asList(<span class="string">"Alice"</span>, <span class="string">"Bob"</span>, <span class="string">"Charlie"</span>);</span><br><span class="line"><span class="type">String</span> <span class="variable">joined</span> <span class="operator">=</span> names.stream().collect(Collectors.joining(<span class="string">", "</span>));</span><br><span class="line">Map&lt;Integer, List&lt;String&gt;&gt; byLen = names.stream()</span><br><span class="line">    .collect(Collectors.groupingBy(String::length));</span><br><span class="line">Map&lt;Boolean, List&lt;String&gt;&gt; partition = names.stream()</span><br><span class="line">    .collect(Collectors.partitioningBy(s -&gt; s.length() &gt; <span class="number">4</span>));</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="七-新的日期时间-apijavatime"><a class="markdownIt-Anchor" href="#七-新的日期时间-apijavatime"></a> 七、新的日期时间 API（java.time）</h2>
<p><strong>作用</strong>：替代易错的 <code>Date</code>/<code>Calendar</code>，提供不可变、线程安全的日期时间类型。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.time.*;</span><br><span class="line"><span class="keyword">import</span> java.time.format.DateTimeFormatter;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 本地日期、时间、日期时间</span></span><br><span class="line"><span class="type">LocalDate</span> <span class="variable">date</span> <span class="operator">=</span> LocalDate.now();</span><br><span class="line"><span class="type">LocalDate</span> <span class="variable">date2</span> <span class="operator">=</span> LocalDate.of(<span class="number">2025</span>, <span class="number">3</span>, <span class="number">2</span>);</span><br><span class="line"><span class="type">LocalTime</span> <span class="variable">time</span> <span class="operator">=</span> LocalTime.of(<span class="number">14</span>, <span class="number">30</span>);</span><br><span class="line"><span class="type">LocalDateTime</span> <span class="variable">dateTime</span> <span class="operator">=</span> LocalDateTime.of(date2, time);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 解析与格式化</span></span><br><span class="line"><span class="type">DateTimeFormatter</span> <span class="variable">fmt</span> <span class="operator">=</span> DateTimeFormatter.ofPattern(<span class="string">"yyyy-MM-dd HH:mm"</span>);</span><br><span class="line"><span class="type">String</span> <span class="variable">str</span> <span class="operator">=</span> dateTime.format(fmt);</span><br><span class="line"><span class="type">LocalDateTime</span> <span class="variable">parsed</span> <span class="operator">=</span> LocalDateTime.parse(<span class="string">"2025-03-02 14:30"</span>, fmt);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 加减</span></span><br><span class="line"><span class="type">LocalDate</span> <span class="variable">tomorrow</span> <span class="operator">=</span> date.plusDays(<span class="number">1</span>);</span><br><span class="line"><span class="type">LocalDateTime</span> <span class="variable">before</span> <span class="operator">=</span> dateTime.minusHours(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 带时区</span></span><br><span class="line"><span class="type">ZonedDateTime</span> <span class="variable">zoned</span> <span class="operator">=</span> ZonedDateTime.now(ZoneId.of(<span class="string">"Asia/Shanghai"</span>));</span><br><span class="line"><span class="type">Instant</span> <span class="variable">instant</span> <span class="operator">=</span> Instant.now();</span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="八-重复注解与类型注解"><a class="markdownIt-Anchor" href="#八-重复注解与类型注解"></a> 八、重复注解与类型注解</h2>
<h3 id="81-重复注解repeating-annotations"><a class="markdownIt-Anchor" href="#81-重复注解repeating-annotations"></a> 8.1 重复注解（Repeating Annotations）</h3>
<p><strong>作用</strong>：同一注解可在同一位置多次使用，内部用 “容器注解” 保存。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.annotation.*;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Repeatable(Roles.class)</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@interface</span> Role {</span><br><span class="line">    String <span class="title function_">value</span><span class="params">()</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span></span><br><span class="line"><span class="meta">@interface</span> Roles {</span><br><span class="line">    Role[] value();</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="meta">@Role("admin")</span></span><br><span class="line"><span class="meta">@Role("user")</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">User</span> { }</span><br></pre></td></tr></tbody></table></figure>
<h3 id="82-类型注解type-annotations"><a class="markdownIt-Anchor" href="#82-类型注解type-annotations"></a> 8.2 类型注解（Type Annotations）</h3>
<p><strong>作用</strong>：注解可写在类型使用处（如泛型、类型转换），用于更强类型检查或工具分析。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">// 需配合 Checker Framework 等使用，此处仅展示语法位置</span></span><br><span class="line"><span class="comment">// List&lt;@NonNull String&gt; list;</span></span><br><span class="line"><span class="comment">// String s = (@NonNull String) obj;</span></span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="九-base64-编码解码"><a class="markdownIt-Anchor" href="#九-base64-编码解码"></a> 九、Base64 编码解码</h2>
<p><strong>作用</strong>：标准库内置 Base64，无需第三方库。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Base64;</span><br><span class="line"></span><br><span class="line"><span class="type">String</span> <span class="variable">text</span> <span class="operator">=</span> <span class="string">"Hello, Java 8!"</span>;</span><br><span class="line"><span class="type">String</span> <span class="variable">encoded</span> <span class="operator">=</span> Base64.getEncoder().encodeToString(text.getBytes());</span><br><span class="line">System.out.println(encoded);</span><br><span class="line"></span><br><span class="line"><span class="type">byte</span>[] decoded = Base64.getDecoder().decode(encoded);</span><br><span class="line">System.out.println(<span class="keyword">new</span> <span class="title class_">String</span>(decoded));</span><br><span class="line"></span><br><span class="line"><span class="comment">// URL 安全、MIME 等变体：Base64.getUrlEncoder() / getMimeEncoder()</span></span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="十-nashorn-javascript-引擎"><a class="markdownIt-Anchor" href="#十-nashorn-javascript-引擎"></a> 十、Nashorn JavaScript 引擎</h2>
<p><strong>作用</strong>：在 JVM 内执行 JavaScript（Java 15+ 已移除，仅作历史特性了解）。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.script.*;</span><br><span class="line"></span><br><span class="line"><span class="type">ScriptEngine</span> <span class="variable">engine</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ScriptEngineManager</span>().getEngineByName(<span class="string">"nashorn"</span>);</span><br><span class="line">engine.eval(<span class="string">"var greeting = 'Hello from JS'; greeting"</span>);</span><br><span class="line"><span class="type">Object</span> <span class="variable">result</span> <span class="operator">=</span> engine.eval(<span class="string">"1 + 2"</span>);</span><br><span class="line">System.out.println(result);  <span class="comment">// 3.0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用 JS 函数</span></span><br><span class="line">engine.eval(<span class="string">"function add(a, b) { return a + b; }"</span>);</span><br><span class="line"><span class="type">Invocable</span> <span class="variable">invocable</span> <span class="operator">=</span> (Invocable) engine;</span><br><span class="line"><span class="type">Object</span> <span class="variable">sum</span> <span class="operator">=</span> invocable.invokeFunction(<span class="string">"add"</span>, <span class="number">10</span>, <span class="number">20</span>);</span><br><span class="line">System.out.println(sum);  <span class="comment">// 30.0</span></span><br></pre></td></tr></tbody></table></figure>
<hr>
<h2 id="十一-其它改进"><a class="markdownIt-Anchor" href="#十一-其它改进"></a> 十一、其它改进</h2>
<ul>
<li><strong>并行数组排序</strong>：<code>Arrays.parallelSort(...)</code> 大数组排序时可利用多核。</li>
<li><strong>HashMap 冲突优化</strong>：桶内链表过长时转为红黑树，减少极端情况下的性能退化。</li>
<li><strong>Compact Profiles</strong>：Java SE 8 提供 compact1/2/3 子集，便于小型设备。</li>
<li><strong>参数名反射</strong>：通过 <code>Parameter</code> 可获取方法参数名（需编译时加 <code>-parameters</code>）。</li>
</ul>
<hr>
<h2 id="十二-小结"><a class="markdownIt-Anchor" href="#十二-小结"></a> 十二、小结</h2>
<table>
<thead>
<tr>
<th>类别</th>
<th>特性</th>
<th>一句话</th>
</tr>
</thead>
<tbody>
<tr>
<td>语言</td>
<td>Lambda</td>
<td>把行为当参数，简化匿名类</td>
</tr>
<tr>
<td>语言</td>
<td>方法 / 构造器引用</td>
<td><code>::</code> 简化 “只调一个方法” 的 Lambda</td>
</tr>
<tr>
<td>语言</td>
<td>接口 default/static</td>
<td>接口可带实现，便于演进</td>
</tr>
<tr>
<td>语言</td>
<td>重复 / 类型注解</td>
<td>多次注解、类型上的注解</td>
</tr>
<tr>
<td>类库</td>
<td>Optional</td>
<td>显式 “可能无值”，减少 NPE</td>
</tr>
<tr>
<td>类库</td>
<td>Stream API</td>
<td>声明式集合操作，支持并行</td>
</tr>
<tr>
<td>类库</td>
<td>java.util.function</td>
<td>常用函数式接口</td>
</tr>
<tr>
<td>类库</td>
<td>java.time</td>
<td>新日期时间 API</td>
</tr>
<tr>
<td>类库</td>
<td>Base64</td>
<td>内置编码解码</td>
</tr>
<tr>
<td>其它</td>
<td>Nashorn、并行排序、HashMap 等</td>
<td>脚本、性能与平台优化</td>
</tr>
</tbody>
</table>
<p>以上示例均在 JDK 8 下可运行（Nashorn 在 JDK 15+ 需单独依赖或改用 GraalJS）。掌握这些特性即可覆盖日常开发中绝大部分 Java 8 用法。</p>
]]></content>
      <categories>
        <category>Java</category>
        <category>新版本特性</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>Java8</tag>
      </tags>
  </entry>
</search>
